generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model User {
  id                                String                           @id @default(cuid())
  code                              String                           @unique
  name                              String
  email                             String                           @unique
  normalizedEmail                   String?                          @map("normalized_email") @db.VarChar(320)
  phone                             String
  role                              String
  status                            String
  department                        String?
  joinDate                          String
  points                            Int                              @default(0)
  rating                            Float                            @default(0)
  passwordHash                      String
  createdAt                         DateTime                         @default(now())
  updatedAt                         DateTime                         @updatedAt
  profilePhotoUrl                   String?
  employeeProfile                   Json?
  commercial_audit_logs             CommercialAuditLog[]
  leadAuditLogs                     LeadAuditLog[]                   @relation("LeadAuditActor")
  leadVolumeEstimatesUpdated        LeadVolumeEstimate[]             @relation("LeadVolumeEstimateUpdatedBy")
  pipelineCasesOwned                PipelineCase[]
  projectPgdsApplied                ProjectPgd[]                     @relation("ProjectPgdAppliedBy")
  projectPgdItemsValidated          ProjectPgdItem[]                 @relation("ProjectPgdItemValidatedBy")
  projectSignalsAcked               ProjectSignal[]                  @relation("ProjectSignalAckBy")
  templateVersionsApproved          TemplateVersion[]                @relation("TemplateApprovedBy")
  templateVersionsCreated           TemplateVersion[]                @relation("TemplateCreatedBy")
  templateVersionsRejected          TemplateVersion[]                @relation("TemplateRejectedBy")
  tipoServicioConfigsUpdated        TipoServicioConfig[]             @relation("TipoServicioConfigUpdatedBy")
  volumeAreaProfilesUpdated         VolumeAreaProfile[]              @relation("VolumeAreaProfileUpdatedBy")
  tenantMemberships                 TenantMembership[]
  acceptedProvisioningInvitations   EmployeeProvisioningInvitation[] @relation("EmployeeProvisioningInvitationAcceptedUser")
  activatedAdminIdentityInvitations AdminIdentityInvitation[]        @relation("AdminIdentityInvitationActivatedUser")

  @@map("osi_users")
}

model Tenant {
  id                               String                             @id @default(cuid())
  code                             String                             @unique @db.VarChar(64)
  name                             String                             @db.VarChar(160)
  legalName                        String?                            @map("legal_name") @db.VarChar(200)
  countryCode                      String?                            @map("country_code") @db.Char(2)
  timezone                         String                             @default("America/Santo_Domingo") @db.VarChar(64)
  defaultCurrency                  String                             @default("DOP") @map("default_currency") @db.Char(3)
  status                           TenantStatus                       @default(ACTIVE)
  provisioningSource               TenantProvisioningSource           @default(MANUAL) @map("provisioning_source")
  provisioningBatchId              String?                            @map("provisioning_batch_id")
  createdAt                        DateTime                           @default(now()) @map("created_at")
  updatedAt                        DateTime                           @updatedAt @map("updated_at")
  approval_requests                ApprovalRequest[]
  commercial_audit_logs            CommercialAuditLog[]
  crate_calculation_snapshots      crate_calculation_snapshots[]
  crate_settings_versions          crate_settings_versions[]
  logistic_override_approvals      LogisticOverrideApproval[]
  logistics_configuration_versions logistics_configuration_versions[]
  osi_geo_regions                  GeoRegion[]
  osi_transport_zone_rules         TransportZoneRule[]
  osi_vehicle_engine_settings      VehicleEngineSettings[]
  osi_vehicles                     Vehicle[]
  osi_zone_rules                   ZoneRule[]
  quote_change_order_policies      quote_change_order_policies[]
  quote_change_order_sequences     quote_change_order_sequences?
  quote_change_order_subjects      quote_change_order_subjects[]
  quote_change_orders              QuoteChangeOrder[]
  risk_engine_rules                risk_engine_rules[]
  risk_engine_settings             risk_engine_settings?
  risk_evaluations                 risk_evaluations[]
  employeeProfiles                 EmployeeProfile[]
  employeeProvisioningRequests     EmployeeProvisioningRequest[]
  employeeProvisioningInvitations  EmployeeProvisioningInvitation[]
  employeeAdminRoleProposals       EmployeeAdminRoleProposal[]
  adminIdentityInvitations         AdminIdentityInvitation[]
  clients                          Client[]
  clientAddresses                  ClientAddress[]
  projects                         Project[]
  leads                            Lead[]
  pipelineCases                    PipelineCase[]
  pipelineCaseRouteSnapshots       PipelineCaseRouteSnapshot[]
  pipelineCaseCommands             PipelineCaseCommand[]
  serviceCatalogItems              ServiceCatalogItem[]
  serviceCatalogCompatibilities    ServiceCatalogCompatibility[]
  serviceDefaultCombinations       ServiceDefaultCombination[]
  serviceDefaultCombinationItems   ServiceDefaultCombinationItem[]
  pipelineCaseServiceRevisions     PipelineCaseServiceRevision[]
  pipelineCaseServiceItems         PipelineCaseServiceItem[]
  serviceMutationCommands          ServiceMutationCommand[]
  surveyCatalogVersions            SurveyCatalogVersion[]
  surveyArticleCatalogItems        SurveyArticleCatalogItem[]
  surveyAreaCatalogItems           SurveyAreaCatalogItem[]
  surveyConditionCatalogItems      SurveyConditionCatalogItem[]
  surveyAssignments                SurveyAssignment[]
  surveyDrafts                     SurveyDraft[]
  surveyDraftItems                 SurveyDraftItem[]
  surveyAccessObservations         SurveyAccessObservation[]
  surveyBlobObjects                SurveyBlobObject[]
  surveyPhotos                     SurveyPhoto[]
  surveyPublications               SurveyPublication[]
  surveyPublicationItems           SurveyPublicationItem[]
  surveyPublicationAccess          SurveyPublicationAccess[]
  surveySignatures                 SurveyPublicationSignature[]
  surveyMutationCommands           SurveyMutationCommand[]
  materialUnits                    MaterialUnit[]
  materialCatalogItems             MaterialCatalogItem[]
  materialUnitConversions          MaterialUnitConversion[]
  materialCostVersions             MaterialCostVersion[]
  materialSupplierReferences       MaterialSupplierReference[]
  materialWarehouses               MaterialWarehouse[]
  materialLocations                MaterialLocation[]
  materialInventoryCommands        MaterialInventoryCommand[]
  materialInventoryMovements       MaterialInventoryMovement[]
  materialReservations             MaterialReservation[]
  materialReservationEvents        MaterialReservationEvent[]
  packingRecipes                   PackingRecipe[]
  packingRecipeVersions            PackingRecipeVersion[]
  packingRecipeLines               PackingRecipeLine[]
  materialRequirementSnapshots     MaterialRequirementSnapshot[]
  materialRequirementLines         MaterialRequirementLine[]
  materialPurchaseRequests         MaterialPurchaseRequest[]
  assetModels                      AssetModel[]
  assetInstances                   AssetInstance[]
  assetCodeCounters                AssetCodeCounter[]
  assetCostVersions                AssetCostVersion[]
  assetReservations                AssetReservation[]
  assetAssignments                 AssetAssignment[]
  assetInspections                 AssetInspection[]
  assetIncidents                   AssetIncident[]
  assetMaintenanceRules            AssetMaintenanceRule[]
  assetMaintenanceOrders           AssetMaintenanceOrder[]
  assetHistoryEvents               AssetHistoryEvent[]
  assetMutationCommands            AssetMutationCommand[]
  externalResourceOffers           ExternalResourceOffer[]
  externalResourceReservations     ExternalResourceReservation[]
  logisticsRules                   LogisticsRule[]
  logisticsCalculations            LogisticsCalculation[]
  logisticsPlans                   LogisticsPlan[]
  logisticsPlanRevisions           LogisticsPlanRevision[]
  logisticsPlanItems               LogisticsPlanItem[]
  logisticsPlanIssues              LogisticsPlanIssue[]
  logisticsPlanOverrides           LogisticsPlanOverride[]
  logisticsMutationCommands        LogisticsMutationCommand[]
  costingRules                     CostingRule[]
  costingExchangeRates             CostingExchangeRate[]
  costingCalculations              CostingCalculation[]
  costingRevisions                 CostingRevision[]
  costingLines                     CostingLine[]
  costingIssues                    CostingIssue[]
  costingOverrides                 CostingOverride[]
  costingMarginAuthorizations      CostingMarginAuthorization[]
  costingMutationCommands          CostingMutationCommand[]
  quoteProposals                   QuoteProposal[]
  quoteProposalRevisions           QuoteProposalRevision[]
  quoteLines                       QuoteLine[]
  quoteIssues                      QuoteIssue[]
  quoteDispatches                  QuoteDispatch[]
  quoteClientDecisions             QuoteClientDecision[]
  quoteMutationCommands            QuoteMutationCommand[]
  quoteReferenceCounters           QuoteReferenceCounter[]
  memberships                      TenantMembership[]
  vehicle_import_batches           vehicle_import_batches[]
  vehicle_import_items             vehicle_import_items[]
  pipelineCaseQuotes               PipelineCaseQuote[]

  @@index([status])
  @@index([provisioningBatchId])
  @@map("tenants")
}

model TenantMembership {
  id                                                                                                                                            String                             @id @default(cuid())
  publicRef                                                                                                                                     String                             @default(dbgenerated("gen_random_uuid()")) @map("public_ref") @db.Uuid
  tenantId                                                                                                                                      String                             @map("tenant_id")
  userId                                                                                                                                        String                             @map("user_id")
  role                                                                                                                                          TenantMembershipRole
  status                                                                                                                                        TenantMembershipStatus             @default(ACTIVE)
  grantedPermissions                                                                                                                            String[]                           @default([]) @map("granted_permissions")
  deniedPermissions                                                                                                                             String[]                           @default([]) @map("denied_permissions")
  isDefault                                                                                                                                     Boolean                            @default(false) @map("is_default")
  authorizationVersion                                                                                                                          Int                                @default(1) @map("authorization_version")
  firstAccessAt                                                                                                                                 DateTime?                          @map("first_access_at")
  lastAccessAt                                                                                                                                  DateTime?                          @map("last_access_at")
  provisioningSource                                                                                                                            TenantProvisioningSource           @default(MANUAL) @map("provisioning_source")
  provisioningBatchId                                                                                                                           String?                            @map("provisioning_batch_id")
  createdAt                                                                                                                                     DateTime                           @default(now()) @map("created_at")
  updatedAt                                                                                                                                     DateTime                           @updatedAt @map("updated_at")
  approval_requests_approval_requests_tenant_id_assigned_approver_membership_id_assigned_approver_user_idTotenant_memberships                   ApprovalRequest[]                  @relation("approval_requests_tenant_id_assigned_approver_membership_id_assigned_approver_user_idTotenant_memberships")
  approval_requests_approval_requests_tenant_id_decider_membership_id_decider_user_idTotenant_memberships                                       ApprovalRequest[]                  @relation("approval_requests_tenant_id_decider_membership_id_decider_user_idTotenant_memberships")
  approval_requests_approval_requests_tenant_id_requester_membership_id_requester_user_idTotenant_memberships                                   ApprovalRequest[]                  @relation("approval_requests_tenant_id_requester_membership_id_requester_user_idTotenant_memberships")
  commercial_audit_logs                                                                                                                         CommercialAuditLog[]
  crate_calculation_snapshots                                                                                                                   crate_calculation_snapshots[]
  crate_settings_versions_crate_settings_versions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships                   crate_settings_versions[]          @relation("crate_settings_versions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships")
  crate_settings_versions_crate_settings_versions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships                     crate_settings_versions[]          @relation("crate_settings_versions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships")
  logistics_configuration_versions_logistics_configuration_versions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships logistics_configuration_versions[] @relation("logistics_configuration_versions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships")
  logistics_configuration_versions_logistics_configuration_versions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships   logistics_configuration_versions[] @relation("logistics_configuration_versions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships")
  osi_geo_regions_osi_geo_regions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships                                   GeoRegion[]                        @relation("osi_geo_regions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships")
  osi_geo_regions_osi_geo_regions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships                                     GeoRegion[]                        @relation("osi_geo_regions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships")
  osi_transport_zone_rules_osi_transport_zone_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships                 TransportZoneRule[]                @relation("osi_transport_zone_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships")
  osi_transport_zone_rules_osi_transport_zone_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships                   TransportZoneRule[]                @relation("osi_transport_zone_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships")
  osi_vehicle_engine_settings_osi_vehicle_engine_settings_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships           VehicleEngineSettings[]            @relation("osi_vehicle_engine_settings_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships")
  osi_vehicle_engine_settings_osi_vehicle_engine_settings_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships             VehicleEngineSettings[]            @relation("osi_vehicle_engine_settings_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships")
  osi_vehicles                                                                                                                                  Vehicle[]
  osi_zone_rules_osi_zone_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships                                     ZoneRule[]                         @relation("osi_zone_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships")
  osi_zone_rules_osi_zone_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships                                       ZoneRule[]                         @relation("osi_zone_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships")
  quote_change_order_commands                                                                                                                   quote_change_order_commands[]
  quote_change_order_policies                                                                                                                   quote_change_order_policies[]
  quote_change_order_subjects                                                                                                                   quote_change_order_subjects[]
  quote_change_orders_quote_change_orders_tenant_id_executed_by_membership_id_executed_by_user_idTotenant_memberships                           QuoteChangeOrder[]                 @relation("quote_change_orders_tenant_id_executed_by_membership_id_executed_by_user_idTotenant_memberships")
  quote_change_orders_quote_change_orders_tenant_id_requested_by_membership_id_requested_by_user_idTotenant_memberships                         QuoteChangeOrder[]                 @relation("quote_change_orders_tenant_id_requested_by_membership_id_requested_by_user_idTotenant_memberships")
  risk_engine_rules_risk_engine_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships                               risk_engine_rules[]                @relation("risk_engine_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships")
  risk_engine_rules_risk_engine_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships                                 risk_engine_rules[]                @relation("risk_engine_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships")
  risk_engine_settings                                                                                                                          risk_engine_settings[]             @relation("risk_engine_settings_actor") @ignore
  risk_evaluations                                                                                                                              risk_evaluations[]
  tenant                                                                                                                                        Tenant                             @relation(fields: [tenantId], references: [id])
  user                                                                                                                                          User                               @relation(fields: [userId], references: [id])
  vehicle_import_batches_vehicle_import_batches_tenant_id_confirmed_by_membership_id_confirmed_by_user_idTotenant_memberships                   vehicle_import_batches[]           @relation("vehicle_import_batches_tenant_id_confirmed_by_membership_id_confirmed_by_user_idTotenant_memberships")
  vehicle_import_batches_vehicle_import_batches_tenant_id_rolled_back_by_membership_id_rolled_back_by_user_idTotenant_memberships               vehicle_import_batches[]           @relation("vehicle_import_batches_tenant_id_rolled_back_by_membership_id_rolled_back_by_user_idTotenant_memberships")
  authSessions                                                                                                                                  AuthSession[]                      @relation("AuthSessionMembership")
  employeeProfile                                                                                                                               EmployeeProfile?                   @relation("EmployeeProfileMembership")
  supervisedEmployeeProfiles                                                                                                                    EmployeeProfile[]                  @relation("EmployeeProfileSupervisor")
  provisionedEmployeeRequests                                                                                                                   EmployeeProvisioningRequest[]      @relation("EmployeeProvisioningRequestProvisionedMembership")
  issuedProvisioningInvitations                                                                                                                 EmployeeProvisioningInvitation[]   @relation("EmployeeProvisioningInvitationIssuer")
  issuedAdminIdentityInvitations                                                                                                                AdminIdentityInvitation[]          @relation("AdminIdentityInvitationIssuer")
  activatedAdminIdentityInvitations                                                                                                             AdminIdentityInvitation[]          @relation("AdminIdentityInvitationActivatedMembership")
  proposedEmployeeAdminRoles                                                                                                                    EmployeeAdminRoleProposal[]        @relation("EmployeeAdminRoleProposalProposer")
  supervisedProvisioningRequests                                                                                                                EmployeeProvisioningRequest[]      @relation("EmployeeProvisioningRequestSupervisor")
  enterpriseOwnedPipelineCases                                                                                                                  PipelineCase[]                     @relation("PipelineCaseEnterpriseOwner")
  pipelineCaseCommandsAsActor                                                                                                                   PipelineCaseCommand[]              @relation("PipelineCaseCommandActor")
  pipelineCaseCommandsAsPreviousOwner                                                                                                           PipelineCaseCommand[]              @relation("PipelineCaseCommandPreviousOwner")
  pipelineCaseCommandsAsResultingOwner                                                                                                          PipelineCaseCommand[]              @relation("PipelineCaseCommandResultingOwner")
  serviceMutationCommandsAsActor                                                                                                                ServiceMutationCommand[]           @relation("ServiceMutationCommandActor")
  serviceRevisionsCreated                                                                                                                       PipelineCaseServiceRevision[]      @relation("PipelineCaseServiceRevisionActor")
  surveyCatalogVersionsCreated                                                                                                                  SurveyCatalogVersion[]             @relation("SurveyCatalogVersionActor")
  surveyAssignmentsCreated                                                                                                                      SurveyAssignment[]                 @relation("SurveyAssignmentCreator")
  surveyAssignmentsEvaluated                                                                                                                    SurveyAssignment[]                 @relation("SurveyAssignmentEvaluator")
  surveyPublicationsCreated                                                                                                                     SurveyPublication[]                @relation("SurveyPublicationActor")
  surveyMutationCommandsAsActor                                                                                                                 SurveyMutationCommand[]            @relation("SurveyMutationCommandActor")
  materialInventoryCommandsAsActor                                                                                                              MaterialInventoryCommand[]         @relation("MaterialInventoryCommandActor")
  materialInventoryMovementsAsActor                                                                                                             MaterialInventoryMovement[]        @relation("MaterialInventoryMovementActor")
  materialReservationEventsAsActor                                                                                                              MaterialReservationEvent[]         @relation("MaterialReservationEventActor")
  materialCostVersionsAsActor                                                                                                                   MaterialCostVersion[]              @relation("MaterialCostVersionActor")
  packingRecipeVersionsAsActor                                                                                                                  PackingRecipeVersion[]             @relation("PackingRecipeVersionActor")
  materialRequirementSnapshotsAsActor                                                                                                           MaterialRequirementSnapshot[]      @relation("MaterialRequirementSnapshotActor")
  materialPurchaseRequestsAsActor                                                                                                               MaterialPurchaseRequest[]          @relation("MaterialPurchaseRequestActor")
  assetCommandsAsActor                                                                                                                          AssetMutationCommand[]             @relation("AssetMutationCommandActor")
  assetReservationsAsActor                                                                                                                      AssetReservation[]                 @relation("AssetReservationActor")
  assetAssignmentsAsActor                                                                                                                       AssetAssignment[]                  @relation("AssetAssignmentActor")
  assetAssignmentsAsAssignee                                                                                                                    AssetAssignment[]                  @relation("AssetAssignmentAssignee")
  assetAssignmentsAsCustodian                                                                                                                   AssetAssignment[]                  @relation("AssetAssignmentCustodian")
  assetInspectionsAsActor                                                                                                                       AssetInspection[]                  @relation("AssetInspectionActor")
  assetIncidentsAsActor                                                                                                                         AssetIncident[]                    @relation("AssetIncidentActor")
  assetMaintenanceOrdersAsActor                                                                                                                 AssetMaintenanceOrder[]            @relation("AssetMaintenanceOrderActor")
  assetHistoryEventsAsActor                                                                                                                     AssetHistoryEvent[]                @relation("AssetHistoryEventActor")
  externalResourceReservationsAsActor                                                                                                           ExternalResourceReservation[]      @relation("ExternalResourceReservationActor")
  logisticsRulesAsActor                                                                                                                         LogisticsRule[]                    @relation("LogisticsRuleActor")
  logisticsCalculationsAsActor                                                                                                                  LogisticsCalculation[]             @relation("LogisticsCalculationActor")
  logisticsRevisionsAsActor                                                                                                                     LogisticsPlanRevision[]            @relation("LogisticsRevisionActor")
  logisticsIssuesResolved                                                                                                                       LogisticsPlanIssue[]               @relation("LogisticsIssueResolver")
  logisticsOverridesAsActor                                                                                                                     LogisticsPlanOverride[]            @relation("LogisticsOverrideActor")
  logisticsCommandsAsActor                                                                                                                      LogisticsMutationCommand[]         @relation("LogisticsCommandActor")
  costingRulesAsActor                                                                                                                           CostingRule[]                      @relation("CostingRuleActor")
  costingExchangeRatesAsActor                                                                                                                   CostingExchangeRate[]              @relation("CostingExchangeRateActor")
  costingCalculationsAsActor                                                                                                                    CostingCalculation[]               @relation("CostingCalculationActor")
  costingRevisionsAsActor                                                                                                                       CostingRevision[]                  @relation("CostingRevisionActor")
  costingIssuesResolved                                                                                                                         CostingIssue[]                     @relation("CostingIssueResolver")
  costingOverridesAsActor                                                                                                                       CostingOverride[]                  @relation("CostingOverrideActor")
  costingMarginAuthorizationsAsActor                                                                                                            CostingMarginAuthorization[]       @relation("CostingMarginAuthorizationActor")
  costingCommandsAsActor                                                                                                                        CostingMutationCommand[]           @relation("CostingCommandActor")
  quoteProposalRevisionsAsActor                                                                                                                 QuoteProposalRevision[]            @relation("QuoteProposalRevisionActor")
  quoteDispatchesAsActor                                                                                                                        QuoteDispatch[]                    @relation("QuoteDispatchActor")
  quoteClientDecisionsAsActor                                                                                                                   QuoteClientDecision[]              @relation("QuoteClientDecisionActor")
  quoteMutationCommandsAsActor                                                                                                                  QuoteMutationCommand[]             @relation("QuoteMutationCommandActor")

  @@unique([tenantId, userId])
  @@unique([tenantId, publicRef], map: "tenant_memberships_tenant_id_public_ref_key")
  @@unique([tenantId, id, userId])
  @@index([tenantId, status, role])
  @@index([userId, status])
  @@index([userId, isDefault])
  @@index([provisioningBatchId])
  @@map("tenant_memberships")
}

model EmployeeProfile {
  id                     String                     @id @default(cuid())
  tenantId               String                     @map("tenant_id")
  membershipId           String                     @map("membership_id")
  userId                 String                     @map("user_id")
  employeeCode           String                     @map("employee_code") @db.VarChar(64)
  normalizedEmployeeCode String                     @map("normalized_employee_code") @db.VarChar(64)
  jobTitle               String?                    @map("job_title") @db.VarChar(120)
  departmentCode         String?                    @map("department_code") @db.VarChar(64)
  employmentStatus       EmployeeEmploymentStatus   @map("employment_status")
  contractType           EmployeeContractType?      @map("contract_type")
  availabilityStatus     EmployeeAvailabilityStatus @map("availability_status")
  supervisorMembershipId String?                    @map("supervisor_membership_id")
  supervisorUserId       String?                    @map("supervisor_user_id")
  hiredAt                DateTime?                  @map("hired_at") @db.Date
  contractStartsAt       DateTime?                  @map("contract_starts_at") @db.Date
  contractEndsAt         DateTime?                  @map("contract_ends_at") @db.Date
  terminatedAt           DateTime?                  @map("terminated_at") @db.Date
  provisioningSource     TenantProvisioningSource   @default(MANUAL) @map("provisioning_source")
  provisioningBatchId    String?                    @map("provisioning_batch_id") @db.VarChar(128)
  createdAt              DateTime                   @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt              DateTime                   @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant                 Tenant                     @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade)
  membership             TenantMembership           @relation("EmployeeProfileMembership", fields: [tenantId, membershipId, userId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade)
  supervisorMembership   TenantMembership?          @relation("EmployeeProfileSupervisor", fields: [tenantId, supervisorMembershipId, supervisorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade)

  @@unique([tenantId, id])
  @@unique([tenantId, membershipId])
  @@unique([tenantId, membershipId, userId])
  @@unique([tenantId, normalizedEmployeeCode])
  @@index([tenantId, employmentStatus, departmentCode])
  @@index([tenantId, availabilityStatus])
  @@index([tenantId, supervisorMembershipId])
  @@index([provisioningBatchId])
  @@map("employee_profiles")
}

model EmployeeProvisioningRequest {
  id                      String                               @id @default(uuid())
  tenant_id               String
  approval_request_id     String
  identityMode            EmployeeProvisioningIdentityMode     @map("identity_mode")
  normalizedEmail         String                               @map("normalized_email") @db.VarChar(320)
  normalizedEmployeeCode  String                               @map("normalized_employee_code") @db.VarChar(64)
  jobTitle                String?                              @map("job_title") @db.VarChar(120)
  departmentCode          String?                              @map("department_code") @db.VarChar(64)
  employmentStatus        EmployeeEmploymentStatus             @map("employment_status")
  contractType            EmployeeContractType?                @map("contract_type")
  availabilityStatus      EmployeeAvailabilityStatus           @map("availability_status")
  supervisorMembershipId  String?                              @map("supervisor_membership_id")
  supervisorUserId        String?                              @map("supervisor_user_id")
  hiredAt                 DateTime?                            @map("hired_at") @db.Date
  contractStartsAt        DateTime?                            @map("contract_starts_at") @db.Date
  contractEndsAt          DateTime?                            @map("contract_ends_at") @db.Date
  terminatedAt            DateTime?                            @map("terminated_at") @db.Date
  requestedRole           TenantMembershipRole                 @map("requested_role")
  grantedPermissions      String[]                             @default([]) @map("granted_permissions")
  deniedPermissions       String[]                             @default([]) @map("denied_permissions")
  lifecycleStatus         EmployeeProvisioningLifecycleStatus? @map("lifecycle_status")
  lifecycleVersion        Int                                  @default(0) @map("lifecycle_version")
  provisionedUserId       String?                              @map("provisioned_user_id")
  provisionedMembershipId String?                              @map("provisioned_membership_id")
  provisionedAt           DateTime?                            @map("provisioned_at") @db.Timestamptz(6)
  activatedAt             DateTime?                            @map("activated_at") @db.Timestamptz(6)
  revokedAt               DateTime?                            @map("revoked_at") @db.Timestamptz(6)
  createdAt               DateTime                             @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt               DateTime                             @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant                  Tenant                               @relation(fields: [tenant_id], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "employee_provisioning_requests_tenant_fkey")
  approvalRequest         ApprovalRequest                      @relation(fields: [tenant_id, approval_request_id], references: [tenant_id, id], onDelete: Restrict, onUpdate: Cascade, map: "employee_provisioning_requests_approval_fkey")
  supervisorMembership    TenantMembership?                    @relation("EmployeeProvisioningRequestSupervisor", fields: [tenant_id, supervisorMembershipId, supervisorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "employee_provisioning_requests_supervisor_fkey")
  provisionedMembership   TenantMembership?                    @relation("EmployeeProvisioningRequestProvisionedMembership", fields: [tenant_id, provisionedMembershipId, provisionedUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "employee_provisioning_requests_provisioned_membership_fkey")
  invitations             EmployeeProvisioningInvitation[]
  adminRoleProposals      EmployeeAdminRoleProposal[]

  @@unique([tenant_id, id])
  @@unique([tenant_id, approval_request_id], map: "employee_provisioning_requests_tenant_approval_key")
  @@index([tenant_id, lifecycleStatus, createdAt(sort: Desc)], map: "employee_provisioning_requests_tenant_lifecycle_created_idx")
  @@index([tenant_id, normalizedEmail], map: "employee_provisioning_requests_tenant_email_idx")
  @@index([tenant_id, normalizedEmployeeCode], map: "employee_provisioning_requests_tenant_employee_code_idx")
  @@index([tenant_id, provisionedMembershipId], map: "employee_provisioning_requests_tenant_provisioned_membership_id")
  @@map("employee_provisioning_requests")
}

model EmployeeProvisioningInvitation {
  id                    String                               @id @default(uuid())
  tenant_id             String
  provisioningRequestId String                               @map("provisioning_request_id")
  tokenHmac             String                               @unique @map("token_hmac") @db.Char(64)
  status                EmployeeProvisioningInvitationStatus
  expiresAt             DateTime                             @map("expires_at") @db.Timestamptz(6)
  attemptCount          Int                                  @default(0) @map("attempt_count")
  maxAttempts           Int                                  @map("max_attempts")
  issuedByMembershipId  String                               @map("issued_by_membership_id")
  issuedByUserId        String                               @map("issued_by_user_id")
  issueRequestId        String                               @map("issue_request_id") @db.VarChar(191)
  issuePayloadHash      String                               @map("issue_payload_hash") @db.Char(64)
  acceptanceRequestId   String?                              @map("acceptance_request_id") @db.VarChar(191)
  acceptancePayloadHash String?                              @map("acceptance_payload_hash") @db.Char(64)
  acceptedUserId        String?                              @map("accepted_user_id")
  acceptedAt            DateTime?                            @map("accepted_at") @db.Timestamptz(6)
  revokedAt             DateTime?                            @map("revoked_at") @db.Timestamptz(6)
  expiredAt             DateTime?                            @map("expired_at") @db.Timestamptz(6)
  createdAt             DateTime                             @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                Tenant                               @relation(fields: [tenant_id], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "employee_provisioning_invitations_tenant_fkey")
  provisioningRequest   EmployeeProvisioningRequest          @relation(fields: [tenant_id, provisioningRequestId], references: [tenant_id, id], onDelete: Restrict, onUpdate: Cascade, map: "employee_provisioning_invitations_request_fkey")
  issuerMembership      TenantMembership                     @relation("EmployeeProvisioningInvitationIssuer", fields: [tenant_id, issuedByMembershipId, issuedByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "employee_provisioning_invitations_issuer_fkey")
  acceptedUser          User?                                @relation("EmployeeProvisioningInvitationAcceptedUser", fields: [acceptedUserId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "employee_provisioning_invitations_accepted_user_fkey")

  @@unique([tenant_id, id])
  @@unique([tenant_id, issueRequestId], map: "employee_provisioning_invitations_tenant_issue_request_key")
  @@unique([tenant_id, acceptanceRequestId], map: "employee_provisioning_invitations_tenant_acceptance_request_key")
  @@index([tenant_id, provisioningRequestId, createdAt(sort: Desc)], map: "employee_provisioning_invitations_tenant_request_created_idx")
  @@index([tenant_id, status, expiresAt], map: "employee_provisioning_invitations_tenant_status_expires_idx")
  @@map("employee_provisioning_invitations")
}

model AdminIdentityInvitation {
  id                    String                        @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  publicRef             String                        @default(dbgenerated("gen_random_uuid()")) @map("public_ref") @db.Uuid
  tenantId              String                        @map("tenant_id")
  normalizedEmail       String                        @map("normalized_email") @db.VarChar(320)
  intendedRole          TenantMembershipRole          @default(A) @map("intended_role")
  grantedPermissions    String[]                      @default([]) @map("granted_permissions")
  tokenHash             String                        @unique @map("token_hash") @db.Char(64)
  status                AdminIdentityInvitationStatus @default(PENDING)
  expiresAt             DateTime                      @map("expires_at") @db.Timestamptz(6)
  issuedByMembershipId  String                        @map("issued_by_membership_id")
  issuedByUserId        String                        @map("issued_by_user_id")
  activatedUserId       String?                       @map("activated_user_id")
  activatedMembershipId String?                       @map("activated_membership_id")
  requestId             String                        @map("request_id") @db.VarChar(191)
  payloadHash           String                        @map("payload_hash") @db.Char(64)
  consumedAt            DateTime?                     @map("consumed_at") @db.Timestamptz(6)
  revokedAt             DateTime?                     @map("revoked_at") @db.Timestamptz(6)
  createdAt             DateTime                      @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt             DateTime                      @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant                Tenant                        @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "admin_identity_invitations_tenant_fkey")
  issuerMembership      TenantMembership              @relation("AdminIdentityInvitationIssuer", fields: [tenantId, issuedByMembershipId, issuedByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "admin_identity_invitations_issuer_fkey")
  activatedUser         User?                         @relation("AdminIdentityInvitationActivatedUser", fields: [activatedUserId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "admin_identity_invitations_activated_user_fkey")
  activatedMembership   TenantMembership?             @relation("AdminIdentityInvitationActivatedMembership", fields: [tenantId, activatedMembershipId, activatedUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "admin_identity_invitations_activated_membership_fkey")

  @@unique([tenantId, publicRef], map: "admin_identity_invitations_tenant_public_ref_key")
  @@unique([tenantId, requestId], map: "admin_identity_invitations_tenant_request_key")
  @@index([tenantId, normalizedEmail, status], map: "admin_identity_invitations_tenant_email_status_idx")
  @@index([tenantId, status, expiresAt], map: "admin_identity_invitations_tenant_status_expires_idx")
  @@map("admin_identity_invitations")
}

model EmployeeAdminRoleProposal {
  id                    String                      @id @default(uuid())
  tenant_id             String
  provisioningRequestId String                      @map("provisioning_request_id")
  proposedRole          TenantMembershipRole        @map("proposed_role")
  proposerMembershipId  String                      @map("proposer_membership_id")
  proposerUserId        String                      @map("proposer_user_id")
  requestId             String                      @map("request_id") @db.VarChar(191)
  payloadHash           String                      @map("payload_hash") @db.Char(64)
  grantedPermissions    String[]                    @default([]) @map("granted_permissions")
  deniedPermissions     String[]                    @default([]) @map("denied_permissions")
  createdAt             DateTime                    @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                Tenant                      @relation(fields: [tenant_id], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "employee_admin_role_proposals_tenant_fkey")
  provisioningRequest   EmployeeProvisioningRequest @relation(fields: [tenant_id, provisioningRequestId], references: [tenant_id, id], onDelete: Restrict, onUpdate: Cascade, map: "employee_admin_role_proposals_request_fkey")
  proposerMembership    TenantMembership            @relation("EmployeeAdminRoleProposalProposer", fields: [tenant_id, proposerMembershipId, proposerUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "employee_admin_role_proposals_proposer_fkey")

  @@unique([tenant_id, id])
  @@unique([tenant_id, requestId], map: "employee_admin_role_proposals_tenant_request_key")
  @@index([tenant_id, provisioningRequestId, createdAt(sort: Desc)], map: "employee_admin_role_proposals_tenant_provisioning_created_idx")
  @@index([tenant_id, proposerMembershipId, createdAt(sort: Desc)], map: "employee_admin_role_proposals_tenant_proposer_created_idx")
  @@map("employee_admin_role_proposals")
}

model AuthSession {
  id                           String             @id
  tenantId                     String             @map("tenant_id")
  membershipId                 String             @map("membership_id")
  userId                       String             @map("user_id")
  status                       AuthSessionStatus  @default(ACTIVE)
  authorizationVersionSnapshot Int                @map("authorization_version_snapshot")
  currentRefreshVersion        Int                @default(0) @map("current_refresh_version")
  fingerprintHash              String             @map("fingerprint_hash") @db.Char(64)
  createdAt                    DateTime           @default(now()) @map("created_at")
  updatedAt                    DateTime           @updatedAt @map("updated_at")
  lastRefreshedAt              DateTime?          @map("last_refreshed_at")
  expiresAt                    DateTime           @map("expires_at")
  revokedAt                    DateTime?          @map("revoked_at")
  compromisedAt                DateTime?          @map("compromised_at")
  revocationReason             String?            @map("revocation_reason") @db.VarChar(160)
  membership                   TenantMembership   @relation("AuthSessionMembership", fields: [tenantId, membershipId, userId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade)
  refreshTokens                AuthRefreshToken[]

  @@unique([tenantId, id])
  @@index([tenantId, membershipId, status])
  @@index([userId, status])
  @@index([expiresAt])
  @@map("auth_sessions")
}

model AuthRefreshToken {
  id                String                 @id
  tenantId          String                 @map("tenant_id")
  sessionId         String                 @map("session_id")
  version           Int
  tokenHash         String                 @unique @map("token_hash") @db.Char(64)
  fingerprintHash   String                 @map("fingerprint_hash") @db.Char(64)
  status            AuthRefreshTokenStatus @default(ACTIVE)
  createdAt         DateTime               @default(now()) @map("created_at")
  expiresAt         DateTime               @map("expires_at")
  rotatedAt         DateTime?              @map("rotated_at")
  revokedAt         DateTime?              @map("revoked_at")
  reuseDetectedAt   DateTime?              @map("reuse_detected_at")
  replacedByTokenId String?                @unique @map("replaced_by_token_id")
  session           AuthSession            @relation(fields: [tenantId, sessionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade)
  replacedByToken   AuthRefreshToken?      @relation("AuthRefreshReplacement", fields: [replacedByTokenId], references: [id], onDelete: Restrict, onUpdate: Cascade)
  replacesToken     AuthRefreshToken?      @relation("AuthRefreshReplacement")

  @@unique([tenantId, sessionId, version])
  @@index([tenantId, sessionId, status])
  @@index([expiresAt])
  @@map("auth_refresh_tokens")
}

model Client {
  id                        String          @id @default(cuid())
  publicRef                 String          @default(dbgenerated("gen_random_uuid()")) @map("public_ref") @db.Uuid
  tenantId                  String?         @map("tenant_id")
  code                      String          @unique
  name                      String
  email                     String
  phone                     String
  address                   String
  type                      String
  status                    String
  totalServices             Int             @default(0)
  lastService               String?
  createdAt                 String
  fiscalName                String?
  taxId                     String?
  billingAddress            String?
  paymentMethod             String?
  currency                  String?
  paymentTerms              String?
  accountsPayableName       String?
  accountsPayableEmail      String?
  accountsPayablePhone      String?
  complianceNotes           String?
  kycCompleted              Boolean         @default(false)
  normalizedPhone           String?
  normalizedEmail           String?         @map("normalized_email") @db.VarChar(320)
  taxIdNormalized           String?         @map("tax_id_normalized") @db.VarChar(32)
  billingTaxId              String?
  serviceOriginAddress      String?
  serviceDestinationAddress String?
  updatedAt                 DateTime        @default(now()) @updatedAt
  kycDocuments              KycDocument[]
  leads                     Lead[]
  projects                  Project[]
  tenant                    Tenant?         @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "osi_clients_tenant_id_fkey")
  tenantLeads               Lead[]          @relation("LeadTenantCustomer")
  tenantProjects            Project[]       @relation("ProjectTenantClient")
  pipelineCases             PipelineCase[]  @relation("PipelineCaseServiceClient")
  addresses                 ClientAddress[]
  surveys                   Survey[]

  @@unique([tenantId, id], map: "osi_clients_tenant_id_id_key")
  @@unique([tenantId, publicRef], map: "osi_clients_tenant_id_public_ref_key")
  @@index([tenantId, status], map: "osi_clients_tenant_id_status_idx")
  @@map("osi_clients")
}

model ClientAddress {
  id                   String                      @id @default(cuid())
  addressRef           String                      @default(dbgenerated("gen_random_uuid()")) @map("address_ref") @db.Uuid
  tenantId             String                      @map("tenant_id")
  clientId             String                      @map("client_id")
  countryCode          String                      @map("country_code") @db.Char(2)
  provinceState        String?                     @map("province_state") @db.VarChar(160)
  cityMunicipality     String                      @map("city_municipality") @db.VarChar(160)
  sector               String?                     @db.VarChar(160)
  streetAndNumber      String?                     @map("street_and_number") @db.VarChar(240)
  buildingResidential  String?                     @map("building_residential") @db.VarChar(160)
  floorUnit            String?                     @map("floor_unit") @db.VarChar(80)
  arrivalReference     String?                     @map("arrival_reference") @db.VarChar(320)
  locationContactName  String?                     @map("location_contact_name") @db.VarChar(160)
  locationContactPhone String?                     @map("location_contact_phone") @db.VarChar(40)
  label                String?                     @db.VarChar(80)
  status               ClientAddressStatus         @default(ACTIVE)
  createdAt            DateTime                    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt            DateTime                    @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant               Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "client_addresses_tenant_fkey")
  client               Client                      @relation(fields: [tenantId, clientId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "client_addresses_tenant_client_fkey")
  routeSnapshots       PipelineCaseRouteSnapshot[] @relation("RouteSnapshotSourceAddress")

  @@unique([tenantId, id], map: "client_addresses_tenant_id_key")
  @@unique([tenantId, addressRef], map: "client_addresses_tenant_address_ref_key")
  @@index([tenantId, clientId, status, label], map: "client_addresses_tenant_client_status_label_idx")
  @@index([tenantId, countryCode, provinceState, cityMunicipality], map: "client_addresses_tenant_geography_idx")
  @@map("client_addresses")
}

model Project {
  id                String               @id @default(cuid())
  tenantId          String?              @map("tenant_id")
  pipelineCaseId    String?              @map("pipeline_case_id")
  code              String               @unique
  name              String
  clientId          String
  clientName        String
  status            String
  startDate         String
  endDate           String?
  osiCount          Int                  @default(0)
  totalValue        Float                @default(0)
  assignedTo        String?
  notes             String?
  kReleasedAt       DateTime?
  kState            ProjectKState        @default(PENDING_VALIDATION)
  kValidatedAt      DateTime?
  leadId            String?
  pstCode           String?
  pstServiceName    String?
  quoteId           String?
  fileNumber        String?              @unique
  leadRecords       Lead[]               @relation("LeadProject")
  tenantLeadRecords Lead[]               @relation("LeadTenantProject")
  osis              Osi[]
  coordination      ProjectCoordination?
  pgd               ProjectPgd?
  signals           ProjectSignal[]
  client            Client               @relation(fields: [clientId], references: [id], onDelete: Cascade)
  tenant            Tenant?              @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "osi_projects_tenant_id_fkey")
  tenantClient      Client?              @relation("ProjectTenantClient", fields: [tenantId, clientId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "osi_projects_tenant_id_client_id_fkey")
  pipelineCase      PipelineCase?        @relation("PipelineCaseProjects", fields: [tenantId, pipelineCaseId, clientId], references: [tenantId, id, clientId], onDelete: Restrict, onUpdate: Restrict, map: "osi_projects_tenant_id_pipeline_case_id_client_id_fkey")
  surveys           Survey[]

  @@unique([tenantId, id], map: "osi_projects_tenant_id_id_key")
  @@index([tenantId, status], map: "osi_projects_tenant_id_status_idx")
  @@index([tenantId, clientId], map: "osi_projects_tenant_id_client_id_idx")
  @@index([tenantId, pipelineCaseId, clientId], map: "osi_projects_tenant_id_pipeline_case_id_client_id_idx")
  @@map("osi_projects")
}

model ProjectCoordination {
  id               String                             @id @default(cuid())
  projectId        String                             @unique @map("project_id")
  status           String
  requirementsJson Json                               @default("{}") @map("requirements_json")
  milestonesJson   Json                               @default("[]") @map("milestones_json")
  documentsJson    Json                               @default("[]") @map("documents_json")
  createdAt        DateTime                           @default(now()) @map("created_at")
  updatedAt        DateTime                           @updatedAt @map("updated_at")
  project          Project                            @relation(fields: [projectId], references: [id], onDelete: Cascade)
  communications   ProjectCoordinationCommunication[]

  @@map("osi_project_coordination")
}

model ProjectCoordinationCommunication {
  id             String              @id @default(cuid())
  coordinationId String              @map("coordination_id")
  templateKey    String              @map("template_key")
  channel        String
  subject        String?
  content        String
  recipientName  String?             @map("recipient_name")
  recipientEmail String?             @map("recipient_email")
  sentById       String?             @map("sent_by_id")
  sentByRole     String?             @map("sent_by_role")
  sentAt         DateTime            @map("sent_at")
  createdAt      DateTime            @default(now()) @map("created_at")
  coordination   ProjectCoordination @relation(fields: [coordinationId], references: [id], onDelete: Cascade)

  @@index([coordinationId, sentAt], map: "osi_project_coordination_communications_coordination_id_sent_at")
  @@map("osi_project_coordination_communications")
}

model Lead {
  id                       String                 @id @default(cuid())
  tenantId                 String?                @map("tenant_id")
  code                     String                 @unique
  status                   String
  leadType                 LeadType?
  channel                  String?
  clientName               String
  phone                    String?
  email                    String?
  preliminaryOriginAddress String?
  estimatedServiceType     String?
  estimatedMoveDate        String?
  pstCode                  String?
  originAddress            String?
  destinationAddress       String?
  originFloor              String?
  destinationFloor         String?
  accessType               String?
  allowedTimeWindow        String?
  serviceResponsibleName   String?
  paymentResponsibleName   String?
  billingCompanyName       String?
  surveyMethod             String?
  geoDistanceKm            Float?
  geoValidated             Boolean                @default(false)
  viaticInformed           Boolean                @default(false)
  acceptanceStatus         AcceptanceStatus       @default(PENDING)
  acceptanceEvidence       String?
  acceptanceNote           String?
  conversionStatus         LeadConversionStatus   @default(PENDING)
  confirmedServiceDate     String?
  lostReason               String?
  lostReasonNote           String?
  fiscalData               Json?
  kycRequired              Boolean                @default(false)
  kycCompleted             Boolean                @default(false)
  nestingRequired          Boolean                @default(false)
  nestingCompleted         Boolean                @default(false)
  fileNumber               String?
  convertedAt              DateTime?
  sentToKAt                DateTime?
  gatewayCompletedAt       DateTime?
  createdByRole            String?
  updatedByRole            String?
  customerId               String?
  projectId                String?
  createdAt                DateTime               @default(now())
  updatedAt                DateTime               @default(now()) @updatedAt
  surveyRequired           Boolean                @default(false)
  surveyStatus             SurveyStatus           @default(NOT_STARTED)
  surveyCompletedAt        DateTime?
  surveyFlagsJson          Json?
  surveySummaryJson        Json?
  originCountryCode        String?
  destinationCountryCode   String?
  derivedMode              LeadDerivedMode        @default(PENDING)
  modeUpdatedAt            DateTime?
  visitSkipped             Boolean                @default(false)
  visitSkipReason          String?
  visitSkipAt              DateTime?
  title                    String?
  sourceChannelV2          LeadSourceChannel?     @map("source_channel_v2")
  stageV2                  LeadStage?             @map("stage_v2")
  serviceScopeV2           CommercialServiceType? @map("service_scope_v2")
  leadAddresses            LeadAddress[]
  leadCommissions          LeadCommission[]
  leadParties              LeadParty[]
  leadServices             LeadService[]
  leadStageHistory         LeadStageHistory[]
  kycDocuments             KycDocument[]
  audits                   LeadAuditLog[]
  volumeEstimate           LeadVolumeEstimate?
  customer                 Client?                @relation(fields: [customerId], references: [id])
  project                  Project?               @relation("LeadProject", fields: [projectId], references: [id])
  tenant                   Tenant?                @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "osi_leads_tenant_id_fkey")
  tenantCustomer           Client?                @relation("LeadTenantCustomer", fields: [tenantId, customerId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "osi_leads_tenant_id_customer_id_fkey")
  tenantProject            Project?               @relation("LeadTenantProject", fields: [tenantId, projectId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "osi_leads_tenant_id_project_id_fkey")
  surveys                  Survey[]

  @@unique([tenantId, id], map: "osi_leads_tenant_id_id_key")
  @@index([tenantId, status, updatedAt], map: "osi_leads_tenant_id_status_updated_at_idx")
  @@index([tenantId, customerId], map: "osi_leads_tenant_id_customer_id_idx")
  @@index([tenantId, projectId], map: "osi_leads_tenant_id_project_id_idx")
  @@index([status, updatedAt])
  @@index([stageV2, updatedAt])
  @@index([sourceChannelV2, updatedAt])
  @@index([leadType, conversionStatus])
  @@index([derivedMode, modeUpdatedAt])
  @@index([customerId])
  @@index([projectId])
  @@map("osi_leads")
}

model BusinessEntity {
  id                        String                @id @default(uuid())
  code                      String                @unique
  legalName                 String                @map("legal_name")
  tradeName                 String?               @map("trade_name")
  entityKind                EntityKind            @map("entity_kind")
  taxId                     String?               @map("tax_id")
  countryCode               String?               @map("country_code")
  phone                     String?
  email                     String?
  website                   String?
  notes                     String?
  isActive                  Boolean               @default(true) @map("is_active")
  createdAt                 DateTime              @default(now()) @map("created_at")
  updatedAt                 DateTime              @updatedAt @map("updated_at")
  entityTypes               BusinessEntityType[]
  commissionRules           CommissionAgreement[] @relation("BusinessEntityCommissionRules")
  contacts                  EntityContact[]
  leadCommissionsAsReferral LeadCommission[]      @relation("ReferralLeadCommissions")
  leadParties               LeadParty[]

  @@index([legalName])
  @@index([entityKind, isActive])
  @@map("business_entities")
}

model EntityType {
  id          String               @id @default(uuid())
  code        EntityTypeCode       @unique
  name        String
  description String?
  entities    BusinessEntityType[]

  @@map("entity_types")
}

model BusinessEntityType {
  id               String         @id @default(uuid())
  businessEntityId String         @map("business_entity_id")
  entityTypeId     String         @map("entity_type_id")
  createdAt        DateTime       @default(now()) @map("created_at")
  businessEntity   BusinessEntity @relation(fields: [businessEntityId], references: [id], onDelete: Cascade)
  entityType       EntityType     @relation(fields: [entityTypeId], references: [id], onDelete: Cascade)

  @@unique([businessEntityId, entityTypeId])
  @@index([entityTypeId])
  @@map("business_entity_types")
}

model EntityContact {
  id               String         @id @default(uuid())
  businessEntityId String         @map("business_entity_id")
  fullName         String         @map("full_name")
  position         String?
  email            String?
  phone            String?
  mobile           String?
  isPrimary        Boolean        @default(false) @map("is_primary")
  notes            String?
  createdAt        DateTime       @default(now()) @map("created_at")
  updatedAt        DateTime       @updatedAt @map("updated_at")
  businessEntity   BusinessEntity @relation(fields: [businessEntityId], references: [id], onDelete: Cascade)
  leadParties      LeadParty[]

  @@index([businessEntityId, isPrimary])
  @@map("entity_contacts")
}

model LeadParty {
  id               String         @id @default(uuid())
  leadId           String         @map("lead_id")
  businessEntityId String         @map("business_entity_id")
  contactId        String?        @map("contact_id")
  role             LeadPartyRole
  isPrimary        Boolean        @default(false) @map("is_primary")
  notes            String?
  createdAt        DateTime       @default(now()) @map("created_at")
  businessEntity   BusinessEntity @relation(fields: [businessEntityId], references: [id])
  contact          EntityContact? @relation(fields: [contactId], references: [id])
  lead             Lead           @relation(fields: [leadId], references: [id], onDelete: Cascade)

  @@index([leadId, role])
  @@index([businessEntityId])
  @@index([contactId])
  @@map("lead_parties")
}

model LeadAddress {
  id           String      @id @default(uuid())
  leadId       String      @map("lead_id")
  addressRole  AddressRole @map("address_role")
  country      String?
  city         String?
  state        String?
  postalCode   String?     @map("postal_code")
  addressLine1 String?     @map("address_line1")
  addressLine2 String?     @map("address_line2")
  notes        String?
  createdAt    DateTime    @default(now()) @map("created_at")
  lead         Lead        @relation(fields: [leadId], references: [id], onDelete: Cascade)

  @@index([leadId, addressRole])
  @@map("lead_addresses")
}

model LeadService {
  id           String                   @id @default(uuid())
  leadId       String                   @map("lead_id")
  serviceType  CommercialServiceType    @map("service_type")
  isPrimary    Boolean                  @default(false) @map("is_primary")
  sequence     Int?
  notes        String?
  createdAt    DateTime                 @default(now()) @map("created_at")
  requirements LeadServiceRequirement[]
  lead         Lead                     @relation(fields: [leadId], references: [id], onDelete: Cascade)

  @@index([leadId, serviceType])
  @@map("lead_services")
}

model LeadServiceRequirement {
  id             String            @id @default(uuid())
  leadServiceId  String            @map("lead_service_id")
  requirementKey String            @map("requirement_key")
  status         RequirementStatus @default(PENDING)
  notes          String?
  createdAt      DateTime          @default(now()) @map("created_at")
  updatedAt      DateTime          @updatedAt @map("updated_at")
  leadService    LeadService       @relation(fields: [leadServiceId], references: [id], onDelete: Cascade)

  @@index([leadServiceId, status])
  @@map("lead_service_requirements")
}

model LeadStageHistory {
  id              String     @id @default(uuid())
  leadId          String     @map("lead_id")
  fromStage       LeadStage? @map("from_stage")
  toStage         LeadStage  @map("to_stage")
  changedByUserId String?    @map("changed_by_user_id")
  comment         String?
  createdAt       DateTime   @default(now()) @map("created_at")
  lead            Lead       @relation(fields: [leadId], references: [id], onDelete: Cascade)

  @@index([leadId, createdAt])
  @@index([toStage, createdAt])
  @@map("lead_stage_history")
}

model CommissionAgreement {
  id               String                 @id @default(uuid())
  businessEntityId String                 @map("business_entity_id")
  appliesTo        CommissionAppliesTo    @map("applies_to")
  commissionType   CommissionType         @map("commission_type")
  commissionValue  Decimal                @map("commission_value") @db.Decimal(12, 2)
  currency         String
  serviceType      CommercialServiceType? @map("service_type")
  validFrom        DateTime?              @map("valid_from")
  validTo          DateTime?              @map("valid_to")
  notes            String?
  isActive         Boolean                @default(true) @map("is_active")
  createdAt        DateTime               @default(now()) @map("created_at")
  updatedAt        DateTime               @updatedAt @map("updated_at")
  businessEntity   BusinessEntity         @relation("BusinessEntityCommissionRules", fields: [businessEntityId], references: [id], onDelete: Cascade)
  leadCommissions  LeadCommission[]

  @@index([businessEntityId, isActive])
  @@map("commission_agreements")
}

model LeadCommission {
  id                    String               @id @default(uuid())
  leadId                String               @map("lead_id")
  referralEntityId      String               @map("referral_entity_id")
  commissionAgreementId String?              @map("commission_agreement_id")
  baseAmount            Decimal              @map("base_amount") @db.Decimal(14, 2)
  commissionType        CommissionType       @map("commission_type")
  commissionValue       Decimal              @map("commission_value") @db.Decimal(12, 2)
  commissionAmount      Decimal              @map("commission_amount") @db.Decimal(14, 2)
  currency              String
  status                CommissionStatus     @default(PENDING)
  notes                 String?
  createdAt             DateTime             @default(now()) @map("created_at")
  updatedAt             DateTime             @updatedAt @map("updated_at")
  commissionAgreement   CommissionAgreement? @relation(fields: [commissionAgreementId], references: [id])
  lead                  Lead                 @relation(fields: [leadId], references: [id], onDelete: Cascade)
  referralEntity        BusinessEntity       @relation("ReferralLeadCommissions", fields: [referralEntityId], references: [id])

  @@index([leadId, status])
  @@index([referralEntityId, status])
  @@map("lead_commissions")
}

model Survey {
  id                          String            @id @default(cuid())
  leadId                      String
  clientId                    String?
  projectId                   String?
  type                        SurveyType        @default(PRESENCIAL)
  status                      SurveyStatus      @default(IN_PROGRESS)
  inspectorUserId             String?
  originAddressId             String?
  checkInGps                  Json?
  checkInAt                   DateTime?
  submittedAt                 DateTime?
  estimatedTotalVolume        Float?
  volumeUnit                  String?
  prohibitedItemsAcknowledged Boolean           @default(false)
  riskNotes                   String?
  longCarry                   Boolean           @default(false)
  stairCarryRisk              Boolean           @default(false)
  needsPermits                Boolean           @default(false)
  needsCratingCount           Int               @default(0)
  packP1Count                 Int               @default(0)
  packP2Count                 Int               @default(0)
  packP3Count                 Int               @default(0)
  flagsJson                   Json?
  summaryJson                 Json?
  createdAt                   DateTime          @default(now())
  updatedAt                   DateTime          @default(now()) @updatedAt
  items                       SurveyItem[]
  media                       SurveyMedia[]
  rooms                       SurveyRoom[]
  signature                   SurveySignature?
  siteAccess                  SurveySiteAccess?
  client                      Client?           @relation(fields: [clientId], references: [id])
  lead                        Lead              @relation(fields: [leadId], references: [id], onDelete: Cascade)
  project                     Project?          @relation(fields: [projectId], references: [id])

  @@index([leadId, createdAt])
  @@index([status, updatedAt])
  @@map("osi_surveys")
}

model SurveySiteAccess {
  id                        String              @id @default(cuid())
  surveyId                  String              @unique
  propertyType              SurveyPropertyType?
  floorLevel                Int?
  elevatorAvailableForMove  Boolean?
  elevatorDims              Json?
  stairsType                SurveyStairsType?
  stairsWidthOk             Boolean?
  truckParkingDistanceM     Float?
  timeRestrictionsForTrucks Boolean?
  timeRestrictionsNote      String?
  permitsRequired           String[]
  permitsEvidenceProvided   Boolean             @default(false)
  permitsTaskCreated        Boolean             @default(false)
  longCarry                 Boolean             @default(false)
  stairCarryRisk            Boolean             @default(false)
  needsPermits              Boolean             @default(false)
  createdAt                 DateTime            @default(now())
  updatedAt                 DateTime            @default(now()) @updatedAt
  survey                    Survey              @relation(fields: [surveyId], references: [id], onDelete: Cascade)

  @@index([surveyId, longCarry, stairCarryRisk])
  @@map("osi_survey_site_access")
}

model SurveyRoom {
  id        String       @id @default(cuid())
  surveyId  String
  name      String
  sortOrder Int          @default(0)
  note      String?
  createdAt DateTime     @default(now())
  updatedAt DateTime     @default(now()) @updatedAt
  items     SurveyItem[]
  survey    Survey       @relation(fields: [surveyId], references: [id], onDelete: Cascade)

  @@index([surveyId, sortOrder])
  @@map("osi_survey_rooms")
}

model SurveyItem {
  id               String             @id @default(cuid())
  surveyId         String
  roomId           String?
  itemRef          String?
  itemName         String?
  quantity         Int                @default(1)
  packLevel        SurveyPackLevel    @default(P1)
  needsDisassembly Boolean            @default(false)
  notes            String?
  unitVolume       Float?
  createdAt        DateTime           @default(now())
  updatedAt        DateTime           @default(now()) @updatedAt
  nesting          SurveyItemNesting?
  room             SurveyRoom?        @relation(fields: [roomId], references: [id])
  survey           Survey             @relation(fields: [surveyId], references: [id], onDelete: Cascade)
  media            SurveyMedia[]

  @@index([surveyId, packLevel])
  @@index([roomId])
  @@map("osi_survey_items")
}

model SurveyItemNesting {
  id             String              @id @default(cuid())
  surveyItemId   String              @unique
  realLength     Float?
  realWidth      Float?
  realHeight     Float?
  unit           SurveyDimensionUnit @default(CM)
  technicalNote  String?
  ispm15Required Boolean             @default(false)
  fragileTier    String?
  createdAt      DateTime            @default(now())
  updatedAt      DateTime            @default(now()) @updatedAt
  surveyItem     SurveyItem          @relation(fields: [surveyItemId], references: [id], onDelete: Cascade)

  @@map("osi_survey_item_nesting")
}

model SurveyMedia {
  id           String          @id @default(cuid())
  surveyId     String
  surveyItemId String?
  type         SurveyMediaType @default(OTHER)
  url          String
  description  String?
  createdAt    DateTime        @default(now())
  survey       Survey          @relation(fields: [surveyId], references: [id], onDelete: Cascade)
  surveyItem   SurveyItem?     @relation(fields: [surveyItemId], references: [id])

  @@index([surveyId, type])
  @@index([surveyItemId, type])
  @@map("osi_survey_media")
}

model SurveySignature {
  id               String    @id @default(cuid())
  surveyId         String    @unique
  signerName       String?
  acceptedDigital  Boolean   @default(false)
  signatureDataUrl String?
  signatureNote    String?
  signedAt         DateTime?
  createdAt        DateTime  @default(now())
  updatedAt        DateTime  @default(now()) @updatedAt
  survey           Survey    @relation(fields: [surveyId], references: [id], onDelete: Cascade)

  @@map("osi_survey_signatures")
}

model KycDocument {
  id             String            @id @default(cuid())
  leadId         String?
  clientId       String?
  type           KycDocumentType
  url            String
  status         KycDocumentStatus @default(UPLOADED)
  note           String?
  uploadedByRole String?
  uploadedAt     DateTime          @default(now())
  verifiedAt     DateTime?
  client         Client?           @relation(fields: [clientId], references: [id])
  lead           Lead?             @relation(fields: [leadId], references: [id])

  @@index([leadId, type])
  @@index([clientId, type])
  @@index([status, uploadedAt])
  @@map("osi_kyc_documents")
}

model LeadAuditLog {
  id         String   @id @default(cuid())
  leadId     String
  actorId    String?
  actorRole  String
  action     String
  note       String?
  beforeJson Json?
  afterJson  Json?
  createdAt  DateTime @default(now())
  actor      User?    @relation("LeadAuditActor", fields: [actorId], references: [id])
  lead       Lead     @relation(fields: [leadId], references: [id], onDelete: Cascade)

  @@index([leadId, createdAt])
  @@map("osi_lead_audit_logs")
}

model VolumeAreaProfile {
  id            String               @id @default(cuid())
  name          String               @unique
  defaultM3     Float
  minM3         Float
  maxM3         Float
  active        Boolean              @default(true)
  updatedAt     DateTime             @default(now()) @updatedAt
  updatedById   String?
  leadEstimates LeadVolumeEstimate[]
  updatedBy     User?                @relation("VolumeAreaProfileUpdatedBy", fields: [updatedById], references: [id])

  @@index([active, updatedAt])
  @@map("osi_volume_area_profiles")
}

model LeadVolumeEstimate {
  id               String                 @id @default(cuid())
  leadId           String                 @unique
  areaProfileId    String
  estimatedM3Base  Float
  adjustmentPct    Float                  @default(0)
  estimatedM3Final Float
  confidence       LeadEstimateConfidence @default(MED)
  notes            String?
  createdAt        DateTime               @default(now())
  updatedAt        DateTime               @default(now()) @updatedAt
  updatedById      String?
  areaProfile      VolumeAreaProfile      @relation(fields: [areaProfileId], references: [id])
  lead             Lead                   @relation(fields: [leadId], references: [id], onDelete: Cascade)
  updatedBy        User?                  @relation("LeadVolumeEstimateUpdatedBy", fields: [updatedById], references: [id])

  @@index([areaProfileId, updatedAt])
  @@index([confidence, updatedAt])
  @@map("osi_lead_volume_estimates")
}

model TipoServicioConfig {
  id                String         @id @default(cuid())
  serviceKey        String         @unique
  modePolicy        LeadModePolicy @default(FIXED)
  fixedMode         ServiceMode?
  surveyRule        SurveyRule     @default(OPTIONAL)
  surveyThresholdM3 Float?
  enableModules     Json?
  active            Boolean        @default(true)
  updatedAt         DateTime       @default(now()) @updatedAt
  updatedById       String?
  updatedBy         User?          @relation("TipoServicioConfigUpdatedBy", fields: [updatedById], references: [id])

  @@index([active, serviceKey])
  @@map("osi_tipos_servicio_config")
}

model ProjectSignal {
  id        String       @id @default(cuid())
  projectId String
  kind      SignalKind
  policy    SignalPolicy
  warnAt    DateTime?
  dueAt     DateTime?
  doneAt    DateTime?
  ackAt     DateTime?
  ackNote   String?
  ackById   String?
  createdAt DateTime     @default(now())
  updatedAt DateTime     @updatedAt
  ackBy     User?        @relation("ProjectSignalAckBy", fields: [ackById], references: [id])
  project   Project      @relation(fields: [projectId], references: [id], onDelete: Cascade)

  @@unique([projectId, kind])
  @@index([projectId, kind])
  @@map("osi_project_signals")
}

model ProjectPgd {
  id                String           @id @default(cuid())
  projectId         String           @unique
  templateId        String
  templateVersionId String
  appliedAt         DateTime         @default(now())
  appliedById       String?
  appliedBy         User?            @relation("ProjectPgdAppliedBy", fields: [appliedById], references: [id])
  project           Project          @relation(fields: [projectId], references: [id], onDelete: Cascade)
  template          Template         @relation(fields: [templateId], references: [id])
  templateVersion   TemplateVersion  @relation("ProjectPgdTemplateVersion", fields: [templateVersionId], references: [id])
  items             ProjectPgdItem[]

  @@map("osi_project_pgd")
}

model ProjectPgdItem {
  id               String         @id @default(cuid())
  projectPgdId     String
  name             String
  visibility       PgdVisibility
  responsible      PgdResponsible
  isBlocking       Boolean        @default(false)
  expectedFileType PgdFileType    @default(OTHER)
  serviceTags      String[]
  status           PgdItemStatus  @default(MISSING)
  validatedAt      DateTime?
  validatedById    String?
  note             String?
  createdAt        DateTime       @default(now())
  updatedAt        DateTime       @updatedAt
  projectPgd       ProjectPgd     @relation(fields: [projectPgdId], references: [id], onDelete: Cascade)
  validatedBy      User?          @relation("ProjectPgdItemValidatedBy", fields: [validatedById], references: [id])

  @@index([projectPgdId, status])
  @@map("osi_project_pgd_items")
}

model Osi {
  id                    String              @id @default(cuid())
  code                  String              @unique
  projectId             String
  projectCode           String
  clientId              String
  clientName            String
  status                String
  type                  String
  origin                String
  destination           String
  scheduledDate         String
  createdAt             String
  assignedTo            String?
  team                  String[]
  vehicles              String[]
  value                 Float
  notes                 String?
  custodyStatus         OsiCustodyStatus    @default(DRIVER)
  custodyTransferredAt  DateTime?
  driverAvailable       Boolean             @default(false)
  driverId              String?
  ecoPoints             Int?
  endedAt               DateTime?
  kind                  OsiKind             @default(EXTERNAL)
  lastMaterialDeviation Json?
  npsScore              Int?
  petCode               String?
  petEditedManually     Boolean             @default(false)
  petPlan               Json?
  pstCode               String?
  pstTemplateVersionId  String?
  ptfCode               String?
  ptfEditedManually     Boolean             @default(false)
  ptfMaterialPlan       Json?
  scheduledEndAt        String?
  scheduledStartAt      String?
  startedAt             DateTime?
  supervisorId          String?
  supervisorNotes       String?
  vehicleAvailable      Boolean             @default(false)
  changeLogs            OsiChangeLog[]
  handshakes            OsiHandshake[]
  materialReturns       OsiMaterialReturn[]
  project               Project             @relation(fields: [projectId], references: [id], onDelete: Cascade)

  @@map("osi_osis")
}

model OsiChangeLog {
  id          String   @id @default(cuid())
  osiId       String
  actorUserId String?
  actorRole   String
  action      String
  fieldPath   String?
  beforeJson  Json?
  afterJson   Json?
  reason      String?
  createdAt   DateTime @default(now())
  osi         Osi      @relation(fields: [osiId], references: [id], onDelete: Cascade)

  @@index([osiId, createdAt])
  @@map("osi_osi_change_logs")
}

model OsiHandshake {
  id          String             @id @default(cuid())
  osiId       String
  status      OsiHandshakeStatus @default(PENDING)
  fromRole    String
  fromUserId  String?
  toRole      String
  toUserId    String?
  type        String             @default("TACTICAL_TRANSFER")
  timestamp   DateTime           @default(now())
  completedAt DateTime?
  notes       String?
  payloadJson Json?
  createdAt   DateTime           @default(now())
  osi         Osi                @relation(fields: [osiId], references: [id], onDelete: Cascade)

  @@index([osiId, status])
  @@map("osi_osi_handshakes")
}

model OsiMaterialReturn {
  id             String   @id @default(cuid())
  osiId          String
  pstCode        String?
  ptfCode        String?
  dispatchedJson Json
  returnedJson   Json
  deviationJson  Json
  recordedById   String?
  recordedByRole String?
  recordedAt     DateTime @default(now())
  createdAt      DateTime @default(now())
  osi            Osi      @relation(fields: [osiId], references: [id], onDelete: Cascade)

  @@index([pstCode, recordedAt])
  @@index([osiId, recordedAt])
  @@map("osi_osi_material_returns")
}

model PtfAdjustmentSuggestion {
  id                    String              @id @default(cuid())
  pstCode               String
  ptfCode               String?
  status                PtfSuggestionStatus @default(PENDING)
  basedOnOsiIds         String[]
  occurrences           Int                 @default(0)
  recommendedDelta      Json
  reason                String?
  ignoredCount          Int                 @default(0)
  lastIgnoredAt         DateTime?
  lastActionById        String?
  createdAt             DateTime            @default(now())
  updatedAt             DateTime            @updatedAt
  osi_escalation_events EscalationEvent[]

  @@index([pstCode, status, createdAt])
  @@map("osi_ptf_adjustment_suggestions")
}

model EscalationEvent {
  id                             String                   @id @default(cuid())
  type                           String
  message                        String
  targetRoles                    String[]
  suggestionId                   String?
  createdAt                      DateTime                 @default(now())
  resolvedAt                     DateTime?
  resolvedById                   String?
  metadataJson                   Json?
  osi_ptf_adjustment_suggestions PtfAdjustmentSuggestion? @relation(fields: [suggestionId], references: [id])

  @@index([createdAt])
  @@index([suggestionId])
  @@map("osi_escalation_events")
}

model Template {
  id                 String            @id @default(cuid())
  type               TemplateType
  name               String
  scope              TemplateScope     @default(TENANT)
  tenantId           String?
  isActive           Boolean           @default(true)
  publishedVersionId String?
  createdAt          DateTime          @default(now())
  updatedAt          DateTime          @updatedAt
  projectPgds        ProjectPgd[]
  versions           TemplateVersion[]
  publishedVersion   TemplateVersion?  @relation("TemplatePublishedVersion", fields: [publishedVersionId], references: [id])

  @@unique([type, name, tenantId], name: "type_name_tenantId")
  @@index([type, tenantId])
  @@map("osi_templates")
}

model TemplateVersion {
  id                    String                @id @default(cuid())
  templateId            String
  version               Int
  status                TemplateVersionStatus @default(DRAFT)
  contentJson           Json?
  contentHtml           String?
  changeSummary         String?
  requestedAt           DateTime?
  approvedAt            DateTime?
  publishedAt           DateTime?
  createdById           String
  approvedById          String?
  rejectedById          String?
  baseVersionId         String?
  createdAt             DateTime              @default(now())
  updatedAt             DateTime              @updatedAt
  projectPgdsLocked     ProjectPgd[]          @relation("ProjectPgdTemplateVersion")
  approvedBy            User?                 @relation("TemplateApprovedBy", fields: [approvedById], references: [id])
  baseVersion           TemplateVersion?      @relation("TemplateBaseVersion", fields: [baseVersionId], references: [id])
  derivedVersions       TemplateVersion[]     @relation("TemplateBaseVersion")
  createdBy             User                  @relation("TemplateCreatedBy", fields: [createdById], references: [id])
  rejectedBy            User?                 @relation("TemplateRejectedBy", fields: [rejectedById], references: [id])
  template              Template              @relation(fields: [templateId], references: [id], onDelete: Cascade)
  publishedForTemplates Template[]            @relation("TemplatePublishedVersion")

  @@unique([templateId, version])
  @@index([status, createdAt])
  @@index([templateId, status])
  @@map("osi_template_versions")
}

model PipelineCase {
  id                            String                        @id @default(cuid())
  publicRef                     String                        @default(dbgenerated("gen_random_uuid()")) @map("public_ref") @db.Uuid
  tenantId                      String?                       @map("tenant_id")
  clientId                      String?                       @map("client_id")
  caseCode                      String
  clientName                    String?
  mode                          PipelineMode
  serviceType                   String
  customerType                  PipelineCustomerType
  status                        PipelineCaseStatus            @default(NEW_INBOX)
  version                       Int                           @default(1)
  statusChangedAt               DateTime?                     @map("status_changed_at") @db.Timestamptz(6)
  lossReasonCode                String?                       @map("loss_reason_code") @db.VarChar(64)
  ownerId                       String?
  ownerMembershipId             String?                       @map("owner_membership_id")
  ownerUserId                   String?                       @map("owner_user_id")
  ownerName                     String
  estimatedCbm                  Float                         @default(0)
  requiresSurvey                Boolean                       @default(false)
  surveyMethod                  PipelineSurveyMethod          @default(NO_APLICA)
  flags                         String[]                      @default([])
  milestonesJson                Json?
  originLocation                String
  destinationLocation           String
  destinationContracted         Boolean                       @default(true)
  caseContactName               String?                       @map("case_contact_name") @db.VarChar(160)
  caseContactPhone              String?                       @map("case_contact_phone") @db.VarChar(40)
  caseContactPhoneNormalized    String?                       @map("case_contact_phone_normalized") @db.VarChar(32)
  caseContactEmail              String?                       @map("case_contact_email") @db.VarChar(320)
  caseContactEmailNormalized    String?                       @map("case_contact_email_normalized") @db.VarChar(320)
  intakeChannel                 PipelineIntakeChannel?        @map("intake_channel")
  clientProfileType             PipelineClientProfileType?    @map("client_profile_type")
  routeContractVersion          Int                           @default(1) @map("route_contract_version")
  routeRevision                 Int                           @default(0) @map("route_revision")
  destinationStatus             PipelineDestinationStatus?    @map("destination_status")
  destinationOverrideType       String?
  destinationAcceptanceUploaded Boolean                       @default(false)
  noDestinationCaseType         String?
  assetsCount                   Int                           @default(0)
  createdAt                     DateTime                      @default(now())
  updatedAt                     DateTime                      @default(now()) @updatedAt
  quotes                        PipelineCaseQuote[]
  owner                         User?                         @relation(fields: [ownerId], references: [id])
  tenant                        Tenant?                       @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "osi_pipeline_cases_tenant_id_fkey")
  client                        Client?                       @relation("PipelineCaseServiceClient", fields: [tenantId, clientId], references: [tenantId, id], onDelete: Restrict, onUpdate: Restrict, map: "osi_pipeline_cases_tenant_id_client_id_fkey")
  enterpriseOwner               TenantMembership?             @relation("PipelineCaseEnterpriseOwner", fields: [tenantId, ownerMembershipId, ownerUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "osi_pipeline_cases_enterprise_owner_fkey")
  crating                       PipelineCratingRequest?
  events                        PipelineEvent[]
  quote_change_order_subjects   quote_change_order_subjects[]
  commands                      PipelineCaseCommand[]
  routeSnapshots                PipelineCaseRouteSnapshot[]
  serviceRevisions              PipelineCaseServiceRevision[]
  surveyAssignments             SurveyAssignment[]
  surveyDrafts                  SurveyDraft[]
  surveyPublications            SurveyPublication[]
  materialReservations          MaterialReservation[]
  materialRequirementSnapshots  MaterialRequirementSnapshot[]
  assetReservations             AssetReservation[]
  assetAssignments              AssetAssignment[]
  externalResourceReservations  ExternalResourceReservation[]
  logisticsCalculations         LogisticsCalculation[]
  logisticsPlans                LogisticsPlan[]
  costingCalculations           CostingCalculation[]
  costingRevisions              CostingRevision[]
  quoteProposals                QuoteProposal[]
  projects                      Project[]                     @relation("PipelineCaseProjects")

  @@unique([tenantId, id], map: "osi_pipeline_cases_tenant_id_id_key")
  @@unique([tenantId, id, clientId], map: "osi_pipeline_cases_tenant_id_id_client_id_key")
  @@unique([tenantId, publicRef], map: "osi_pipeline_cases_tenant_id_public_ref_key")
  @@unique([tenantId, caseCode], map: "osi_pipeline_cases_tenant_id_case_code_key")
  @@index([tenantId, status, updatedAt], map: "osi_pipeline_cases_tenant_id_status_updated_at_idx")
  @@index([tenantId, clientId, status, updatedAt], map: "osi_pipeline_cases_tenant_id_client_id_status_updated_at_idx")
  @@index([tenantId, ownerMembershipId, ownerUserId], map: "osi_pipeline_cases_tenant_owner_idx")
  @@index([mode, status, updatedAt])
  @@index([ownerId, status, updatedAt])
  @@index([createdAt])
  @@map("osi_pipeline_cases")
}

model PipelineCaseRouteSnapshot {
  id                   String                @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId             String                @map("tenant_id")
  pipelineCaseId       String                @map("pipeline_case_id")
  routeVersion         Int                   @map("route_version")
  role                 PipelineCaseRouteRole
  stopOrder            Int                   @default(0) @map("stop_order")
  sourceAddressRef     String?               @map("source_address_ref") @db.Uuid
  countryCode          String                @map("country_code") @db.Char(2)
  provinceState        String?               @map("province_state") @db.VarChar(160)
  cityMunicipality     String                @map("city_municipality") @db.VarChar(160)
  sector               String?               @db.VarChar(160)
  streetAndNumber      String?               @map("street_and_number") @db.VarChar(240)
  buildingResidential  String?               @map("building_residential") @db.VarChar(160)
  floorUnit            String?               @map("floor_unit") @db.VarChar(80)
  arrivalReference     String?               @map("arrival_reference") @db.VarChar(320)
  locationContactName  String?               @map("location_contact_name") @db.VarChar(160)
  locationContactPhone String?               @map("location_contact_phone") @db.VarChar(40)
  createdAt            DateTime              @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant               Tenant                @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_route_snapshots_tenant_fkey")
  pipelineCase         PipelineCase          @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_route_snapshots_tenant_case_fkey")
  sourceAddress        ClientAddress?        @relation("RouteSnapshotSourceAddress", fields: [tenantId, sourceAddressRef], references: [tenantId, addressRef], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_route_snapshots_source_address_fkey")

  @@unique([tenantId, pipelineCaseId, routeVersion, role, stopOrder], map: "pipeline_case_route_snapshots_position_key")
  @@index([tenantId, pipelineCaseId, routeVersion], map: "pipeline_case_route_snapshots_route_idx")
  @@index([tenantId, sourceAddressRef], map: "pipeline_case_route_snapshots_source_address_idx")
  @@map("pipeline_case_route_snapshots")
}

model PipelineCaseCommand {
  id                         String                    @id @default(uuid())
  tenantId                   String                    @map("tenant_id")
  pipelineCaseId             String                    @map("pipeline_case_id")
  requestId                  String                    @map("request_id") @db.VarChar(191)
  commandType                PipelineCaseCommandType   @map("command_type")
  payloadHash                String                    @map("payload_hash") @db.Char(64)
  expectedVersion            Int                       @map("expected_version")
  resultingVersion           Int                       @map("resulting_version")
  previousStatus             PipelineCaseStatus        @map("previous_status")
  resultingStatus            PipelineCaseStatus        @map("resulting_status")
  previousOwnerMembershipId  String?                   @map("previous_owner_membership_id")
  previousOwnerUserId        String?                   @map("previous_owner_user_id")
  resultingOwnerMembershipId String?                   @map("resulting_owner_membership_id")
  resultingOwnerUserId       String?                   @map("resulting_owner_user_id")
  actorMembershipId          String                    @map("actor_membership_id")
  actorUserId                String                    @map("actor_user_id")
  actorRole                  String                    @map("actor_role") @db.VarChar(64)
  reasonCode                 String?                   @map("reason_code") @db.VarChar(64)
  evidenceType               PipelineCaseEvidenceType? @map("evidence_type")
  evidenceId                 String?                   @map("evidence_id") @db.VarChar(191)
  createdAt                  DateTime                  @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                     Tenant                    @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_commands_tenant_id_fkey")
  pipelineCase               PipelineCase              @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_commands_tenant_case_fkey")
  actorMembership            TenantMembership          @relation("PipelineCaseCommandActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_commands_actor_fkey")
  previousOwnerMembership    TenantMembership?         @relation("PipelineCaseCommandPreviousOwner", fields: [tenantId, previousOwnerMembershipId, previousOwnerUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_commands_previous_owner_fkey")
  resultingOwnerMembership   TenantMembership?         @relation("PipelineCaseCommandResultingOwner", fields: [tenantId, resultingOwnerMembershipId, resultingOwnerUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_commands_resulting_owner_fkey")

  @@unique([tenantId, requestId], map: "pipeline_case_commands_tenant_request_key")
  @@unique([tenantId, pipelineCaseId, resultingVersion], map: "pipeline_case_commands_tenant_case_resulting_version_key")
  @@index([tenantId, pipelineCaseId, createdAt(sort: Desc), id(sort: Desc)], map: "pipeline_case_commands_tenant_case_created_at_idx")
  @@index([tenantId, actorMembershipId, createdAt(sort: Desc)], map: "pipeline_case_commands_tenant_actor_created_at_idx")
  @@map("pipeline_case_commands")
}

model ServiceCatalogItem {
  id                      String                          @id @default(cuid())
  serviceRef              String                          @default(dbgenerated("gen_random_uuid()")) @map("service_ref") @db.Uuid
  tenantId                String                          @map("tenant_id")
  code                    String                          @db.VarChar(64)
  name                    String                          @db.VarChar(160)
  category                String?                         @db.VarChar(80)
  usage                   ServiceCatalogUsage
  compatibleModes         PipelineMode[]                  @default([]) @map("compatible_modes")
  status                  ServiceCatalogStatus            @default(ACTIVE)
  sortOrder               Int                             @default(0) @map("sort_order")
  version                 Int                             @default(1)
  createdAt               DateTime                        @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt               DateTime                        @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant                  Tenant                          @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "service_catalog_items_tenant_fkey")
  allowedAsPrimary        ServiceCatalogCompatibility[]   @relation("ServiceCompatibilityPrimary")
  allowedAsComplementary  ServiceCatalogCompatibility[]   @relation("ServiceCompatibilityComplementary")
  defaultCombinations     ServiceDefaultCombination[]     @relation("ServiceDefaultPrimary")
  defaultCombinationItems ServiceDefaultCombinationItem[]
  caseServiceItems        PipelineCaseServiceItem[]

  @@unique([tenantId, id], map: "service_catalog_items_tenant_id_id_key")
  @@unique([tenantId, serviceRef], map: "service_catalog_items_tenant_service_ref_key")
  @@unique([tenantId, code], map: "service_catalog_items_tenant_code_key")
  @@index([tenantId, usage, status, sortOrder], map: "service_catalog_items_tenant_usage_status_order_idx")
  @@map("service_catalog_items")
}

model ServiceCatalogCompatibility {
  id                     String             @id @default(cuid())
  tenantId               String             @map("tenant_id")
  primaryServiceId       String             @map("primary_service_id")
  complementaryServiceId String             @map("complementary_service_id")
  createdAt              DateTime           @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                 Tenant             @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "service_catalog_compatibilities_tenant_fkey")
  primaryService         ServiceCatalogItem @relation("ServiceCompatibilityPrimary", fields: [tenantId, primaryServiceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "service_catalog_compatibilities_primary_fkey")
  complementaryService   ServiceCatalogItem @relation("ServiceCompatibilityComplementary", fields: [tenantId, complementaryServiceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "service_catalog_compatibilities_complementary_fkey")

  @@unique([tenantId, primaryServiceId, complementaryServiceId], map: "service_catalog_compatibilities_unique")
  @@index([tenantId, complementaryServiceId], map: "service_catalog_compatibilities_complementary_idx")
  @@map("service_catalog_compatibilities")
}

model ServiceDefaultCombination {
  id               String                          @id @default(cuid())
  combinationRef   String                          @default(dbgenerated("gen_random_uuid()")) @map("combination_ref") @db.Uuid
  tenantId         String                          @map("tenant_id")
  primaryServiceId String                          @map("primary_service_id")
  code             String                          @db.VarChar(64)
  name             String                          @db.VarChar(160)
  status           ServiceCatalogStatus            @default(ACTIVE)
  isDefault        Boolean                         @default(false) @map("is_default")
  version          Int                             @default(1)
  createdAt        DateTime                        @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt        DateTime                        @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant           Tenant                          @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "service_default_combinations_tenant_fkey")
  primaryService   ServiceCatalogItem              @relation("ServiceDefaultPrimary", fields: [tenantId, primaryServiceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "service_default_combinations_primary_fkey")
  items            ServiceDefaultCombinationItem[]

  @@unique([tenantId, id], map: "service_default_combinations_tenant_id_id_key")
  @@unique([tenantId, combinationRef], map: "service_default_combinations_tenant_ref_key")
  @@unique([tenantId, code], map: "service_default_combinations_tenant_code_key")
  @@index([tenantId, primaryServiceId, status], map: "service_default_combinations_primary_status_idx")
  @@map("service_default_combinations")
}

model ServiceDefaultCombinationItem {
  id            String                    @id @default(cuid())
  tenantId      String                    @map("tenant_id")
  combinationId String                    @map("combination_id")
  serviceId     String                    @map("service_id")
  position      Int
  tenant        Tenant                    @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "service_default_combination_items_tenant_fkey")
  combination   ServiceDefaultCombination @relation(fields: [tenantId, combinationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "service_default_combination_items_combination_fkey")
  service       ServiceCatalogItem        @relation(fields: [tenantId, serviceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "service_default_combination_items_service_fkey")

  @@unique([tenantId, combinationId, serviceId], map: "service_default_combination_items_service_key")
  @@unique([tenantId, combinationId, position], map: "service_default_combination_items_position_key")
  @@map("service_default_combination_items")
}

model PipelineCaseServiceRevision {
  id                    String                     @id @default(cuid())
  selectionRef          String                     @default(dbgenerated("gen_random_uuid()")) @map("selection_ref") @db.Uuid
  tenantId              String                     @map("tenant_id")
  pipelineCaseId        String                     @map("pipeline_case_id")
  revision              Int
  modeSnapshot          PipelineMode               @map("mode_snapshot")
  source                CaseServiceSelectionSource
  defaultCombinationRef String?                    @map("default_combination_ref") @db.Uuid
  createdByMembershipId String                     @map("created_by_membership_id")
  createdByUserId       String                     @map("created_by_user_id")
  createdAt             DateTime                   @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                Tenant                     @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_service_revisions_tenant_fkey")
  pipelineCase          PipelineCase               @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_service_revisions_case_fkey")
  createdByMembership   TenantMembership           @relation("PipelineCaseServiceRevisionActor", fields: [tenantId, createdByMembershipId, createdByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_service_revisions_actor_fkey")
  items                 PipelineCaseServiceItem[]
  surveyAssignments     SurveyAssignment[]
  surveyDrafts          SurveyDraft[]
  surveyPublications    SurveyPublication[]

  @@unique([tenantId, id], map: "pipeline_case_service_revisions_tenant_id_id_key")
  @@unique([tenantId, selectionRef], map: "pipeline_case_service_revisions_tenant_ref_key")
  @@unique([tenantId, pipelineCaseId, revision], map: "pipeline_case_service_revisions_case_revision_key")
  @@index([tenantId, pipelineCaseId, createdAt(sort: Desc)], map: "pipeline_case_service_revisions_case_created_idx")
  @@map("pipeline_case_service_revisions")
}

model PipelineCaseServiceItem {
  id                     String                       @id @default(cuid())
  tenantId               String                       @map("tenant_id")
  revisionId             String                       @map("revision_id")
  serviceId              String?                      @map("service_id")
  kind                   CaseServiceItemKind
  source                 CaseServiceItemSource
  position               Int
  serviceRefSnapshot     String?                      @map("service_ref_snapshot") @db.Uuid
  codeSnapshot           String                       @map("code_snapshot") @db.VarChar(64)
  nameSnapshot           String                       @map("name_snapshot") @db.VarChar(320)
  categorySnapshot       String?                      @map("category_snapshot") @db.VarChar(80)
  catalogVersionSnapshot Int?                         @map("catalog_version_snapshot")
  classificationStatus   ServiceClassificationStatus? @map("classification_status")
  tenant                 Tenant                       @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_service_items_tenant_fkey")
  revision               PipelineCaseServiceRevision  @relation(fields: [tenantId, revisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_service_items_revision_fkey")
  service                ServiceCatalogItem?          @relation(fields: [tenantId, serviceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "pipeline_case_service_items_service_fkey")

  @@unique([tenantId, revisionId, position], map: "pipeline_case_service_items_position_key")
  @@unique([tenantId, revisionId, serviceId], map: "pipeline_case_service_items_service_key")
  @@index([tenantId, serviceId], map: "pipeline_case_service_items_service_idx")
  @@map("pipeline_case_service_items")
}

model ServiceMutationCommand {
  id                String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String           @map("tenant_id")
  requestId         String           @map("request_id") @db.VarChar(191)
  operation         String           @db.VarChar(80)
  payloadHash       String           @map("payload_hash") @db.Char(64)
  targetRef         String           @map("target_ref") @db.VarChar(191)
  resultingVersion  Int              @map("resulting_version")
  actorMembershipId String           @map("actor_membership_id")
  actorUserId       String           @map("actor_user_id")
  createdAt         DateTime         @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant           @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "service_mutation_commands_tenant_fkey")
  actorMembership   TenantMembership @relation("ServiceMutationCommandActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "service_mutation_commands_actor_fkey")

  @@unique([tenantId, requestId], map: "service_mutation_commands_tenant_request_key")
  @@index([tenantId, targetRef, createdAt(sort: Desc)], map: "service_mutation_commands_target_idx")
  @@map("service_mutation_commands")
}

model SurveyCatalogVersion {
  id                    String                       @id @default(cuid())
  catalogRef            String                       @default(dbgenerated("gen_random_uuid()")) @map("catalog_ref") @db.Uuid
  tenantId              String                       @map("tenant_id")
  version               Int
  status                SurveyCatalogVersionStatus   @default(DRAFT)
  createdByMembershipId String                       @map("created_by_membership_id")
  createdByUserId       String                       @map("created_by_user_id")
  activatedAt           DateTime?                    @map("activated_at") @db.Timestamptz(6)
  retiredAt             DateTime?                    @map("retired_at") @db.Timestamptz(6)
  createdAt             DateTime                     @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                Tenant                       @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_catalog_versions_tenant_fkey")
  createdByMembership   TenantMembership             @relation("SurveyCatalogVersionActor", fields: [tenantId, createdByMembershipId, createdByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "survey_catalog_versions_actor_fkey")
  articles              SurveyArticleCatalogItem[]
  areas                 SurveyAreaCatalogItem[]
  conditions            SurveyConditionCatalogItem[]
  drafts                SurveyDraft[]

  @@unique([tenantId, id], map: "survey_catalog_versions_tenant_id_key")
  @@unique([tenantId, catalogRef], map: "survey_catalog_versions_tenant_ref_key")
  @@unique([tenantId, version], map: "survey_catalog_versions_tenant_version_key")
  @@index([tenantId, status, version(sort: Desc)], map: "survey_catalog_versions_active_idx")
  @@map("survey_catalog_versions")
}

model SurveyArticleCatalogItem {
  id               String               @id @default(cuid())
  articleRef       String               @map("article_ref") @db.Uuid
  tenantId         String               @map("tenant_id")
  catalogVersionId String               @map("catalog_version_id")
  code             String               @db.VarChar(64)
  name             String               @db.VarChar(160)
  aliases          String[]             @default([])
  frequentAreaRefs String[]             @default([]) @map("frequent_area_refs") @db.Uuid
  defaultVolumeM3  Decimal?             @map("default_volume_m3") @db.Decimal(12, 6)
  defaultWeightKg  Decimal?             @map("default_weight_kg") @db.Decimal(12, 3)
  weightSource     SurveyMetricSource?  @map("weight_source")
  status           ServiceCatalogStatus @default(ACTIVE)
  sortOrder        Int                  @default(0) @map("sort_order")
  createdAt        DateTime             @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant           Tenant               @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_article_catalog_items_tenant_fkey")
  catalogVersion   SurveyCatalogVersion @relation(fields: [tenantId, catalogVersionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_article_catalog_items_version_fkey")
  draftItems       SurveyDraftItem[]

  @@unique([tenantId, id], map: "survey_article_catalog_items_tenant_id_key")
  @@unique([tenantId, catalogVersionId, id], map: "survey_article_catalog_items_tenant_version_id_key")
  @@unique([tenantId, catalogVersionId, articleRef], map: "survey_article_catalog_items_version_ref_key")
  @@unique([tenantId, catalogVersionId, code], map: "survey_article_catalog_items_version_code_key")
  @@index([tenantId, catalogVersionId, status, sortOrder], map: "survey_article_catalog_items_lookup_idx")
  @@map("survey_article_catalog_items")
}

model SurveyAreaCatalogItem {
  id               String               @id @default(cuid())
  areaRef          String               @map("area_ref") @db.Uuid
  tenantId         String               @map("tenant_id")
  catalogVersionId String               @map("catalog_version_id")
  code             String               @db.VarChar(64)
  name             String               @db.VarChar(120)
  status           ServiceCatalogStatus @default(ACTIVE)
  sortOrder        Int                  @default(0) @map("sort_order")
  createdAt        DateTime             @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant           Tenant               @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_area_catalog_items_tenant_fkey")
  catalogVersion   SurveyCatalogVersion @relation(fields: [tenantId, catalogVersionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_area_catalog_items_version_fkey")
  draftItems       SurveyDraftItem[]

  @@unique([tenantId, id], map: "survey_area_catalog_items_tenant_id_key")
  @@unique([tenantId, catalogVersionId, id], map: "survey_area_catalog_items_tenant_version_id_key")
  @@unique([tenantId, catalogVersionId, areaRef], map: "survey_area_catalog_items_version_ref_key")
  @@unique([tenantId, catalogVersionId, code], map: "survey_area_catalog_items_version_code_key")
  @@index([tenantId, catalogVersionId, status, sortOrder], map: "survey_area_catalog_items_lookup_idx")
  @@map("survey_area_catalog_items")
}

model SurveyConditionCatalogItem {
  id               String               @id @default(cuid())
  conditionRef     String               @map("condition_ref") @db.Uuid
  tenantId         String               @map("tenant_id")
  catalogVersionId String               @map("catalog_version_id")
  code             String               @db.VarChar(64)
  name             String               @db.VarChar(160)
  kind             SurveyConditionKind
  status           ServiceCatalogStatus @default(ACTIVE)
  sortOrder        Int                  @default(0) @map("sort_order")
  createdAt        DateTime             @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant           Tenant               @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_condition_catalog_items_tenant_fkey")
  catalogVersion   SurveyCatalogVersion @relation(fields: [tenantId, catalogVersionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_condition_catalog_items_version_fkey")

  @@unique([tenantId, id], map: "survey_condition_catalog_items_tenant_id_key")
  @@unique([tenantId, catalogVersionId, conditionRef], map: "survey_condition_catalog_items_version_ref_key")
  @@unique([tenantId, catalogVersionId, code], map: "survey_condition_catalog_items_version_code_key")
  @@index([tenantId, catalogVersionId, kind, status, sortOrder], map: "survey_condition_catalog_items_lookup_idx")
  @@map("survey_condition_catalog_items")
}

model SurveyAssignment {
  id                     String                      @id @default(cuid())
  assignmentRef          String                      @default(dbgenerated("gen_random_uuid()")) @map("assignment_ref") @db.Uuid
  tenantId               String                      @map("tenant_id")
  pipelineCaseId         String                      @map("pipeline_case_id")
  serviceRevisionId      String                      @map("service_revision_id")
  routeVersion           Int                         @map("route_version")
  evaluatorMembershipId  String                      @map("evaluator_membership_id")
  evaluatorUserId        String                      @map("evaluator_user_id")
  scheduledStart         DateTime                    @map("scheduled_start") @db.Timestamptz(6)
  scheduledEnd           DateTime?                   @map("scheduled_end") @db.Timestamptz(6)
  status                 SurveyAssignmentStatus      @default(ASSIGNED)
  arrivalAt              DateTime?                   @map("arrival_at") @db.Timestamptz(6)
  punctualityConfirmedAt DateTime?                   @map("punctuality_confirmed_at") @db.Timestamptz(6)
  contextSnapshot        Json                        @map("context_snapshot")
  instructionSnapshot    String?                     @map("instruction_snapshot") @db.VarChar(1000)
  version                Int                         @default(1)
  createdByMembershipId  String                      @map("created_by_membership_id")
  createdByUserId        String                      @map("created_by_user_id")
  createdAt              DateTime                    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt              DateTime                    @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant                 Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_assignments_tenant_fkey")
  pipelineCase           PipelineCase                @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_assignments_case_fkey")
  serviceRevision        PipelineCaseServiceRevision @relation(fields: [tenantId, serviceRevisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_assignments_service_revision_fkey")
  evaluatorMembership    TenantMembership            @relation("SurveyAssignmentEvaluator", fields: [tenantId, evaluatorMembershipId, evaluatorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "survey_assignments_evaluator_fkey")
  createdByMembership    TenantMembership            @relation("SurveyAssignmentCreator", fields: [tenantId, createdByMembershipId, createdByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "survey_assignments_creator_fkey")
  drafts                 SurveyDraft[]

  @@unique([tenantId, id], map: "survey_assignments_tenant_id_key")
  @@unique([tenantId, assignmentRef], map: "survey_assignments_tenant_ref_key")
  @@index([tenantId, evaluatorMembershipId, evaluatorUserId, scheduledStart], map: "survey_assignments_evaluator_agenda_idx")
  @@index([tenantId, pipelineCaseId, scheduledStart], map: "survey_assignments_case_idx")
  @@map("survey_assignments")
}

model SurveyDraft {
  id                        String                      @id @default(cuid())
  surveyRef                 String                      @default(dbgenerated("gen_random_uuid()")) @map("survey_ref") @db.Uuid
  tenantId                  String                      @map("tenant_id")
  assignmentId              String                      @map("assignment_id")
  pipelineCaseId            String                      @map("pipeline_case_id")
  serviceRevisionId         String                      @map("service_revision_id")
  catalogVersionId          String                      @map("catalog_version_id")
  routeVersion              Int                         @map("route_version")
  revision                  Int
  version                   Int                         @default(1)
  status                    SurveyDraftStatus           @default(IN_PROGRESS)
  measurementUnitPreference SurveyMeasurementUnit       @default(CM) @map("measurement_unit_preference")
  notes                     String?                     @db.VarChar(2000)
  startedAt                 DateTime                    @default(now()) @map("started_at") @db.Timestamptz(6)
  updatedAt                 DateTime                    @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant                    Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_drafts_tenant_fkey")
  assignment                SurveyAssignment            @relation(fields: [tenantId, assignmentId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_drafts_assignment_fkey")
  pipelineCase              PipelineCase                @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_drafts_case_fkey")
  serviceRevision           PipelineCaseServiceRevision @relation(fields: [tenantId, serviceRevisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_drafts_service_revision_fkey")
  catalogVersion            SurveyCatalogVersion        @relation(fields: [tenantId, catalogVersionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_drafts_catalog_version_fkey")
  items                     SurveyDraftItem[]
  accessObservations        SurveyAccessObservation[]
  photos                    SurveyPhoto[]
  publications              SurveyPublication[]

  @@unique([tenantId, id], map: "survey_drafts_tenant_id_key")
  @@unique([tenantId, id, catalogVersionId], map: "survey_drafts_tenant_id_catalog_version_key")
  @@unique([tenantId, surveyRef], map: "survey_drafts_tenant_ref_key")
  @@unique([tenantId, assignmentId, revision], map: "survey_drafts_assignment_revision_key")
  @@index([tenantId, pipelineCaseId, status, updatedAt(sort: Desc)], map: "survey_drafts_case_status_idx")
  @@map("survey_drafts")
}

model SurveyDraftItem {
  id                  String                   @id @default(cuid())
  itemRef             String                   @default(dbgenerated("gen_random_uuid()")) @map("item_ref") @db.Uuid
  tenantId            String                   @map("tenant_id")
  draftId             String                   @map("draft_id")
  catalogVersionId    String                   @map("catalog_version_id")
  catalogItemId       String                   @map("catalog_item_id")
  areaCatalogItemId   String                   @map("area_catalog_item_id")
  articleRefSnapshot  String                   @map("article_ref_snapshot") @db.Uuid
  articleCodeSnapshot String                   @map("article_code_snapshot") @db.VarChar(64)
  articleNameSnapshot String                   @map("article_name_snapshot") @db.VarChar(160)
  areaRefSnapshot     String                   @map("area_ref_snapshot") @db.Uuid
  areaCodeSnapshot    String                   @map("area_code_snapshot") @db.VarChar(64)
  areaNameSnapshot    String                   @map("area_name_snapshot") @db.VarChar(120)
  shipmentMode        SurveyShipmentMode       @map("shipment_mode")
  quantity            Int                      @default(1)
  condition           SurveyItemCondition      @default(GOOD)
  flags               SurveyItemFlag[]         @default([])
  originalUnit        SurveyMeasurementUnit?   @map("original_unit")
  originalDimensions  Json?                    @map("original_dimensions")
  lengthCm            Decimal?                 @map("length_cm") @db.Decimal(12, 3)
  widthCm             Decimal?                 @map("width_cm") @db.Decimal(12, 3)
  heightCm            Decimal?                 @map("height_cm") @db.Decimal(12, 3)
  unitVolumeM3        Decimal?                 @map("unit_volume_m3") @db.Decimal(12, 6)
  unitWeightKg        Decimal?                 @map("unit_weight_kg") @db.Decimal(12, 3)
  volumeSource        SurveyMetricSource?      @map("volume_source")
  weightSource        SurveyMetricSource?      @map("weight_source")
  note                String?                  @db.VarChar(1000)
  sortOrder           Int                      @map("sort_order")
  version             Int                      @default(1)
  deletedAt           DateTime?                @map("deleted_at") @db.Timestamptz(6)
  createdAt           DateTime                 @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt           DateTime                 @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant              Tenant                   @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_draft_items_tenant_fkey")
  draft               SurveyDraft              @relation(fields: [tenantId, draftId, catalogVersionId], references: [tenantId, id, catalogVersionId], onDelete: Restrict, onUpdate: Cascade, map: "survey_draft_items_draft_fkey")
  catalogItem         SurveyArticleCatalogItem @relation(fields: [tenantId, catalogVersionId, catalogItemId], references: [tenantId, catalogVersionId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_draft_items_catalog_item_fkey")
  areaCatalogItem     SurveyAreaCatalogItem    @relation(fields: [tenantId, catalogVersionId, areaCatalogItemId], references: [tenantId, catalogVersionId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_draft_items_area_item_fkey")
  photos              SurveyPhoto[]

  @@unique([tenantId, id], map: "survey_draft_items_tenant_id_key")
  @@unique([tenantId, draftId, id], map: "survey_draft_items_tenant_draft_id_key")
  @@unique([tenantId, itemRef], map: "survey_draft_items_tenant_ref_key")
  @@unique([tenantId, draftId, sortOrder], map: "survey_draft_items_sort_key")
  @@index([tenantId, draftId, areaRefSnapshot, deletedAt], map: "survey_draft_items_area_idx")
  @@map("survey_draft_items")
}

model SurveyAccessObservation {
  id                String             @id @default(cuid())
  accessRef         String             @default(dbgenerated("gen_random_uuid()")) @map("access_ref") @db.Uuid
  tenantId          String             @map("tenant_id")
  draftId           String             @map("draft_id")
  side              SurveyAccessSide
  floorNumber       Int?               @map("floor_number")
  stairsFloors      Int?               @map("stairs_floors")
  elevatorAvailable Boolean?           @map("elevator_available")
  elevatorFloor     Int?               @map("elevator_floor")
  parkingDistanceM  Decimal?           @map("parking_distance_m") @db.Decimal(12, 2)
  flags             SurveyAccessFlag[] @default([])
  notes             String?            @db.VarChar(1000)
  version           Int                @default(1)
  createdAt         DateTime           @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt         DateTime           @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant            Tenant             @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_access_observations_tenant_fkey")
  draft             SurveyDraft        @relation(fields: [tenantId, draftId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_access_observations_draft_fkey")
  photos            SurveyPhoto[]

  @@unique([tenantId, id], map: "survey_access_observations_tenant_id_key")
  @@unique([tenantId, draftId, id], map: "survey_access_observations_tenant_draft_id_key")
  @@unique([tenantId, accessRef], map: "survey_access_observations_tenant_ref_key")
  @@unique([tenantId, draftId, side], map: "survey_access_observations_side_key")
  @@map("survey_access_observations")
}

model SurveyBlobObject {
  id             String                      @id @default(cuid())
  blobRef        String                      @default(dbgenerated("gen_random_uuid()")) @map("blob_ref") @db.Uuid
  tenantId       String                      @map("tenant_id")
  provider       String                      @db.VarChar(40)
  storageKey     String                      @map("storage_key") @db.VarChar(320)
  mimeType       String                      @map("mime_type") @db.VarChar(100)
  sizeBytes      Int                         @map("size_bytes")
  sha256         String                      @db.Char(64)
  status         SurveyBlobStatus            @default(ACTIVE)
  createdAt      DateTime                    @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant         Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_blob_objects_tenant_fkey")
  photos         SurveyPhoto[]
  signature      SurveyPublicationSignature? @relation("SurveySignatureBlob")
  publicationPdf SurveyPublication?          @relation("SurveyPublicationPdf")

  @@unique([tenantId, id], map: "survey_blob_objects_tenant_id_key")
  @@unique([tenantId, blobRef], map: "survey_blob_objects_tenant_ref_key")
  @@unique([tenantId, storageKey], map: "survey_blob_objects_tenant_storage_key")
  @@index([tenantId, status, createdAt], map: "survey_blob_objects_status_idx")
  @@map("survey_blob_objects")
}

model SurveyPhoto {
  id           String                   @id @default(cuid())
  photoRef     String                   @default(dbgenerated("gen_random_uuid()")) @map("photo_ref") @db.Uuid
  tenantId     String                   @map("tenant_id")
  draftId      String                   @map("draft_id")
  draftItemId  String?                  @map("draft_item_id")
  accessId     String?                  @map("access_id")
  blobObjectId String                   @unique @map("blob_object_id")
  purpose      SurveyPhotoPurpose
  createdAt    DateTime                 @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant       Tenant                   @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_photos_tenant_fkey")
  draft        SurveyDraft              @relation(fields: [tenantId, draftId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_photos_draft_fkey")
  draftItem    SurveyDraftItem?         @relation(fields: [tenantId, draftId, draftItemId], references: [tenantId, draftId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_photos_item_fkey")
  access       SurveyAccessObservation? @relation(fields: [tenantId, draftId, accessId], references: [tenantId, draftId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_photos_access_fkey")
  blobObject   SurveyBlobObject         @relation(fields: [tenantId, blobObjectId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_photos_blob_fkey")

  @@unique([tenantId, id], map: "survey_photos_tenant_id_key")
  @@unique([tenantId, photoRef], map: "survey_photos_tenant_ref_key")
  @@index([tenantId, draftId, purpose], map: "survey_photos_draft_purpose_idx")
  @@map("survey_photos")
}

model SurveyPublication {
  id                      String                        @id @default(cuid())
  publicationRef          String                        @default(dbgenerated("gen_random_uuid()")) @map("publication_ref") @db.Uuid
  tenantId                String                        @map("tenant_id")
  draftId                 String                        @map("draft_id")
  pipelineCaseId          String                        @map("pipeline_case_id")
  serviceRevisionId       String                        @map("service_revision_id")
  revision                Int
  status                  SurveyPublicationStatus       @default(CURRENT)
  routeVersion            Int                           @map("route_version")
  catalogVersion          Int                           @map("catalog_version")
  serviceSelectionRef     String                        @map("service_selection_ref") @db.Uuid
  contextSnapshot         Json                          @map("context_snapshot")
  totalsSnapshot          Json                          @map("totals_snapshot")
  logicalSha256           String                        @map("logical_sha256") @db.Char(64)
  pdfBlobObjectId         String                        @unique @map("pdf_blob_object_id")
  pdfSha256               String                        @map("pdf_sha256") @db.Char(64)
  replacesPublicationId   String?                       @map("replaces_publication_id")
  publishedByMembershipId String                        @map("published_by_membership_id")
  publishedByUserId       String                        @map("published_by_user_id")
  publishedAt             DateTime                      @default(now()) @map("published_at") @db.Timestamptz(6)
  tenant                  Tenant                        @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publications_tenant_fkey")
  draft                   SurveyDraft                   @relation(fields: [tenantId, draftId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publications_draft_fkey")
  pipelineCase            PipelineCase                  @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publications_case_fkey")
  serviceRevision         PipelineCaseServiceRevision   @relation(fields: [tenantId, serviceRevisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publications_service_revision_fkey")
  pdfBlobObject           SurveyBlobObject              @relation("SurveyPublicationPdf", fields: [tenantId, pdfBlobObjectId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publications_pdf_blob_fkey")
  replacesPublication     SurveyPublication?            @relation("SurveyPublicationReplacement", fields: [tenantId, replacesPublicationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publications_replaces_fkey")
  replacement             SurveyPublication?            @relation("SurveyPublicationReplacement")
  publishedByMembership   TenantMembership              @relation("SurveyPublicationActor", fields: [tenantId, publishedByMembershipId, publishedByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "survey_publications_actor_fkey")
  items                   SurveyPublicationItem[]
  accessSnapshots         SurveyPublicationAccess[]
  signature               SurveyPublicationSignature?
  materialRequirements    MaterialRequirementSnapshot[]

  @@unique([tenantId, id], map: "survey_publications_tenant_id_key")
  @@unique([tenantId, publicationRef], map: "survey_publications_tenant_ref_key")
  @@unique([tenantId, draftId, revision], map: "survey_publications_draft_revision_key")
  @@unique([tenantId, pdfBlobObjectId], map: "survey_publications_tenant_pdf_blob_key")
  @@unique([tenantId, replacesPublicationId], map: "survey_publications_tenant_replaces_key")
  @@index([tenantId, pipelineCaseId, status, publishedAt(sort: Desc)], map: "survey_publications_case_status_idx")
  @@map("survey_publications")
}

model SurveyPublicationItem {
  id            String              @id @default(cuid())
  tenantId      String              @map("tenant_id")
  publicationId String              @map("publication_id")
  position      Int
  articleRef    String              @map("article_ref") @db.Uuid
  articleCode   String              @map("article_code") @db.VarChar(64)
  articleName   String              @map("article_name") @db.VarChar(160)
  areaRef       String              @map("area_ref") @db.Uuid
  areaCode      String              @map("area_code") @db.VarChar(64)
  areaName      String              @map("area_name") @db.VarChar(120)
  shipmentMode  SurveyShipmentMode  @map("shipment_mode")
  quantity      Int
  condition     SurveyItemCondition
  flags         SurveyItemFlag[]    @default([])
  measurements  Json?
  unitVolumeM3  Decimal?            @map("unit_volume_m3") @db.Decimal(12, 6)
  unitWeightKg  Decimal?            @map("unit_weight_kg") @db.Decimal(12, 3)
  metricSources Json                @map("metric_sources")
  note          String?             @db.VarChar(1000)
  photoRefs     String[]            @default([]) @map("photo_refs") @db.Uuid
  tenant        Tenant              @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publication_items_tenant_fkey")
  publication   SurveyPublication   @relation(fields: [tenantId, publicationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publication_items_publication_fkey")

  @@unique([tenantId, publicationId, position], map: "survey_publication_items_position_key")
  @@index([tenantId, articleRef], map: "survey_publication_items_article_idx")
  @@map("survey_publication_items")
}

model SurveyPublicationAccess {
  id            String            @id @default(cuid())
  tenantId      String            @map("tenant_id")
  publicationId String            @map("publication_id")
  side          SurveyAccessSide
  factsSnapshot Json              @map("facts_snapshot")
  photoRefs     String[]          @default([]) @map("photo_refs") @db.Uuid
  tenant        Tenant            @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publication_access_tenant_fkey")
  publication   SurveyPublication @relation(fields: [tenantId, publicationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publication_access_publication_fkey")

  @@unique([tenantId, publicationId, side], map: "survey_publication_access_side_key")
  @@map("survey_publication_access")
}

model SurveyPublicationSignature {
  id            String            @id @default(cuid())
  signatureRef  String            @default(dbgenerated("gen_random_uuid()")) @map("signature_ref") @db.Uuid
  tenantId      String            @map("tenant_id")
  publicationId String            @unique @map("publication_id")
  blobObjectId  String            @unique @map("blob_object_id")
  signerName    String            @map("signer_name") @db.VarChar(160)
  relationship  String            @db.VarChar(120)
  signedAt      DateTime          @map("signed_at") @db.Timestamptz(6)
  tenant        Tenant            @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publication_signatures_tenant_fkey")
  publication   SurveyPublication @relation(fields: [tenantId, publicationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publication_signatures_publication_fkey")
  blobObject    SurveyBlobObject  @relation("SurveySignatureBlob", fields: [tenantId, blobObjectId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "survey_publication_signatures_blob_fkey")

  @@unique([tenantId, signatureRef], map: "survey_publication_signatures_tenant_ref_key")
  @@unique([tenantId, publicationId], map: "survey_publication_signatures_tenant_publication_key")
  @@unique([tenantId, blobObjectId], map: "survey_publication_signatures_tenant_blob_key")
  @@map("survey_publication_signatures")
}

model SurveyMutationCommand {
  id                String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String           @map("tenant_id")
  requestId         String           @map("request_id") @db.VarChar(191)
  operation         String           @db.VarChar(80)
  payloadHash       String           @map("payload_hash") @db.Char(64)
  targetRef         String           @map("target_ref") @db.VarChar(191)
  resultingVersion  Int              @map("resulting_version")
  actorMembershipId String           @map("actor_membership_id")
  actorUserId       String           @map("actor_user_id")
  resultJson        Json             @map("result_json")
  createdAt         DateTime         @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant           @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "survey_mutation_commands_tenant_fkey")
  actorMembership   TenantMembership @relation("SurveyMutationCommandActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "survey_mutation_commands_actor_fkey")

  @@unique([tenantId, requestId], map: "survey_mutation_commands_tenant_request_key")
  @@index([tenantId, targetRef, createdAt(sort: Desc)], map: "survey_mutation_commands_target_idx")
  @@map("survey_mutation_commands")
}

model GeoRegion {
  id                                                                                                             String                   @id @default(cuid())
  tenant_id                                                                                                      String
  series_id                                                                                                      String
  version                                                                                                        Int
  country_code                                                                                                   String                   @db.Char(2)
  code                                                                                                           String                   @db.VarChar(100)
  normalized_code                                                                                                String                   @db.VarChar(120)
  name                                                                                                           String                   @db.VarChar(180)
  normalized_name                                                                                                String                   @db.VarChar(200)
  administrative_division                                                                                        String?                  @db.VarChar(160)
  region_type                                                                                                    String                   @db.VarChar(80)
  zone_type                                                                                                      String                   @db.VarChar(80)
  latitude                                                                                                       Decimal                  @db.Decimal(10, 7)
  longitude                                                                                                      Decimal                  @db.Decimal(10, 7)
  sla_hours                                                                                                      Int?
  geography_json                                                                                                 Json                     @default("{}")
  aliases_snapshot_json                                                                                          Json                     @default("[]")
  state                                                                                                          LogisticsConfigState     @default(DRAFT)
  valid_from                                                                                                     DateTime?
  valid_to                                                                                                       DateTime?
  version_hash                                                                                                   String                   @db.Char(64)
  replaces_region_id                                                                                             String?
  source                                                                                                         String                   @db.VarChar(120)
  evidence_refs_json                                                                                             Json                     @default("[]")
  created_by_user_id                                                                                             String
  created_by_membership_id                                                                                       String
  approved_by_user_id                                                                                            String?
  approved_by_membership_id                                                                                      String?
  approved_at                                                                                                    DateTime?
  activated_at                                                                                                   DateTime?
  retired_at                                                                                                     DateTime?
  request_id                                                                                                     String                   @db.VarChar(191)
  payload_hash                                                                                                   String                   @db.Char(64)
  row_version                                                                                                    Int                      @default(1)
  created_at                                                                                                     DateTime                 @default(now())
  updated_at                                                                                                     DateTime                 @default(now())
  osi_geo_region_aliases                                                                                         osi_geo_region_aliases[]
  tenant_memberships_osi_geo_regions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships TenantMembership?        @relation("osi_geo_regions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships", fields: [tenant_id, approved_by_membership_id, approved_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "osi_geo_regions_approver_fkey")
  tenant_memberships_osi_geo_regions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships   TenantMembership         @relation("osi_geo_regions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships", fields: [tenant_id, created_by_membership_id, created_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "osi_geo_regions_creator_fkey")
  osi_geo_regions                                                                                                GeoRegion?               @relation("osi_geo_regionsToosi_geo_regions", fields: [tenant_id, replaces_region_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_geo_regions_replaces_fkey")
  other_osi_geo_regions                                                                                          GeoRegion[]              @relation("osi_geo_regionsToosi_geo_regions")
  tenants                                                                                                        Tenant                   @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "osi_geo_regions_tenant_fkey")
  osi_transport_zone_rules_osi_transport_zone_rules_tenant_id_destination_region_idToosi_geo_regions             TransportZoneRule[]      @relation("osi_transport_zone_rules_tenant_id_destination_region_idToosi_geo_regions")
  osi_transport_zone_rules_osi_transport_zone_rules_tenant_id_origin_region_idToosi_geo_regions                  TransportZoneRule[]      @relation("osi_transport_zone_rules_tenant_id_origin_region_idToosi_geo_regions")
  osi_zone_rules_osi_zone_rules_tenant_id_destination_region_idToosi_geo_regions                                 ZoneRule[]               @relation("osi_zone_rules_tenant_id_destination_region_idToosi_geo_regions")
  osi_zone_rules_osi_zone_rules_tenant_id_origin_region_idToosi_geo_regions                                      ZoneRule[]               @relation("osi_zone_rules_tenant_id_origin_region_idToosi_geo_regions")
  osi_zone_rules_osi_zone_rules_tenant_id_region_idToosi_geo_regions                                             ZoneRule[]               @relation("osi_zone_rules_tenant_id_region_idToosi_geo_regions")

  @@unique([tenant_id, version_hash], map: "osi_geo_regions_tenant_hash_key")
  @@unique([tenant_id, id], map: "osi_geo_regions_tenant_id_key")
  @@unique([tenant_id, request_id], map: "osi_geo_regions_tenant_request_key")
  @@unique([tenant_id, series_id, version], map: "osi_geo_regions_tenant_series_version_key")
  @@index([tenant_id, country_code, state, normalized_name], map: "osi_geo_regions_lookup_idx")
  @@index([tenant_id, zone_type, state], map: "osi_geo_regions_zone_idx")
  @@map("osi_geo_regions")
}

model Hub {
  id                       String                     @id @default(cuid())
  name                     String
  lat                      Float
  lng                      Float
  active                   Boolean                    @default(true)
  priority                 Int                        @default(1)
  createdAt                DateTime                   @default(now()) @map("created_at")
  globalCommercialSettings GlobalCommercialSettings[]

  @@index([active, priority])
  @@map("osi_hubs")
}

model ZoneRule {
  id                                                                                                            String               @id @default(cuid())
  tenant_id                                                                                                     String
  series_id                                                                                                     String
  code                                                                                                          String               @db.VarChar(100)
  version                                                                                                       Int
  name                                                                                                          String               @db.VarChar(180)
  kind                                                                                                          ZoneRuleKind
  priority                                                                                                      Int                  @default(100)
  scope_key                                                                                                     String               @db.VarChar(300)
  country_code                                                                                                  String?              @db.Char(2)
  zoneType                                                                                                      String?              @map("zone_type") @db.VarChar(80)
  region_id                                                                                                     String?
  origin_region_id                                                                                              String?
  destination_region_id                                                                                         String?
  distance_min_km                                                                                               Decimal?             @db.Decimal(12, 3)
  distance_max_km                                                                                               Decimal?             @db.Decimal(12, 3)
  freeKm                                                                                                        Decimal?             @map("free_km") @db.Decimal(12, 3)
  kmRate                                                                                                        Decimal?             @map("km_rate") @db.Decimal(18, 4)
  surchargePercent                                                                                              Decimal?             @map("surcharge_percent") @db.Decimal(9, 4)
  slaHours                                                                                                      Int?                 @map("sla_hours")
  weekend_surcharge_percent                                                                                     Decimal?             @db.Decimal(9, 4)
  after_hours_surcharge_percent                                                                                 Decimal?             @db.Decimal(9, 4)
  result_hash                                                                                                   String               @db.Char(64)
  state                                                                                                         LogisticsConfigState @default(DRAFT)
  valid_from                                                                                                    DateTime?
  valid_to                                                                                                      DateTime?
  version_hash                                                                                                  String               @db.Char(64)
  replaces_rule_id                                                                                              String?
  risk_rule_id                                                                                                  String?
  risk_rule_hash                                                                                                String?              @db.Char(64)
  source                                                                                                        String               @db.VarChar(120)
  evidence_refs_json                                                                                            Json                 @default("[]")
  created_by_user_id                                                                                            String
  created_by_membership_id                                                                                      String
  approved_by_user_id                                                                                           String?
  approved_by_membership_id                                                                                     String?
  approved_at                                                                                                   DateTime?
  activated_at                                                                                                  DateTime?
  retired_at                                                                                                    DateTime?
  request_id                                                                                                    String               @db.VarChar(191)
  payload_hash                                                                                                  String               @db.Char(64)
  row_version                                                                                                   Int                  @default(1)
  createdAt                                                                                                     DateTime             @default(now()) @map("created_at")
  updated_at                                                                                                    DateTime             @default(now())
  currency_code                                                                                                 String?              @db.Char(3)
  km_rate_unit                                                                                                  String?              @db.VarChar(32)
  tenant_memberships_osi_zone_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships TenantMembership?    @relation("osi_zone_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships", fields: [tenant_id, approved_by_membership_id, approved_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "osi_zone_rules_approver_fkey")
  tenant_memberships_osi_zone_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships   TenantMembership     @relation("osi_zone_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships", fields: [tenant_id, created_by_membership_id, created_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "osi_zone_rules_creator_fkey")
  osi_geo_regions_osi_zone_rules_tenant_id_destination_region_idToosi_geo_regions                               GeoRegion?           @relation("osi_zone_rules_tenant_id_destination_region_idToosi_geo_regions", fields: [tenant_id, destination_region_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_zone_rules_destination_fkey")
  osi_geo_regions_osi_zone_rules_tenant_id_origin_region_idToosi_geo_regions                                    GeoRegion?           @relation("osi_zone_rules_tenant_id_origin_region_idToosi_geo_regions", fields: [tenant_id, origin_region_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_zone_rules_origin_fkey")
  osi_geo_regions_osi_zone_rules_tenant_id_region_idToosi_geo_regions                                           GeoRegion?           @relation("osi_zone_rules_tenant_id_region_idToosi_geo_regions", fields: [tenant_id, region_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_zone_rules_region_fkey")
  osi_zone_rules                                                                                                ZoneRule?            @relation("osi_zone_rulesToosi_zone_rules", fields: [tenant_id, replaces_rule_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_zone_rules_replaces_fkey")
  other_osi_zone_rules                                                                                          ZoneRule[]           @relation("osi_zone_rulesToosi_zone_rules")
  risk_engine_rules                                                                                             risk_engine_rules?   @relation(fields: [tenant_id, risk_rule_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_zone_rules_risk_fkey")
  tenants                                                                                                       Tenant               @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "osi_zone_rules_tenant_fkey")

  @@unique([tenant_id, code, version], map: "osi_zone_rules_tenant_code_version_key")
  @@unique([tenant_id, version_hash], map: "osi_zone_rules_tenant_hash_key")
  @@unique([tenant_id, id], map: "osi_zone_rules_tenant_id_key")
  @@unique([tenant_id, request_id], map: "osi_zone_rules_tenant_request_key")
  @@unique([tenant_id, series_id, version], map: "osi_zone_rules_tenant_series_version_key")
  @@index([tenant_id, state, kind, scope_key, priority, valid_from, valid_to], map: "osi_zone_rules_resolve_idx")
  @@map("osi_zone_rules")
}

model TransportZoneRule {
  id                                                                                                                      String               @id @default(cuid())
  tenant_id                                                                                                               String
  series_id                                                                                                               String
  code                                                                                                                    String               @db.VarChar(100)
  version                                                                                                                 Int
  name                                                                                                                    String               @db.VarChar(180)
  scope                                                                                                                   TransportRuleScope
  priority                                                                                                                Int                  @default(100)
  scope_key                                                                                                               String               @db.VarChar(300)
  country_code                                                                                                            String?              @db.Char(2)
  service_mode                                                                                                            String?              @db.VarChar(80)
  zoneType                                                                                                                String?              @map("zone_type") @db.VarChar(80)
  origin_region_id                                                                                                        String?
  destination_region_id                                                                                                   String?
  distance_min_km                                                                                                         Decimal?             @db.Decimal(12, 3)
  distance_max_km                                                                                                         Decimal?             @db.Decimal(12, 3)
  kmMultiplier                                                                                                            Decimal              @default(1) @map("km_multiplier") @db.Decimal(12, 6)
  volumeMultiplier                                                                                                        Decimal              @default(1) @map("volume_multiplier") @db.Decimal(12, 6)
  surchargePercent                                                                                                        Decimal              @default(0) @map("surcharge_percent") @db.Decimal(9, 4)
  minimum_charge                                                                                                          Decimal?             @db.Decimal(18, 2)
  result_hash                                                                                                             String               @db.Char(64)
  state                                                                                                                   LogisticsConfigState @default(DRAFT)
  valid_from                                                                                                              DateTime?
  valid_to                                                                                                                DateTime?
  version_hash                                                                                                            String               @db.Char(64)
  replaces_rule_id                                                                                                        String?
  risk_rule_id                                                                                                            String?
  risk_rule_hash                                                                                                          String?              @db.Char(64)
  source                                                                                                                  String               @db.VarChar(120)
  evidence_refs_json                                                                                                      Json                 @default("[]")
  created_by_user_id                                                                                                      String
  created_by_membership_id                                                                                                String
  approved_by_user_id                                                                                                     String?
  approved_by_membership_id                                                                                               String?
  approved_at                                                                                                             DateTime?
  activated_at                                                                                                            DateTime?
  retired_at                                                                                                              DateTime?
  request_id                                                                                                              String               @db.VarChar(191)
  payload_hash                                                                                                            String               @db.Char(64)
  row_version                                                                                                             Int                  @default(1)
  created_at                                                                                                              DateTime             @default(now())
  updated_at                                                                                                              DateTime             @default(now())
  currency_code                                                                                                           String?              @db.Char(3)
  tenant_memberships_osi_transport_zone_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships TenantMembership?    @relation("osi_transport_zone_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships", fields: [tenant_id, approved_by_membership_id, approved_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "osi_transport_zone_rules_approver_fkey")
  tenant_memberships_osi_transport_zone_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships   TenantMembership     @relation("osi_transport_zone_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships", fields: [tenant_id, created_by_membership_id, created_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "osi_transport_zone_rules_creator_fkey")
  osi_geo_regions_osi_transport_zone_rules_tenant_id_destination_region_idToosi_geo_regions                               GeoRegion?           @relation("osi_transport_zone_rules_tenant_id_destination_region_idToosi_geo_regions", fields: [tenant_id, destination_region_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_transport_zone_rules_destination_fkey")
  osi_geo_regions_osi_transport_zone_rules_tenant_id_origin_region_idToosi_geo_regions                                    GeoRegion?           @relation("osi_transport_zone_rules_tenant_id_origin_region_idToosi_geo_regions", fields: [tenant_id, origin_region_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_transport_zone_rules_origin_fkey")
  osi_transport_zone_rules                                                                                                TransportZoneRule?   @relation("osi_transport_zone_rulesToosi_transport_zone_rules", fields: [tenant_id, replaces_rule_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_transport_zone_rules_replaces_fkey")
  other_osi_transport_zone_rules                                                                                          TransportZoneRule[]  @relation("osi_transport_zone_rulesToosi_transport_zone_rules")
  risk_engine_rules                                                                                                       risk_engine_rules?   @relation(fields: [tenant_id, risk_rule_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_transport_zone_rules_risk_fkey")
  tenants                                                                                                                 Tenant               @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "osi_transport_zone_rules_tenant_fkey")

  @@unique([tenant_id, code, version], map: "osi_transport_zone_rules_tenant_code_version_key")
  @@unique([tenant_id, version_hash], map: "osi_transport_zone_rules_tenant_hash_key")
  @@unique([tenant_id, id], map: "osi_transport_zone_rules_tenant_id_key")
  @@unique([tenant_id, request_id], map: "osi_transport_zone_rules_tenant_request_key")
  @@unique([tenant_id, series_id, version], map: "osi_transport_zone_rules_tenant_series_version_key")
  @@index([tenant_id, state, scope, scope_key, priority, valid_from, valid_to], map: "osi_transport_zone_rules_resolve_idx")
  @@map("osi_transport_zone_rules")
}

model Vehicle {
  id                        String                   @id @default(cuid())
  tenant_id                 String
  business_code             String                   @db.VarChar(80)
  normalized_code           String                   @db.VarChar(80)
  plate                     String?                  @db.VarChar(40)
  normalized_plate          String?                  @db.VarChar(40)
  vin                       String?                  @db.VarChar(80)
  normalized_vin            String?                  @db.VarChar(80)
  source_stable_id          String?                  @db.VarChar(191)
  vehicle_type              String                   @db.VarChar(80)
  brand                     String?                  @db.VarChar(120)
  model                     String?                  @db.VarChar(120)
  model_year                Int?
  capacity_weight           Decimal?                 @db.Decimal(14, 3)
  capacity_volume           Decimal?                 @db.Decimal(14, 4)
  usable_length             Decimal?                 @db.Decimal(12, 4)
  usable_width              Decimal?                 @db.Decimal(12, 4)
  usable_height             Decimal?                 @db.Decimal(12, 4)
  weight_unit               String                   @default("KG") @db.VarChar(12)
  volume_unit               String                   @default("CBM") @db.VarChar(12)
  dimension_unit            String                   @default("M") @db.VarChar(12)
  operational_status        VehicleOperationalStatus @default(AVAILABLE)
  available_for_calculation Boolean                  @default(true)
  effective_from            DateTime                 @default(now()) @db.Timestamptz(3)
  effective_to              DateTime?                @db.Timestamptz(3)
  hub_code                  String?                  @db.VarChar(80)
  source                    String                   @db.VarChar(80)
  import_batch_id           String?
  request_id                String                   @db.VarChar(191)
  payload_hash              String                   @db.Char(64)
  calculation_locked_at     DateTime?                @db.Timestamptz(3)
  retired_at                DateTime?                @db.Timestamptz(3)
  created_by_user_id        String?
  created_by_membership_id  String?
  row_version               Int                      @default(1)
  createdAt                 DateTime                 @default(now()) @map("created_at") @db.Timestamptz(3)
  updated_at                DateTime                 @default(now()) @db.Timestamptz(3)
  tenant_memberships        TenantMembership?        @relation(fields: [tenant_id, created_by_membership_id, created_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "osi_vehicles_created_membership_fkey")
  vehicle_import_batches    vehicle_import_batches?  @relation(fields: [tenant_id, import_batch_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_vehicles_import_batch_fkey")
  tenants                   Tenant                   @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "osi_vehicles_tenant_fkey")
  vehicle_import_items      vehicle_import_items[]

  @@unique([tenant_id, normalized_code], map: "osi_vehicles_tenant_code_key")
  @@unique([tenant_id, id])
  @@unique([tenant_id, request_id], map: "osi_vehicles_tenant_request_id_key")
  @@index([tenant_id, import_batch_id], map: "osi_vehicles_import_batch_idx")
  @@index([tenant_id, capacity_volume, capacity_weight], map: "osi_vehicles_tenant_capacity_idx")
  @@index([tenant_id, operational_status, available_for_calculation], map: "osi_vehicles_tenant_status_available_idx")
  @@map("osi_vehicles")
}

model VehicleEngineSettings {
  id                                                                                                                         String                  @id @default(cuid())
  tenant_id                                                                                                                  String
  series_id                                                                                                                  String
  scope_key                                                                                                                  String                  @db.VarChar(120)
  version                                                                                                                    Int
  name                                                                                                                       String                  @db.VarChar(160)
  state                                                                                                                      LogisticsConfigState    @default(DRAFT)
  operation_mode                                                                                                             LogisticsOperationMode  @default(LEGACY_ONLY)
  allowManualOverride                                                                                                        Boolean                 @default(true) @map("allow_manual_override")
  requireApprovalIfOverride                                                                                                  Boolean                 @default(false) @map("require_approval_if_override")
  distributeWearAutomatically                                                                                                Boolean                 @default(true) @map("distribute_wear_automatically")
  consider_upcoming_maintenance                                                                                              Boolean                 @default(true)
  block_if_no_vehicle                                                                                                        Boolean                 @default(false)
  capacity_utilization_percent                                                                                               Decimal                 @default(85) @db.Decimal(6, 3)
  weight_unit                                                                                                                String                  @default("KG") @db.VarChar(12)
  volume_unit                                                                                                                String                  @default("CBM") @db.VarChar(12)
  distance_unit                                                                                                              String                  @default("KM") @db.VarChar(12)
  settings_json                                                                                                              Json                    @default("{}")
  settings_hash                                                                                                              String                  @db.Char(64)
  valid_from                                                                                                                 DateTime?               @db.Timestamptz(3)
  valid_to                                                                                                                   DateTime?               @db.Timestamptz(3)
  version_hash                                                                                                               String                  @db.Char(64)
  replaces_settings_id                                                                                                       String?
  source                                                                                                                     String                  @db.VarChar(80)
  evidence_json                                                                                                              Json                    @default("{}")
  created_by_user_id                                                                                                         String?
  created_by_membership_id                                                                                                   String?
  approved_by_user_id                                                                                                        String?
  approved_by_membership_id                                                                                                  String?
  approved_at                                                                                                                DateTime?               @db.Timestamptz(3)
  activated_at                                                                                                               DateTime?               @db.Timestamptz(3)
  retired_at                                                                                                                 DateTime?               @db.Timestamptz(3)
  request_id                                                                                                                 String                  @db.VarChar(191)
  payload_hash                                                                                                               String                  @db.Char(64)
  row_version                                                                                                                Int                     @default(1)
  createdAt                                                                                                                  DateTime                @default(now()) @map("created_at") @db.Timestamptz(3)
  updatedAt                                                                                                                  DateTime                @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(3)
  tenant_memberships_osi_vehicle_engine_settings_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships TenantMembership?       @relation("osi_vehicle_engine_settings_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships", fields: [tenant_id, approved_by_membership_id, approved_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "osi_vehicle_engine_settings_approved_membership_fkey")
  tenant_memberships_osi_vehicle_engine_settings_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships   TenantMembership?       @relation("osi_vehicle_engine_settings_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships", fields: [tenant_id, created_by_membership_id, created_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "osi_vehicle_engine_settings_created_membership_fkey")
  osi_vehicle_engine_settings                                                                                                VehicleEngineSettings?  @relation("osi_vehicle_engine_settingsToosi_vehicle_engine_settings", fields: [tenant_id, replaces_settings_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_vehicle_engine_settings_replaces_fkey")
  other_osi_vehicle_engine_settings                                                                                          VehicleEngineSettings[] @relation("osi_vehicle_engine_settingsToosi_vehicle_engine_settings")
  tenants                                                                                                                    Tenant                  @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "osi_vehicle_engine_settings_tenant_fkey")

  @@unique([tenant_id, id])
  @@unique([tenant_id, request_id], map: "osi_vehicle_engine_settings_tenant_request_id_key")
  @@unique([tenant_id, series_id, version], map: "osi_vehicle_engine_settings_tenant_series_version_key")
  @@unique([tenant_id, version_hash], map: "osi_vehicle_engine_settings_tenant_version_hash_key")
  @@index([tenant_id, state, valid_from, valid_to], map: "osi_vehicle_engine_settings_tenant_state_validity_idx")
  @@map("osi_vehicle_engine_settings")
}

model PipelineEvent {
  id        String              @id @default(cuid())
  caseId    String
  eventType PipelineEventType
  status    PipelineEventStatus @default(PENDING)
  startAt   DateTime
  code      String?
  createdAt DateTime            @default(now())
  updatedAt DateTime            @default(now()) @updatedAt
  case      PipelineCase        @relation(fields: [caseId], references: [id], onDelete: Cascade)

  @@index([caseId, startAt])
  @@index([eventType, startAt])
  @@index([code])
  @@map("osi_pipeline_events")
}

model PipelineCaseQuote {
  id                          String                       @id @default(cuid())
  publicRef                   String                       @default(dbgenerated("gen_random_uuid()")) @map("public_ref") @db.Uuid
  tenantId                    String                       @map("tenant_id")
  caseId                      String
  costingRevisionId           String?                      @map("costing_revision_id")
  cycleNumber                 Int                          @default(1) @map("cycle_number")
  contractVersion             Int                          @default(1) @map("contract_version")
  level                       PipelineQuoteLevel
  version                     Int                          @default(1)
  status                      String                       @default("DRAFT")
  sentAt                      DateTime?
  validUntil                  DateTime?
  createdAt                   DateTime                     @default(now())
  updatedAt                   DateTime                     @default(now()) @updatedAt
  tenant                      Tenant                       @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "osi_pipeline_case_quotes_tenant_fkey")
  case                        PipelineCase                 @relation(fields: [tenantId, caseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "osi_pipeline_case_quotes_tenant_case_fkey")
  costingRevision             CostingRevision?             @relation(fields: [tenantId, costingRevisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "osi_pipeline_case_quotes_costing_revision_fkey")
  proposals                   QuoteProposal[]
  commands                    QuoteMutationCommand[]
  quote_change_order_subjects quote_change_order_subjects?

  @@unique([tenantId, id], map: "osi_pipeline_case_quotes_tenant_id_key")
  @@unique([tenantId, publicRef], map: "osi_pipeline_case_quotes_tenant_ref_key")
  @@unique([tenantId, caseId, cycleNumber], map: "osi_pipeline_case_quotes_case_cycle_key")
  @@unique([caseId, id], map: "osi_pipeline_case_quotes_case_id_id_key")
  @@index([tenantId, caseId, version])
  @@index([status, updatedAt])
  @@map("osi_pipeline_case_quotes")
}

model PipelineCratingRequest {
  id            String       @id @default(cuid())
  caseId        String       @unique
  code          String?
  status        String       @default("PENDING")
  estimateReady Boolean      @default(false)
  payloadJson   Json?
  createdAt     DateTime     @default(now())
  updatedAt     DateTime     @default(now()) @updatedAt
  case          PipelineCase @relation(fields: [caseId], references: [id], onDelete: Cascade)

  @@index([status, updatedAt])
  @@map("osi_pipeline_crating_requests")
}

model Account {
  id                  String                  @id @default(cuid())
  accountType         AccountType             @map("account_type")
  legalName           String                  @map("legal_name")
  taxId               String?                 @map("tax_id")
  defaultCurrency     String                  @map("default_currency")
  billingPreferences  String?                 @map("billing_preferences")
  paymentInstructions String?                 @map("payment_instructions")
  createdAt           DateTime                @default(now()) @map("created_at")
  updatedAt           DateTime                @default(now()) @updatedAt @map("updated_at")
  accountContacts     AccountContact[]
  pricingProfiles     AccountPricingProfile[]
  serviceCases        ServiceCase[]

  @@map("accounts")
}

model MasterTariff {
  id              String                  @id @default(uuid())
  code            String                  @unique
  name            String
  scope           MasterTariffScope
  currency        String
  isActive        Boolean                 @default(true) @map("is_active")
  notes           String?
  createdAt       DateTime                @default(now()) @map("created_at")
  updatedAt       DateTime                @updatedAt @map("updated_at")
  coverageOptions Json                    @default("[]") @map("coverage_options")
  serviceRules    Json                    @default("[]") @map("service_rules")
  baseProfiles    AccountPricingProfile[] @relation("BaseMasterTariff")
  rateSets        TariffRateSet[]

  @@index([scope, isActive])
  @@index([currency, isActive])
  @@map("master_tariffs")
}

model CodeSequence {
  key       String   @id
  value     Int      @default(0)
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@map("code_sequences")
}

model TariffRateSet {
  id                String           @id @default(uuid())
  masterTariffId    String           @map("master_tariff_id")
  mode              TariffRateMode
  measurementSystem String           @map("measurement_system")
  densityReference  Decimal?         @map("density_reference") @db.Decimal(12, 4)
  minimumCharge     Decimal?         @map("minimum_charge") @db.Decimal(14, 4)
  baseFee           Decimal?         @map("base_fee") @db.Decimal(14, 4)
  meta              Json             @default("{}")
  createdAt         DateTime         @default(now()) @map("created_at")
  updatedAt         DateTime         @updatedAt @map("updated_at")
  bands             TariffRateBand[]
  masterTariff      MasterTariff     @relation(fields: [masterTariffId], references: [id], onDelete: Cascade)

  @@index([masterTariffId, mode])
  @@map("tariff_rate_sets")
}

model TariffRateBand {
  id        String        @id @default(uuid())
  rateSetId String        @map("rate_set_id")
  fromValue Decimal       @map("from_value") @db.Decimal(14, 4)
  toValue   Decimal?      @map("to_value") @db.Decimal(14, 4)
  rate      Decimal       @db.Decimal(14, 4)
  fixedFee  Decimal?      @map("fixed_fee") @db.Decimal(14, 4)
  unit      String
  meta      Json          @default("{}")
  createdAt DateTime      @default(now()) @map("created_at")
  updatedAt DateTime      @updatedAt @map("updated_at")
  rateSet   TariffRateSet @relation(fields: [rateSetId], references: [id], onDelete: Cascade)

  @@index([rateSetId, fromValue])
  @@map("tariff_rate_bands")
}

model SurchargeCatalog {
  id                 String                   @id @default(uuid())
  code               String                   @unique
  name               String
  defaultPricingMode SurchargePricingMode     @map("default_pricing_mode")
  defaultRate        Decimal                  @map("default_rate") @db.Decimal(14, 4)
  currency           String
  appliesTo          Json                     @default("{}") @map("applies_to")
  createdAt          DateTime                 @default(now()) @map("created_at")
  updatedAt          DateTime                 @updatedAt @map("updated_at")
  accountPolicies    AccountSurchargePolicy[]

  @@index([currency])
  @@map("surcharge_catalog")
}

model AccountPricingProfile {
  id                   String                   @id @default(uuid())
  accountId            String                   @map("account_id")
  baseMasterTariffId   String                   @map("base_master_tariff_id")
  currencyOverride     String?                  @map("currency_override")
  validFrom            DateTime?                @map("valid_from")
  validTo              DateTime?                @map("valid_to")
  globalMarkupPct      Decimal?                 @map("global_markup_pct") @db.Decimal(7, 4)
  globalDiscountPct    Decimal?                 @map("global_discount_pct") @db.Decimal(7, 4)
  isActive             Boolean                  @default(true) @map("is_active")
  notes                String?
  airMarkupPct         Decimal?                 @map("air_markup_pct") @db.Decimal(7, 4)
  airMinKg             Decimal?                 @map("air_min_kg") @db.Decimal(14, 4)
  lclMarkupPct         Decimal?                 @map("lcl_markup_pct") @db.Decimal(7, 4)
  lclMinWm             Decimal?                 @map("lcl_min_wm") @db.Decimal(14, 4)
  fclMarkupPct         Decimal?                 @map("fcl_markup_pct") @db.Decimal(7, 4)
  localMarkupPct       Decimal?                 @map("local_markup_pct") @db.Decimal(7, 4)
  localMinHours        Decimal?                 @map("local_min_hours") @db.Decimal(14, 4)
  localBaseZoneKm      Decimal?                 @map("local_base_zone_km") @db.Decimal(14, 4)
  localPerKmRate       Decimal?                 @map("local_per_km_rate") @db.Decimal(14, 4)
  localMinTripFee      Decimal?                 @map("local_min_trip_fee") @db.Decimal(14, 4)
  afterHoursMultiplier Decimal?                 @map("after_hours_multiplier") @db.Decimal(10, 4)
  weekendMultiplier    Decimal?                 @map("weekend_multiplier") @db.Decimal(10, 4)
  holidayMultiplier    Decimal?                 @map("holiday_multiplier") @db.Decimal(10, 4)
  createdAt            DateTime                 @default(now()) @map("created_at")
  updatedAt            DateTime                 @updatedAt @map("updated_at")
  code                 String                   @unique
  serviceType          String?                  @map("service_type")
  serviceMode          String?                  @map("service_mode")
  customerType         String?                  @map("customer_type")
  account              Account                  @relation(fields: [accountId], references: [id], onDelete: Cascade)
  baseMasterTariff     MasterTariff             @relation("BaseMasterTariff", fields: [baseMasterTariffId], references: [id])
  surchargePolicies    AccountSurchargePolicy[]
  overrides            TariffOverride[]

  @@index([accountId, isActive])
  @@index([baseMasterTariffId, isActive])
  @@index([serviceType, serviceMode, customerType, isActive], map: "account_pricing_profiles_service_type_service_mode_customer_typ")
  @@map("account_pricing_profiles")
}

model AccountSurchargePolicy {
  id            String                   @id @default(uuid())
  profileId     String                   @map("profile_id")
  surchargeCode String                   @map("surcharge_code")
  handling      AccountSurchargeHandling
  overrideRate  Decimal?                 @map("override_rate") @db.Decimal(14, 4)
  meta          Json                     @default("{}")
  createdAt     DateTime                 @default(now()) @map("created_at")
  updatedAt     DateTime                 @updatedAt @map("updated_at")
  profile       AccountPricingProfile    @relation(fields: [profileId], references: [id], onDelete: Cascade)
  surcharge     SurchargeCatalog         @relation(fields: [surchargeCode], references: [code])

  @@unique([profileId, surchargeCode])
  @@index([surchargeCode])
  @@map("account_surcharge_policies")
}

model TariffOverride {
  id        String                @id @default(uuid())
  profileId String                @map("profile_id")
  scope     TariffOverrideScope
  mode      TariffRateMode?
  routeKey  String?               @map("route_key")
  key       String
  value     Json
  notes     String?
  createdAt DateTime              @default(now()) @map("created_at")
  updatedAt DateTime              @updatedAt @map("updated_at")
  profile   AccountPricingProfile @relation(fields: [profileId], references: [id], onDelete: Cascade)

  @@index([profileId, scope, mode])
  @@index([routeKey])
  @@map("tariff_overrides")
}

model Contact {
  id                   String            @id @default(cuid())
  fullName             String            @map("full_name")
  phones               Json              @default("[]")
  emails               Json              @default("[]")
  whatsapp             String?
  createdAt            DateTime          @default(now()) @map("created_at")
  updatedAt            DateTime          @default(now()) @updatedAt @map("updated_at")
  accountContacts      AccountContact[]
  assignedEvents       CommercialEvent[] @relation("CommercialEventAssignedTo")
  approverServiceCases ServiceCase[]     @relation("ServiceCaseApproverContact")
  ownedServiceCases    ServiceCase[]     @relation("ServiceCaseOwner")
  payerServiceCases    ServiceCase[]     @relation("ServiceCasePayerContact")
  primaryServiceCases  ServiceCase[]     @relation("ServiceCasePrimaryContact")
  performedSurveys     CaseSurvey[]      @relation("CaseSurveyPerformedBy")

  @@map("contacts")
}

model AccountContact {
  id               String  @id @default(cuid())
  accountId        String  @map("account_id")
  contactId        String  @map("contact_id")
  relationshipRole String  @map("relationship_role")
  account          Account @relation(fields: [accountId], references: [id], onDelete: Cascade)
  contact          Contact @relation(fields: [contactId], references: [id], onDelete: Cascade)

  @@unique([accountId, contactId, relationshipRole])
  @@map("account_contacts")
}

model Location {
  id                      String                 @id @default(cuid())
  label                   String
  addressLine             String                 @map("address_line")
  sector                  String?
  city                    String
  province                String?
  country                 String
  geoLat                  Decimal?               @map("geo_lat") @db.Decimal(10, 7)
  geoLng                  Decimal?               @map("geo_lng") @db.Decimal(10, 7)
  locationType            LocationType           @map("location_type")
  createdAt               DateTime               @default(now()) @map("created_at")
  updatedAt               DateTime               @default(now()) @updatedAt @map("updated_at")
  events                  CommercialEvent[]
  accessProfile           LocationAccessProfile?
  destinationServiceCases ServiceCase[]          @relation("ServiceCaseDestinationLocation")
  originServiceCases      ServiceCase[]          @relation("ServiceCaseOriginLocation")

  @@index([city, sector])
  @@map("locations")
}

model LocationAccessProfile {
  id                String            @id @default(cuid())
  locationId        String            @unique @map("location_id")
  floorNumber       Int?              @map("floor_number")
  elevatorAvailable Boolean           @default(false) @map("elevator_available")
  elevatorNotes     String?           @map("elevator_notes")
  stairsFloors      Int?              @map("stairs_floors")
  parkingDifficulty ParkingDifficulty @default(MEDIUM) @map("parking_difficulty")
  longCarryBand     LongCarryBand     @default(NONE) @map("long_carry_band")
  truckRestrictions String?           @map("truck_restrictions")
  buildingRules     String?           @map("building_rules")
  riskNotes         String?           @map("risk_notes")
  photosAssetIds    Json              @default("[]") @map("photos_asset_ids")
  updatedAt         DateTime          @default(now()) @updatedAt @map("updated_at")
  location          Location          @relation(fields: [locationId], references: [id], onDelete: Cascade)

  @@map("location_access_profiles")
}

model ServiceCase {
  id                      String                  @id @default(cuid())
  caseCode                String                  @unique @map("case_code")
  mode                    ServiceCaseMode
  serviceType             String                  @map("service_type")
  customerType            ServiceCaseCustomerType @map("customer_type")
  status                  ServiceCaseStatus
  ownerContactId          String?                 @map("owner_contact_id")
  accountId               String?                 @map("account_id")
  primaryContactId        String                  @map("primary_contact_id")
  payerContactId          String?                 @map("payer_contact_id")
  approverContactId       String?                 @map("approver_contact_id")
  originLocationId        String                  @map("origin_location_id")
  destinationLocationId   String?                 @map("destination_location_id")
  blocks                  Json                    @default("{}")
  noDestinationCaseType   NoDestinationCaseType?  @map("no_destination_case_type")
  noDestinationAckAssetId String?                 @map("no_destination_ack_asset_id")
  estimatedCbm            Decimal?                @map("estimated_cbm") @db.Decimal(12, 3)
  surveyMethod            SurveyMethod?           @map("survey_method")
  requiresSurvey          Boolean                 @default(false) @map("requires_survey")
  serviceFlags            Json                    @default("[]") @map("service_flags")
  createdAt               DateTime                @default(now()) @map("created_at")
  updatedAt               DateTime                @default(now()) @updatedAt @map("updated_at")
  milestones              CaseMilestone?
  cratingRequests         CratingRequest[]
  events                  CommercialEvent[]
  quotes                  CaseQuote[]
  account                 Account?                @relation(fields: [accountId], references: [id])
  approverContact         Contact?                @relation("ServiceCaseApproverContact", fields: [approverContactId], references: [id])
  destinationLocation     Location?               @relation("ServiceCaseDestinationLocation", fields: [destinationLocationId], references: [id])
  originLocation          Location                @relation("ServiceCaseOriginLocation", fields: [originLocationId], references: [id])
  ownerContact            Contact?                @relation("ServiceCaseOwner", fields: [ownerContactId], references: [id])
  payerContact            Contact?                @relation("ServiceCasePayerContact", fields: [payerContactId], references: [id])
  primaryContact          Contact                 @relation("ServiceCasePrimaryContact", fields: [primaryContactId], references: [id])
  surveys                 CaseSurvey[]

  @@index([mode, status])
  @@index([ownerContactId, status])
  @@index([createdAt])
  @@index([caseCode])
  @@index([serviceFlags], map: "service_cases_service_flags_gin_idx", type: Gin)
  @@map("service_cases")
}

model CaseMilestone {
  id                String      @id @default(cuid())
  caseId            String      @unique @map("case_id")
  firstResponseAt   DateTime?   @map("first_response_at")
  icpCompletedAt    DateTime?   @map("icp_completed_at")
  surveyScheduledAt DateTime?   @map("survey_scheduled_at")
  surveyCompletedAt DateTime?   @map("survey_completed_at")
  estimateSentAt    DateTime?   @map("estimate_sent_at")
  finalQuoteSentAt  DateTime?   @map("final_quote_sent_at")
  approvedAt        DateTime?   @map("approved_at")
  opsHandoffAt      DateTime?   @map("ops_handoff_at")
  closedAt          DateTime?   @map("closed_at")
  closeReason       String?     @map("close_reason")
  serviceCase       ServiceCase @relation(fields: [caseId], references: [id], onDelete: Cascade)

  @@index([firstResponseAt])
  @@index([finalQuoteSentAt])
  @@map("case_milestones")
}

model CaseSurvey {
  id                         String      @id @default(cuid())
  caseId                     String      @map("case_id")
  surveyType                 String      @map("survey_type")
  performedAt                DateTime    @map("performed_at")
  performedByContactId       String?     @map("performed_by_contact_id")
  originAccessConfirmed      Boolean     @default(false) @map("origin_access_confirmed")
  destinationAccessConfirmed Boolean     @default(false) @map("destination_access_confirmed")
  volumeEstimate             String?     @map("volume_estimate")
  inventorySummary           String?     @map("inventory_summary")
  specialHandlingCodes       Json        @default("[]") @map("special_handling_codes")
  surchargesPossibleCodes    Json        @default("[]") @map("surcharges_possible_codes")
  assumptionsCodes           Json        @default("[]") @map("assumptions_codes")
  evidenceAssetIds           Json        @default("[]") @map("evidence_asset_ids")
  notes                      String?
  createdAt                  DateTime    @default(now()) @map("created_at")
  serviceCase                ServiceCase @relation(fields: [caseId], references: [id], onDelete: Cascade)
  performedByContact         Contact?    @relation("CaseSurveyPerformedBy", fields: [performedByContactId], references: [id])

  @@map("surveys")
}

model CaseQuote {
  id                String           @id @default(cuid())
  caseId            String           @map("case_id")
  level             QuoteLevel
  version           Int
  status            QuoteStatus
  currency          String
  paymentTermsText  String?          @map("payment_terms_text")
  assumptionsCodes  Json             @default("[]") @map("assumptions_codes")
  exclusionsText    String?          @map("exclusions_text")
  changeControlText String?          @map("change_control_text")
  sentAt            DateTime?        @map("sent_at")
  validUntil        DateTime?        @map("valid_until")
  createdAt         DateTime         @default(now()) @map("created_at")
  cratingRequests   CratingRequest[]
  lineItems         QuoteLineItem[]
  quote_versions    QuoteVersion[]
  serviceCase       ServiceCase      @relation(fields: [caseId], references: [id], onDelete: Cascade)

  @@unique([caseId, level, version])
  @@index([caseId, status, sentAt])
  @@map("quotes")
}

model QuoteVersion {
  id                        String    @id @default(uuid())
  quoteId                   String    @map("quote_id")
  versionNumber             Int       @map("version_number")
  data                      Json
  hubSnapshot               String?   @map("hub_snapshot")
  vehicleSnapshot           String?   @map("vehicle_snapshot")
  zoneOriginSnapshot        String?   @map("zone_origin_snapshot")
  zoneDestinationSnapshot   String?   @map("zone_destination_snapshot")
  kmRateSnapshot            Float?    @map("km_rate_snapshot")
  freeKmSnapshot            Float?    @map("free_km_snapshot")
  surchargeSnapshot         Float?    @map("surcharge_snapshot")
  slaSnapshot               Int?      @map("sla_snapshot")
  marginSnapshot            Float?    @map("margin_snapshot")
  engineFlagsSnapshot       Json?     @map("engine_flags_snapshot")
  operationalVolumeTotal    Float?    @map("operational_volume_total")
  operationalVolumeSource   String?   @map("operational_volume_source")
  selectedShippingMethod    String?   @map("selected_shipping_method")
  operationalVolumeSnapshot Json?     @map("operational_volume_snapshot")
  createdAt                 DateTime  @default(now()) @map("created_at")
  createdBy                 String    @map("created_by")
  quotes                    CaseQuote @relation(fields: [quoteId], references: [id], onDelete: Cascade)

  @@index([quoteId, versionNumber])
  @@map("quote_versions")
}

model LogisticOverrideApproval {
  id                    String             @id @default(cuid())
  tenant_id             String
  approval_request_id   String
  risk_evaluation_id    String
  blocking_rule_id      String
  entity                String             @db.VarChar(120)
  entity_id             String             @db.VarChar(191)
  caseId                String?            @map("case_id")
  quoteId               String?            @map("quote_id")
  quote_version         Int?
  material_hash         String             @db.Char(64)
  business_reason       String
  scope_json            Json
  scope_hash            String             @db.Char(64)
  original_value_json   Json
  authorized_value_json Json
  valid_from            DateTime
  valid_to              DateTime
  conditions_json       Json               @default("[]")
  evidence_json         Json               @default("[]")
  references_json       Json               @default("[]")
  decision_hash         String?            @db.Char(64)
  created_at            DateTime           @default(now())
  updated_at            DateTime           @default(now())
  approval_requests     ApprovalRequest    @relation(fields: [tenant_id, approval_request_id], references: [tenant_id, id], onUpdate: NoAction, map: "logistic_override_approval_request_fkey")
  risk_engine_rules     risk_engine_rules  @relation(fields: [tenant_id, blocking_rule_id], references: [tenant_id, id], onUpdate: NoAction, map: "logistic_override_blocking_rule_fkey")
  risk_evaluations      risk_evaluations   @relation(fields: [tenant_id, risk_evaluation_id], references: [tenant_id, id], onUpdate: NoAction, map: "logistic_override_risk_evaluation_fkey")
  tenants               Tenant             @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "logistic_override_tenant_fkey")
  quote_change_orders   QuoteChangeOrder[]

  @@unique([tenant_id, approval_request_id], map: "logistic_override_approvals_approval_key")
  @@unique([tenant_id, id], map: "logistic_override_approvals_tenant_id_key")
  @@index([tenant_id, risk_evaluation_id], map: "logistic_override_evaluation_idx")
  @@index([tenant_id, entity, entity_id, created_at(sort: Desc)], map: "logistic_override_tenant_entity_idx")
  @@index([tenant_id, quoteId, quote_version, material_hash], map: "logistic_override_tenant_quote_idx")
  @@index([tenant_id, valid_from, valid_to], map: "logistic_override_validity_idx")
  @@map("logistic_override_approvals")
}

model ApprovalRequest {
  id                                                                                                                           String                       @id @default(uuid())
  tenant_id                                                                                                                    String
  approval_type                                                                                                                String                       @db.VarChar(120)
  entity                                                                                                                       String                       @db.VarChar(120)
  entity_id                                                                                                                    String                       @db.VarChar(191)
  requester_user_id                                                                                                            String
  requester_membership_id                                                                                                      String
  assigned_approver_user_id                                                                                                    String?
  assigned_approver_membership_id                                                                                              String?
  status                                                                                                                       ApprovalRequestStatus        @default(PENDING)
  request_reason                                                                                                               String
  evaluation_snapshot_json                                                                                                     Json
  risk_level                                                                                                                   String?                      @db.VarChar(40)
  risk_evaluation_ref                                                                                                          String?                      @db.VarChar(191)
  risk_result                                                                                                                  String?                      @db.VarChar(40)
  risk_rules_version                                                                                                           String?                      @db.VarChar(120)
  risk_rules_hash                                                                                                              String?                      @db.Char(64)
  risk_factors_json                                                                                                            Json?
  risk_reasons_json                                                                                                            Json?
  requires_logistic_override                                                                                                   Boolean                      @default(false)
  separation_of_duties_required                                                                                                Boolean                      @default(true)
  policy_snapshot_json                                                                                                         Json                         @default("{}")
  requested_at                                                                                                                 DateTime                     @default(now())
  due_at                                                                                                                       DateTime?
  decider_user_id                                                                                                              String?
  decider_membership_id                                                                                                        String?
  decided_at                                                                                                                   DateTime?
  decision_reason                                                                                                              String?
  request_id                                                                                                                   String                       @db.VarChar(191)
  payload_hash                                                                                                                 String                       @db.Char(64)
  decision_request_id                                                                                                          String?                      @db.VarChar(191)
  decision_payload_hash                                                                                                        String?                      @db.Char(64)
  previous_request_id                                                                                                          String?
  version                                                                                                                      Int                          @default(1)
  createdAt                                                                                                                    DateTime                     @default(now()) @map("created_at")
  updated_at                                                                                                                   DateTime                     @default(now())
  tenant_memberships_approval_requests_tenant_id_assigned_approver_membership_id_assigned_approver_user_idTotenant_memberships TenantMembership?            @relation("approval_requests_tenant_id_assigned_approver_membership_id_assigned_approver_user_idTotenant_memberships", fields: [tenant_id, assigned_approver_membership_id, assigned_approver_user_id], references: [tenantId, id, userId], map: "approval_requests_assigned_approver_membership_fkey")
  tenant_memberships_approval_requests_tenant_id_decider_membership_id_decider_user_idTotenant_memberships                     TenantMembership?            @relation("approval_requests_tenant_id_decider_membership_id_decider_user_idTotenant_memberships", fields: [tenant_id, decider_membership_id, decider_user_id], references: [tenantId, id, userId], map: "approval_requests_decider_membership_fkey")
  approval_requests                                                                                                            ApprovalRequest?             @relation("approval_requestsToapproval_requests", fields: [tenant_id, previous_request_id], references: [tenant_id, id], map: "approval_requests_previous_request_fkey")
  other_approval_requests                                                                                                      ApprovalRequest[]            @relation("approval_requestsToapproval_requests")
  tenant_memberships_approval_requests_tenant_id_requester_membership_id_requester_user_idTotenant_memberships                 TenantMembership             @relation("approval_requests_tenant_id_requester_membership_id_requester_user_idTotenant_memberships", fields: [tenant_id, requester_membership_id, requester_user_id], references: [tenantId, id, userId], map: "approval_requests_requester_membership_fkey")
  tenants                                                                                                                      Tenant                       @relation(fields: [tenant_id], references: [id])
  logistic_override_approvals                                                                                                  LogisticOverrideApproval?
  quote_change_orders                                                                                                          QuoteChangeOrder[]
  risk_evaluations                                                                                                             risk_evaluations[]
  employeeProvisioningRequest                                                                                                  EmployeeProvisioningRequest?

  @@unique([tenant_id, id])
  @@unique([tenant_id, request_id], map: "approval_requests_tenant_request_id_key")
  @@index([tenant_id, previous_request_id], map: "approval_requests_previous_request_id_idx")
  @@index([tenant_id, assigned_approver_membership_id, status, due_at], map: "approval_requests_tenant_assignee_status_idx")
  @@index([tenant_id, createdAt(sort: Desc), id(sort: Desc)], map: "approval_requests_tenant_created_at_id_idx")
  @@index([tenant_id, decider_membership_id, decided_at(sort: Desc)], map: "approval_requests_tenant_decider_idx")
  @@index([tenant_id, entity, entity_id, createdAt(sort: Desc)], map: "approval_requests_tenant_entity_idx")
  @@index([tenant_id, requester_membership_id, createdAt(sort: Desc)], map: "approval_requests_tenant_requester_idx")
  @@index([tenant_id, status, due_at], map: "approval_requests_tenant_status_due_at_idx")
  @@index([tenant_id, approval_type, status], map: "approval_requests_tenant_type_status_idx")
  @@map("approval_requests")
}

model QuoteAddendum {
  id                        String    @id @default(uuid())
  quoteId                   String    @map("quote_id")
  baseVersion               Int       @map("base_version")
  addendumNumber            Int       @map("addendum_number")
  description               String
  amountDelta               Float     @map("amount_delta")
  currency                  String
  status                    String    @default("PENDING")
  createdAt                 DateTime  @default(now()) @map("created_at")
  createdBy                 String    @map("created_by")
  baseApprovedAmount        Float?    @map("base_approved_amount")
  capPercent                Float     @default(15) @map("cap_percent")
  financialNature           String    @default("CUSTOMER_REVENUE") @map("financial_nature")
  serviceClassification     String    @default("SCOPE_ADDITION") @map("service_classification")
  invoiceTreatment          String    @default("ADDITIONAL_LINE") @map("invoice_treatment")
  invoiceLineDescription    String?   @map("invoice_line_description")
  billingStatus             String    @default("PENDING_INVOICE") @map("billing_status")
  invoiceReference          String?   @map("invoice_reference")
  acceptanceJson            Json?     @map("acceptance_json")
  evidenceJson              Json?     @map("evidence_json")
  operationalAdjustmentJson Json?     @map("operational_adjustment_json")
  approvedAt                DateTime? @map("approved_at")

  @@index([quoteId, status], map: "quote_addendums_quote_status_idx")
  @@map("quote_addendums")
}

model QuoteChangeOrder {
  id                                                                                                                   String                        @id @default(uuid())
  tenant_id                                                                                                            String
  pipeline_case_id                                                                                                     String
  base_quote_id                                                                                                        String
  base_quote_version                                                                                                   Int
  base_quote_hash                                                                                                      String                        @db.Char(64)
  base_quote_snapshot_json                                                                                             Json
  series_id                                                                                                            String
  code                                                                                                                 String                        @db.VarChar(80)
  sequence_number                                                                                                      BigInt
  version                                                                                                              Int
  previous_version_id                                                                                                  String?
  is_current                                                                                                           Boolean                       @default(true)
  change_type                                                                                                          String                        @db.VarChar(80)
  classification                                                                                                       String                        @db.VarChar(120)
  contract_stage                                                                                                       String                        @db.VarChar(80)
  reason                                                                                                               String
  description                                                                                                          String
  currency                                                                                                             String                        @db.Char(3)
  previous_subtotal                                                                                                    Decimal                       @db.Decimal(18, 2)
  increment_amount                                                                                                     Decimal                       @db.Decimal(18, 2)
  reduction_amount                                                                                                     Decimal                       @db.Decimal(18, 2)
  tax_amount                                                                                                           Decimal                       @db.Decimal(18, 2)
  previous_total                                                                                                       Decimal                       @db.Decimal(18, 2)
  new_total                                                                                                            Decimal                       @db.Decimal(18, 2)
  variation_amount                                                                                                     Decimal                       @db.Decimal(18, 2)
  variation_percent                                                                                                    Decimal                       @db.Decimal(9, 4)
  policy_id                                                                                                            String
  policy_snapshot_json                                                                                                 Json
  cap_amount                                                                                                           Decimal                       @db.Decimal(18, 2)
  cumulative_increase                                                                                                  Decimal                       @db.Decimal(18, 2)
  requires_approval                                                                                                    Boolean                       @default(false)
  approval_reasons_json                                                                                                Json                          @default("[]")
  approval_request_id                                                                                                  String?
  risk_recheck_required                                                                                                Boolean                       @default(false)
  risk_factor_changes_json                                                                                             Json                          @default("[]")
  risk_evaluation_id                                                                                                   String?
  risk_material_hash                                                                                                   String?                       @db.Char(64)
  risk_snapshot_json                                                                                                   Json?
  logistic_override_id                                                                                                 String?
  customer_acceptance_required                                                                                         Boolean                       @default(true)
  customer_decision                                                                                                    String?                       @db.VarChar(20)
  customer_decided_at                                                                                                  DateTime?
  customer_actor_snapshot                                                                                              String?                       @db.VarChar(240)
  customer_acceptance_method                                                                                           String?                       @db.VarChar(80)
  customer_acceptance_hash                                                                                             String?                       @db.Char(64)
  evidence_refs_json                                                                                                   Json                          @default("[]")
  status                                                                                                               QuoteChangeOrderStatus        @default(DRAFT)
  requested_by_user_id                                                                                                 String
  requested_by_membership_id                                                                                           String
  executed_by_user_id                                                                                                  String?
  executed_by_membership_id                                                                                            String?
  submitted_at                                                                                                         DateTime?
  approvedAt                                                                                                           DateTime?                     @map("approved_at")
  sent_to_customer_at                                                                                                  DateTime?
  executed_at                                                                                                          DateTime?
  expires_at                                                                                                           DateTime?
  request_id                                                                                                           String                        @db.VarChar(191)
  payload_hash                                                                                                         String                        @db.Char(64)
  row_version                                                                                                          Int                           @default(1)
  createdAt                                                                                                            DateTime                      @default(now()) @map("created_at")
  updated_at                                                                                                           DateTime                      @default(now())
  quote_change_order_commands                                                                                          quote_change_order_commands[]
  quote_change_order_items                                                                                             quote_change_order_items[]
  approval_requests                                                                                                    ApprovalRequest?              @relation(fields: [tenant_id, approval_request_id], references: [tenant_id, id], onUpdate: NoAction, map: "quote_change_orders_approval_fkey")
  tenant_memberships_quote_change_orders_tenant_id_executed_by_membership_id_executed_by_user_idTotenant_memberships   TenantMembership?             @relation("quote_change_orders_tenant_id_executed_by_membership_id_executed_by_user_idTotenant_memberships", fields: [tenant_id, executed_by_membership_id, executed_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "quote_change_orders_executor_fkey")
  logistic_override_approvals                                                                                          LogisticOverrideApproval?     @relation(fields: [tenant_id, logistic_override_id], references: [tenant_id, id], onUpdate: NoAction, map: "quote_change_orders_logistic_override_fkey")
  quote_change_order_policies                                                                                          quote_change_order_policies   @relation(fields: [tenant_id, policy_id], references: [tenant_id, id], onUpdate: NoAction, map: "quote_change_orders_policy_fkey")
  quote_change_orders                                                                                                  QuoteChangeOrder?             @relation("quote_change_ordersToquote_change_orders", fields: [tenant_id, previous_version_id], references: [tenant_id, id], onUpdate: NoAction, map: "quote_change_orders_previous_version_fkey")
  other_quote_change_orders                                                                                            QuoteChangeOrder[]            @relation("quote_change_ordersToquote_change_orders")
  tenant_memberships_quote_change_orders_tenant_id_requested_by_membership_id_requested_by_user_idTotenant_memberships TenantMembership              @relation("quote_change_orders_tenant_id_requested_by_membership_id_requested_by_user_idTotenant_memberships", fields: [tenant_id, requested_by_membership_id, requested_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "quote_change_orders_requester_fkey")
  risk_evaluations                                                                                                     risk_evaluations?             @relation(fields: [tenant_id, risk_evaluation_id], references: [tenant_id, id], onUpdate: NoAction, map: "quote_change_orders_risk_evaluation_fkey")
  quote_change_order_subjects                                                                                          quote_change_order_subjects   @relation(fields: [tenant_id, pipeline_case_id, base_quote_id], references: [tenant_id, pipeline_case_id, base_quote_id], onUpdate: NoAction, map: "quote_change_orders_subject_fkey")
  tenants                                                                                                              Tenant                        @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "quote_change_orders_tenant_fkey")

  @@unique([tenant_id, code, version], map: "quote_change_orders_tenant_code_version_key")
  @@unique([tenant_id, id], map: "quote_change_orders_tenant_id_key")
  @@unique([tenant_id, request_id], map: "quote_change_orders_tenant_request_key")
  @@unique([tenant_id, sequence_number, version], map: "quote_change_orders_tenant_sequence_version_key")
  @@index([tenant_id, previous_version_id], map: "quote_change_orders_previous_version_idx")
  @@index([tenant_id, pipeline_case_id, createdAt(sort: Desc)], map: "quote_change_orders_tenant_case_idx")
  @@index([tenant_id, base_quote_id, status, createdAt(sort: Desc)], map: "quote_change_orders_tenant_quote_status_idx")
  @@index([tenant_id, requested_by_membership_id, createdAt(sort: Desc)], map: "quote_change_orders_tenant_requester_idx")
  @@index([tenant_id, status, expires_at], map: "quote_change_orders_tenant_status_expiry_idx")
  @@map("quote_change_orders")
}

model CommercialAuditLog {
  id                  String            @id @default(uuid())
  tenant_id           String
  actor_user_id       String?
  actor_membership_id String?
  role_snapshot       String            @db.VarChar(64)
  action              String            @db.VarChar(160)
  entity              String            @db.VarChar(120)
  entityId            String            @map("entity_id") @db.VarChar(191)
  before_json         Json?
  after_json          Json?
  metadata_json       Json?
  source              String            @db.VarChar(80)
  request_id          String?           @db.VarChar(191)
  correlation_id      String            @db.VarChar(191)
  critical            Boolean           @default(false)
  createdAt           DateTime          @default(now()) @map("created_at")
  tenant_memberships  TenantMembership? @relation(fields: [tenant_id, actor_membership_id, actor_user_id], references: [tenantId, id, userId], map: "commercial_audit_logs_actor_membership_tenant_user_fkey")
  osi_users           User?             @relation(fields: [actor_user_id], references: [id], onDelete: Restrict)
  tenants             Tenant            @relation(fields: [tenant_id], references: [id])

  @@index([tenant_id, action, createdAt(sort: Desc)], map: "commercial_audit_logs_tenant_action_created_at_idx")
  @@index([tenant_id, actor_membership_id, createdAt(sort: Desc)], map: "commercial_audit_logs_tenant_actor_membership_created_at_idx")
  @@index([tenant_id, actor_user_id, createdAt(sort: Desc)], map: "commercial_audit_logs_tenant_actor_user_created_at_idx")
  @@index([tenant_id, correlation_id], map: "commercial_audit_logs_tenant_correlation_id_idx")
  @@index([tenant_id, createdAt(sort: Desc), id(sort: Desc)], map: "commercial_audit_logs_tenant_created_at_id_idx")
  @@index([tenant_id, entity, entityId, createdAt(sort: Desc)], map: "commercial_audit_logs_tenant_entity_created_at_idx")
  @@index([tenant_id, source, createdAt(sort: Desc)], map: "commercial_audit_logs_tenant_source_created_at_idx")
  @@map("commercial_audit_logs")
}

model QuoteLineItem {
  id               String        @id @default(cuid())
  quoteId          String        @map("quote_id")
  block            QuoteBlock
  itemType         QuoteItemType @map("item_type")
  catalogCode      String?       @map("catalog_code")
  description      String
  qty              Decimal       @db.Decimal(12, 3)
  unit             String
  unitPrice        Decimal       @map("unit_price") @db.Decimal(14, 2)
  total            Decimal       @db.Decimal(14, 2)
  evidenceRequired Boolean       @default(false) @map("evidence_required")
  notes            String?
  quote            CaseQuote     @relation(fields: [quoteId], references: [id], onDelete: Cascade)

  @@map("quote_line_items")
}

model CommercialEvent {
  id                  String                @id @default(cuid())
  caseId              String                @map("case_id")
  eventType           CommercialEventType   @map("event_type")
  startAt             DateTime              @map("start_at")
  endAt               DateTime              @map("end_at")
  status              CommercialEventStatus @default(SCHEDULED)
  assignedToContactId String?               @map("assigned_to_contact_id")
  locationId          String?               @map("location_id")
  notes               String?
  meta                Json                  @default("{}")
  createdAt           DateTime              @default(now()) @map("created_at")
  updatedAt           DateTime              @default(now()) @updatedAt @map("updated_at")
  assignedToContact   Contact?              @relation("CommercialEventAssignedTo", fields: [assignedToContactId], references: [id])
  serviceCase         ServiceCase           @relation(fields: [caseId], references: [id], onDelete: Cascade)
  location            Location?             @relation(fields: [locationId], references: [id])

  @@index([caseId, eventType, startAt])
  @@map("events")
}

model CratingRequest {
  id                   String               @id @default(cuid())
  caseId               String               @map("case_id")
  quoteId              String?              @map("quote_id")
  status               CratingRequestStatus
  items                Json                 @default("[]")
  outputCostSummary    Json                 @default("{}") @map("output_cost_summary")
  generatedLineItemIds Json                 @default("[]") @map("generated_line_item_ids")
  createdAt            DateTime             @default(now()) @map("created_at")
  updatedAt            DateTime             @default(now()) @updatedAt @map("updated_at")
  serviceCase          ServiceCase          @relation(fields: [caseId], references: [id], onDelete: Cascade)
  quote                CaseQuote?           @relation(fields: [quoteId], references: [id])

  @@map("crating_requests")
}

model OperationalCompensationConfig {
  id        String   @id
  version   Int
  data      Json
  updatedAt DateTime @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by")

  @@map("operational_compensation_configs")
}

model EvaluatorArticleCatalogSnapshot {
  id        String   @id @default(uuid())
  version   Int
  items     Json     @default("[]")
  updatedAt DateTime @updatedAt @map("updated_at")
  updatedBy String?  @map("updated_by")

  @@map("evaluator_article_catalog_snapshots")
}

model EvaluatorVisitReport {
  id              String   @id @default(uuid())
  externalVisitId String   @unique @map("external_visit_id")
  caseId          String   @map("case_id")
  caseCode        String   @map("case_code")
  status          String   @default("SUBMITTED")
  payloadHash     String   @map("payload_hash")
  payload         Json
  itemCount       Int      @default(0) @map("item_count")
  pieceCount      Int      @default(0) @map("piece_count")
  photoCount      Int      @default(0) @map("photo_count")
  revision        Int      @default(1)
  receivedAt      DateTime @default(now()) @map("received_at")
  confirmedAt     DateTime @default(now()) @map("confirmed_at")
  submittedBy     String?  @map("submitted_by")
  createdAt       DateTime @default(now()) @map("created_at")
  updatedAt       DateTime @updatedAt @map("updated_at")

  @@index([caseId, confirmedAt])
  @@index([status, confirmedAt])
  @@map("evaluator_visit_reports")
}

model GlobalCommercialSettings {
  id                      String    @id @default(uuid())
  version                 Int
  isActive                Boolean   @default(false) @map("is_active")
  isScheduled             Boolean   @default(false) @map("is_scheduled")
  scheduledActivationDate DateTime? @map("scheduled_activation_date") @db.Date
  activatedAt             DateTime? @map("activated_at")
  activatedBy             String?   @map("activated_by")
  createdBy               String    @map("created_by")
  kmMetro                 Int       @map("km_metro")
  kmInterior              Int       @map("km_interior")
  tarifaBase              Decimal   @map("tarifa_base") @db.Decimal(10, 2)
  visitFeeMinimo          Decimal   @map("visit_fee_minimo") @db.Decimal(10, 2)
  margenMinimo            Decimal   @map("margen_minimo") @db.Decimal(5, 2)
  hubPrincipalId          String    @map("hub_principal_id")
  createdAt               DateTime  @default(now()) @map("created_at")
  transportMinimo         Decimal   @default(0) @map("transport_minimo") @db.Decimal(10, 2)
  hubPrincipal            Hub       @relation(fields: [hubPrincipalId], references: [id])

  @@index([version])
  @@index([isActive])
  @@index([isScheduled])
  @@map("global_commercial_settings")
}

model CatalogSurcharge {
  id          String   @id @default(cuid())
  code        String   @unique
  name        String
  description String?
  defaultUnit String?  @map("default_unit")
  active      Boolean  @default(true)
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @default(now()) @updatedAt @map("updated_at")

  @@map("catalog_surcharges")
}

model CatalogSpecialHandling {
  id          String   @id @default(cuid())
  code        String   @unique
  name        String
  description String?
  active      Boolean  @default(true)
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @default(now()) @updatedAt @map("updated_at")

  @@map("catalog_special_handling")
}

model MaterialUnit {
  id                   String                    @id @default(cuid())
  unitRef              String                    @default(dbgenerated("gen_random_uuid()")) @map("unit_ref") @db.Uuid
  tenantId             String                    @map("tenant_id")
  code                 String                    @db.VarChar(24)
  name                 String                    @db.VarChar(80)
  decimalPlaces        Int                       @default(3) @map("decimal_places")
  status               MaterialRecordStatus      @default(ACTIVE)
  version              Int                       @default(1)
  createdAt            DateTime                  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt            DateTime                  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant               Tenant                    @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_units_tenant_fkey")
  baseMaterials        MaterialCatalogItem[]     @relation("MaterialBaseUnit")
  purchaseMaterials    MaterialCatalogItem[]     @relation("MaterialPurchaseUnit")
  consumptionMaterials MaterialCatalogItem[]     @relation("MaterialConsumptionUnit")
  conversionSources    MaterialUnitConversion[]  @relation("MaterialConversionSource")
  conversionTargets    MaterialUnitConversion[]  @relation("MaterialConversionTarget")
  costVersions         MaterialCostVersion[]
  recipeLines          PackingRecipeLine[]
  requirementLines     MaterialRequirementLine[]
  purchaseRequests     MaterialPurchaseRequest[]

  @@unique([tenantId, id], map: "material_units_tenant_id_key")
  @@unique([tenantId, unitRef], map: "material_units_tenant_ref_key")
  @@unique([tenantId, code], map: "material_units_tenant_code_key")
  @@index([tenantId, status, code], map: "material_units_status_idx")
  @@map("material_units")
}

model MaterialCatalogItem {
  id                 String                      @id @default(cuid())
  materialRef        String                      @default(dbgenerated("gen_random_uuid()")) @map("material_ref") @db.Uuid
  tenantId           String                      @map("tenant_id")
  code               String                      @db.VarChar(64)
  name               String                      @db.VarChar(160)
  description        String?                     @db.VarChar(1000)
  family             String                      @db.VarChar(80)
  subfamily          String?                     @db.VarChar(80)
  baseUnitId         String                      @map("base_unit_id")
  purchaseUnitId     String                      @map("purchase_unit_id")
  consumptionUnitId  String                      @map("consumption_unit_id")
  technicalFlags     String[]                    @default([]) @map("technical_flags")
  dimensionPolicy    Json                        @default("{}") @map("dimension_policy")
  lotTrackingEnabled Boolean                     @default(false) @map("lot_tracking_enabled")
  minimumStock       Decimal?                    @map("minimum_stock") @db.Decimal(18, 6)
  maximumStock       Decimal?                    @map("maximum_stock") @db.Decimal(18, 6)
  reorderPoint       Decimal?                    @map("reorder_point") @db.Decimal(18, 6)
  sortOrder          Int                         @default(0) @map("sort_order")
  status             MaterialRecordStatus        @default(ACTIVE)
  version            Int                         @default(1)
  createdAt          DateTime                    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime                    @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant             Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_catalog_items_tenant_fkey")
  baseUnit           MaterialUnit                @relation("MaterialBaseUnit", fields: [tenantId, baseUnitId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_catalog_items_base_unit_fkey")
  purchaseUnit       MaterialUnit                @relation("MaterialPurchaseUnit", fields: [tenantId, purchaseUnitId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_catalog_items_purchase_unit_fkey")
  consumptionUnit    MaterialUnit                @relation("MaterialConsumptionUnit", fields: [tenantId, consumptionUnitId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_catalog_items_consumption_unit_fkey")
  conversions        MaterialUnitConversion[]
  costs              MaterialCostVersion[]
  suppliers          MaterialSupplierReference[]
  movements          MaterialInventoryMovement[]
  reservations       MaterialReservation[]
  recipeLines        PackingRecipeLine[]
  requirementLines   MaterialRequirementLine[]
  purchaseRequests   MaterialPurchaseRequest[]

  @@unique([tenantId, id], map: "material_catalog_items_tenant_id_key")
  @@unique([tenantId, materialRef], map: "material_catalog_items_tenant_ref_key")
  @@unique([tenantId, code], map: "material_catalog_items_tenant_code_key")
  @@index([tenantId, status, family, sortOrder], map: "material_catalog_items_catalog_idx")
  @@map("material_catalog_items")
}

model MaterialUnitConversion {
  id            String               @id @default(cuid())
  conversionRef String               @default(dbgenerated("gen_random_uuid()")) @map("conversion_ref") @db.Uuid
  tenantId      String               @map("tenant_id")
  materialId    String               @map("material_id")
  sourceUnitId  String               @map("source_unit_id")
  targetUnitId  String               @map("target_unit_id")
  multiplier    Decimal              @db.Decimal(24, 9)
  validFrom     DateTime             @map("valid_from") @db.Timestamptz(6)
  validTo       DateTime?            @map("valid_to") @db.Timestamptz(6)
  version       Int
  status        MaterialRecordStatus @default(ACTIVE)
  createdAt     DateTime             @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant        Tenant               @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_unit_conversions_tenant_fkey")
  material      MaterialCatalogItem  @relation(fields: [tenantId, materialId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_unit_conversions_material_fkey")
  sourceUnit    MaterialUnit         @relation("MaterialConversionSource", fields: [tenantId, sourceUnitId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_unit_conversions_source_unit_fkey")
  targetUnit    MaterialUnit         @relation("MaterialConversionTarget", fields: [tenantId, targetUnitId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_unit_conversions_target_unit_fkey")

  @@unique([tenantId, conversionRef], map: "material_unit_conversions_tenant_ref_key")
  @@unique([tenantId, materialId, sourceUnitId, targetUnitId, version], map: "material_unit_conversions_version_key")
  @@index([tenantId, materialId, status, validFrom(sort: Desc)], map: "material_unit_conversions_current_idx")
  @@map("material_unit_conversions")
}

model MaterialCostVersion {
  id                    String                     @id @default(cuid())
  costVersionRef        String                     @default(dbgenerated("gen_random_uuid()")) @map("cost_version_ref") @db.Uuid
  tenantId              String                     @map("tenant_id")
  materialId            String                     @map("material_id")
  unitId                String                     @map("unit_id")
  amount                Decimal                    @db.Decimal(18, 6)
  currency              String                     @db.Char(3)
  source                String                     @db.VarChar(80)
  supplierReferenceId   String?                    @map("supplier_reference_id")
  validFrom             DateTime                   @map("valid_from") @db.Timestamptz(6)
  validTo               DateTime?                  @map("valid_to") @db.Timestamptz(6)
  version               Int
  createdByMembershipId String                     @map("created_by_membership_id")
  createdByUserId       String                     @map("created_by_user_id")
  createdAt             DateTime                   @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                Tenant                     @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_cost_versions_tenant_fkey")
  material              MaterialCatalogItem        @relation(fields: [tenantId, materialId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_cost_versions_material_fkey")
  unit                  MaterialUnit               @relation(fields: [tenantId, unitId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_cost_versions_unit_fkey")
  supplierReference     MaterialSupplierReference? @relation(fields: [tenantId, supplierReferenceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_cost_versions_supplier_fkey")
  createdByMembership   TenantMembership           @relation("MaterialCostVersionActor", fields: [tenantId, createdByMembershipId, createdByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "material_cost_versions_actor_fkey")

  @@unique([tenantId, id], map: "material_cost_versions_tenant_id_key")
  @@unique([tenantId, costVersionRef], map: "material_cost_versions_tenant_ref_key")
  @@unique([tenantId, materialId, version], map: "material_cost_versions_version_key")
  @@index([tenantId, materialId, validFrom(sort: Desc)], map: "material_cost_versions_current_idx")
  @@map("material_cost_versions")
}

model MaterialSupplierReference {
  id           String                @id @default(cuid())
  supplierRef  String                @default(dbgenerated("gen_random_uuid()")) @map("supplier_ref") @db.Uuid
  tenantId     String                @map("tenant_id")
  materialId   String                @map("material_id")
  supplierCode String                @map("supplier_code") @db.VarChar(64)
  supplierName String                @map("supplier_name") @db.VarChar(160)
  supplierSku  String?               @map("supplier_sku") @db.VarChar(100)
  status       MaterialRecordStatus  @default(ACTIVE)
  createdAt    DateTime              @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt    DateTime              @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant       Tenant                @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_supplier_references_tenant_fkey")
  material     MaterialCatalogItem   @relation(fields: [tenantId, materialId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_supplier_references_material_fkey")
  costVersions MaterialCostVersion[]

  @@unique([tenantId, id], map: "material_supplier_references_tenant_id_key")
  @@unique([tenantId, supplierRef], map: "material_supplier_references_tenant_ref_key")
  @@unique([tenantId, materialId, supplierCode], map: "material_supplier_references_code_key")
  @@map("material_supplier_references")
}

model MaterialWarehouse {
  id           String               @id @default(cuid())
  warehouseRef String               @default(dbgenerated("gen_random_uuid()")) @map("warehouse_ref") @db.Uuid
  tenantId     String               @map("tenant_id")
  code         String               @db.VarChar(64)
  name         String               @db.VarChar(160)
  status       MaterialRecordStatus @default(ACTIVE)
  version      Int                  @default(1)
  createdAt    DateTime             @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt    DateTime             @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant       Tenant               @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_warehouses_tenant_fkey")
  locations    MaterialLocation[]

  @@unique([tenantId, id], map: "material_warehouses_tenant_id_key")
  @@unique([tenantId, warehouseRef], map: "material_warehouses_tenant_ref_key")
  @@unique([tenantId, code], map: "material_warehouses_tenant_code_key")
  @@map("material_warehouses")
}

model MaterialLocation {
  id                     String                      @id @default(cuid())
  locationRef            String                      @default(dbgenerated("gen_random_uuid()")) @map("location_ref") @db.Uuid
  tenantId               String                      @map("tenant_id")
  warehouseId            String                      @map("warehouse_id")
  parentLocationId       String?                     @map("parent_location_id")
  code                   String                      @db.VarChar(64)
  name                   String                      @db.VarChar(160)
  kind                   MaterialLocationKind
  depth                  Int                         @default(0)
  path                   String                      @db.VarChar(600)
  status                 MaterialRecordStatus        @default(ACTIVE)
  version                Int                         @default(1)
  createdAt              DateTime                    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt              DateTime                    @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant                 Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_locations_tenant_fkey")
  warehouse              MaterialWarehouse           @relation(fields: [tenantId, warehouseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_locations_warehouse_fkey")
  parent                 MaterialLocation?           @relation("MaterialLocationTree", fields: [tenantId, parentLocationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_locations_parent_fkey")
  children               MaterialLocation[]          @relation("MaterialLocationTree")
  movements              MaterialInventoryMovement[]
  reservations           MaterialReservation[]
  currentAssets          AssetInstance[]             @relation("AssetCurrentLocation")
  assignmentOrigins      AssetAssignment[]           @relation("AssetAssignmentOrigin")
  assignmentDestinations AssetAssignment[]           @relation("AssetAssignmentDestination")
  inspectionLocations    AssetInspection[]           @relation("AssetInspectionLocation")

  @@unique([tenantId, id], map: "material_locations_tenant_id_key")
  @@unique([tenantId, locationRef], map: "material_locations_tenant_ref_key")
  @@unique([tenantId, warehouseId, code], map: "material_locations_warehouse_code_key")
  @@index([tenantId, warehouseId, parentLocationId, status], map: "material_locations_tree_idx")
  @@map("material_locations")
}

model MaterialInventoryCommand {
  id                String                      @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String                      @map("tenant_id")
  requestId         String                      @map("request_id") @db.VarChar(191)
  operation         String                      @db.VarChar(80)
  payloadHash       String                      @map("payload_hash") @db.Char(64)
  targetRef         String                      @map("target_ref") @db.VarChar(191)
  resultJson        Json                        @map("result_json")
  actorMembershipId String                      @map("actor_membership_id")
  actorUserId       String                      @map("actor_user_id")
  createdAt         DateTime                    @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_inventory_commands_tenant_fkey")
  actorMembership   TenantMembership            @relation("MaterialInventoryCommandActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "material_inventory_commands_actor_fkey")
  movements         MaterialInventoryMovement[]

  @@unique([tenantId, id], map: "material_inventory_commands_tenant_id_key")
  @@unique([tenantId, requestId], map: "material_inventory_commands_request_key")
  @@index([tenantId, targetRef, createdAt(sort: Desc)], map: "material_inventory_commands_target_idx")
  @@map("material_inventory_commands")
}

model MaterialInventoryMovement {
  id                String                   @id @default(cuid())
  movementRef       String                   @default(dbgenerated("gen_random_uuid()")) @map("movement_ref") @db.Uuid
  transactionRef    String                   @map("transaction_ref") @db.Uuid
  tenantId          String                   @map("tenant_id")
  materialId        String                   @map("material_id")
  locationId        String                   @map("location_id")
  commandId         String                   @map("command_id") @db.Uuid
  reservationId     String?                  @map("reservation_id")
  requirementLineId String?                  @map("requirement_line_id")
  purchaseRequestId String?                  @map("purchase_request_id")
  movementType      MaterialMovementType     @map("movement_type")
  quantityBase      Decimal                  @map("quantity_base") @db.Decimal(18, 6)
  lotCode           String?                  @map("lot_code") @db.VarChar(100)
  reasonCode        String                   @map("reason_code") @db.VarChar(80)
  actorMembershipId String                   @map("actor_membership_id")
  actorUserId       String                   @map("actor_user_id")
  occurredAt        DateTime                 @default(now()) @map("occurred_at") @db.Timestamptz(6)
  tenant            Tenant                   @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_inventory_movements_tenant_fkey")
  material          MaterialCatalogItem      @relation(fields: [tenantId, materialId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_inventory_movements_material_fkey")
  location          MaterialLocation         @relation(fields: [tenantId, locationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_inventory_movements_location_fkey")
  command           MaterialInventoryCommand @relation(fields: [tenantId, commandId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_inventory_movements_command_fkey")
  reservation       MaterialReservation?     @relation(fields: [tenantId, reservationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_inventory_movements_reservation_fkey")
  requirementLine   MaterialRequirementLine? @relation(fields: [tenantId, requirementLineId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_inventory_movements_requirement_fkey")
  purchaseRequest   MaterialPurchaseRequest? @relation(fields: [tenantId, purchaseRequestId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_inventory_movements_purchase_request_fkey")
  actorMembership   TenantMembership         @relation("MaterialInventoryMovementActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "material_inventory_movements_actor_fkey")

  @@unique([tenantId, id], map: "material_inventory_movements_tenant_id_key")
  @@unique([tenantId, movementRef], map: "material_inventory_movements_tenant_ref_key")
  @@index([tenantId, materialId, locationId, occurredAt], map: "material_inventory_movements_stock_idx")
  @@index([tenantId, transactionRef, movementType], map: "material_inventory_movements_transaction_idx")
  @@map("material_inventory_movements")
}

model MaterialReservation {
  id                String                      @id @default(cuid())
  reservationRef    String                      @default(dbgenerated("gen_random_uuid()")) @map("reservation_ref") @db.Uuid
  tenantId          String                      @map("tenant_id")
  materialId        String                      @map("material_id")
  locationId        String                      @map("location_id")
  pipelineCaseId    String?                     @map("pipeline_case_id")
  requirementLineId String?                     @map("requirement_line_id")
  cratingReference  String?                     @map("crating_reference") @db.VarChar(191)
  quantityBase      Decimal                     @map("quantity_base") @db.Decimal(18, 6)
  status            MaterialReservationStatus   @default(RESERVED)
  version           Int                         @default(1)
  createdAt         DateTime                    @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt         DateTime                    @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant            Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_reservations_tenant_fkey")
  material          MaterialCatalogItem         @relation(fields: [tenantId, materialId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_reservations_material_fkey")
  location          MaterialLocation            @relation(fields: [tenantId, locationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_reservations_location_fkey")
  pipelineCase      PipelineCase?               @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_reservations_case_fkey")
  requirementLine   MaterialRequirementLine?    @relation(fields: [tenantId, requirementLineId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_reservations_requirement_fkey")
  events            MaterialReservationEvent[]
  movements         MaterialInventoryMovement[]

  @@unique([tenantId, id], map: "material_reservations_tenant_id_key")
  @@unique([tenantId, reservationRef], map: "material_reservations_tenant_ref_key")
  @@index([tenantId, materialId, locationId, status], map: "material_reservations_availability_idx")
  @@index([tenantId, pipelineCaseId, status], map: "material_reservations_case_idx")
  @@map("material_reservations")
}

model MaterialReservationEvent {
  id                String                     @id @default(cuid())
  eventRef          String                     @default(dbgenerated("gen_random_uuid()")) @map("event_ref") @db.Uuid
  tenantId          String                     @map("tenant_id")
  reservationId     String                     @map("reservation_id")
  fromStatus        MaterialReservationStatus? @map("from_status")
  toStatus          MaterialReservationStatus  @map("to_status")
  quantityBase      Decimal                    @map("quantity_base") @db.Decimal(18, 6)
  reasonCode        String                     @map("reason_code") @db.VarChar(80)
  actorMembershipId String                     @map("actor_membership_id")
  actorUserId       String                     @map("actor_user_id")
  createdAt         DateTime                   @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant                     @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_reservation_events_tenant_fkey")
  reservation       MaterialReservation        @relation(fields: [tenantId, reservationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_reservation_events_reservation_fkey")
  actorMembership   TenantMembership           @relation("MaterialReservationEventActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "material_reservation_events_actor_fkey")

  @@unique([tenantId, eventRef], map: "material_reservation_events_tenant_ref_key")
  @@index([tenantId, reservationId, createdAt], map: "material_reservation_events_history_idx")
  @@map("material_reservation_events")
}

model PackingRecipe {
  id        String                 @id @default(cuid())
  recipeRef String                 @default(dbgenerated("gen_random_uuid()")) @map("recipe_ref") @db.Uuid
  tenantId  String                 @map("tenant_id")
  code      String                 @db.VarChar(64)
  name      String                 @db.VarChar(160)
  status    MaterialRecordStatus   @default(ACTIVE)
  createdAt DateTime               @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime               @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant                 @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "packing_recipes_tenant_fkey")
  versions  PackingRecipeVersion[]

  @@unique([tenantId, id], map: "packing_recipes_tenant_id_key")
  @@unique([tenantId, recipeRef], map: "packing_recipes_tenant_ref_key")
  @@unique([tenantId, code], map: "packing_recipes_tenant_code_key")
  @@map("packing_recipes")
}

model PackingRecipeVersion {
  id                    String                        @id @default(cuid())
  recipeVersionRef      String                        @default(dbgenerated("gen_random_uuid()")) @map("recipe_version_ref") @db.Uuid
  tenantId              String                        @map("tenant_id")
  recipeId              String                        @map("recipe_id")
  version               Int
  status                PackingRecipeVersionStatus    @default(DRAFT)
  applicability         Json
  applicabilitySha256   String                        @map("applicability_sha256") @db.Char(64)
  createdByMembershipId String                        @map("created_by_membership_id")
  createdByUserId       String                        @map("created_by_user_id")
  activatedAt           DateTime?                     @map("activated_at") @db.Timestamptz(6)
  createdAt             DateTime                      @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                Tenant                        @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "packing_recipe_versions_tenant_fkey")
  recipe                PackingRecipe                 @relation(fields: [tenantId, recipeId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "packing_recipe_versions_recipe_fkey")
  createdByMembership   TenantMembership              @relation("PackingRecipeVersionActor", fields: [tenantId, createdByMembershipId, createdByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "packing_recipe_versions_actor_fkey")
  lines                 PackingRecipeLine[]
  requirementSnapshots  MaterialRequirementSnapshot[]

  @@unique([tenantId, id], map: "packing_recipe_versions_tenant_id_key")
  @@unique([tenantId, recipeVersionRef], map: "packing_recipe_versions_tenant_ref_key")
  @@unique([tenantId, recipeId, version], map: "packing_recipe_versions_version_key")
  @@index([tenantId, status, activatedAt(sort: Desc)], map: "packing_recipe_versions_active_idx")
  @@map("packing_recipe_versions")
}

model PackingRecipeLine {
  id                String               @id @default(cuid())
  tenantId          String               @map("tenant_id")
  recipeVersionId   String               @map("recipe_version_id")
  position          Int
  materialId        String               @map("material_id")
  unitId            String               @map("unit_id")
  formulaType       PackingFormulaType   @map("formula_type")
  fixedQuantity     Decimal?             @map("fixed_quantity") @db.Decimal(18, 6)
  multiplier        Decimal?             @db.Decimal(18, 6)
  roundingIncrement Decimal              @default(1) @map("rounding_increment") @db.Decimal(18, 6)
  wastePercent      Decimal              @default(0) @map("waste_percent") @db.Decimal(7, 4)
  formulaConfig     Json                 @default("{}") @map("formula_config")
  tenant            Tenant               @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "packing_recipe_lines_tenant_fkey")
  recipeVersion     PackingRecipeVersion @relation(fields: [tenantId, recipeVersionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "packing_recipe_lines_version_fkey")
  material          MaterialCatalogItem  @relation(fields: [tenantId, materialId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "packing_recipe_lines_material_fkey")
  unit              MaterialUnit         @relation(fields: [tenantId, unitId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "packing_recipe_lines_unit_fkey")

  @@unique([tenantId, recipeVersionId, position], map: "packing_recipe_lines_position_key")
  @@index([tenantId, materialId], map: "packing_recipe_lines_material_idx")
  @@map("packing_recipe_lines")
}

model MaterialRequirementSnapshot {
  id                    String                            @id @default(cuid())
  requirementRef        String                            @default(dbgenerated("gen_random_uuid()")) @map("requirement_ref") @db.Uuid
  tenantId              String                            @map("tenant_id")
  surveyPublicationId   String                            @map("survey_publication_id")
  pipelineCaseId        String                            @map("pipeline_case_id")
  recipeVersionId       String                            @map("recipe_version_id")
  revision              Int
  status                MaterialRequirementSnapshotStatus @default(CURRENT)
  contextSnapshot       Json                              @map("context_snapshot")
  logicalSha256         String                            @map("logical_sha256") @db.Char(64)
  createdByMembershipId String                            @map("created_by_membership_id")
  createdByUserId       String                            @map("created_by_user_id")
  createdAt             DateTime                          @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                Tenant                            @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_requirement_snapshots_tenant_fkey")
  surveyPublication     SurveyPublication                 @relation(fields: [tenantId, surveyPublicationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_requirement_snapshots_publication_fkey")
  pipelineCase          PipelineCase                      @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_requirement_snapshots_case_fkey")
  recipeVersion         PackingRecipeVersion              @relation(fields: [tenantId, recipeVersionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_requirement_snapshots_recipe_fkey")
  createdByMembership   TenantMembership                  @relation("MaterialRequirementSnapshotActor", fields: [tenantId, createdByMembershipId, createdByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "material_requirement_snapshots_actor_fkey")
  lines                 MaterialRequirementLine[]

  @@unique([tenantId, id], map: "material_requirement_snapshots_tenant_id_key")
  @@unique([tenantId, requirementRef], map: "material_requirement_snapshots_tenant_ref_key")
  @@unique([tenantId, surveyPublicationId, revision], map: "material_requirement_snapshots_publication_revision_key")
  @@index([tenantId, pipelineCaseId, status, createdAt(sort: Desc)], map: "material_requirement_snapshots_case_idx")
  @@map("material_requirement_snapshots")
}

model MaterialRequirementLine {
  id                    String                      @id @default(cuid())
  tenantId              String                      @map("tenant_id")
  requirementSnapshotId String                      @map("requirement_snapshot_id")
  position              Int
  materialId            String                      @map("material_id")
  unitId                String                      @map("unit_id")
  requiredQuantity      Decimal                     @map("required_quantity") @db.Decimal(18, 6)
  formulaSnapshot       Json                        @map("formula_snapshot")
  sourceSnapshot        Json                        @map("source_snapshot")
  tenant                Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_requirement_lines_tenant_fkey")
  requirementSnapshot   MaterialRequirementSnapshot @relation(fields: [tenantId, requirementSnapshotId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_requirement_lines_snapshot_fkey")
  material              MaterialCatalogItem         @relation(fields: [tenantId, materialId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_requirement_lines_material_fkey")
  unit                  MaterialUnit                @relation(fields: [tenantId, unitId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_requirement_lines_unit_fkey")
  reservations          MaterialReservation[]
  movements             MaterialInventoryMovement[]
  purchaseRequests      MaterialPurchaseRequest[]

  @@unique([tenantId, id], map: "material_requirement_lines_tenant_id_key")
  @@unique([tenantId, requirementSnapshotId, position], map: "material_requirement_lines_position_key")
  @@index([tenantId, materialId], map: "material_requirement_lines_material_idx")
  @@map("material_requirement_lines")
}

model MaterialPurchaseRequest {
  id                      String                        @id @default(cuid())
  purchaseRequestRef      String                        @default(dbgenerated("gen_random_uuid()")) @map("purchase_request_ref") @db.Uuid
  tenantId                String                        @map("tenant_id")
  materialId              String                        @map("material_id")
  unitId                  String                        @map("unit_id")
  requirementLineId       String?                       @map("requirement_line_id")
  requestedQuantity       Decimal                       @map("requested_quantity") @db.Decimal(18, 6)
  status                  MaterialPurchaseRequestStatus @default(REQUESTED)
  version                 Int                           @default(1)
  requestedByMembershipId String                        @map("requested_by_membership_id")
  requestedByUserId       String                        @map("requested_by_user_id")
  createdAt               DateTime                      @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt               DateTime                      @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant                  Tenant                        @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "material_purchase_requests_tenant_fkey")
  material                MaterialCatalogItem           @relation(fields: [tenantId, materialId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_purchase_requests_material_fkey")
  unit                    MaterialUnit                  @relation(fields: [tenantId, unitId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_purchase_requests_unit_fkey")
  requirementLine         MaterialRequirementLine?      @relation(fields: [tenantId, requirementLineId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "material_purchase_requests_requirement_fkey")
  requestedByMembership   TenantMembership              @relation("MaterialPurchaseRequestActor", fields: [tenantId, requestedByMembershipId, requestedByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "material_purchase_requests_actor_fkey")
  movements               MaterialInventoryMovement[]

  @@unique([tenantId, id], map: "material_purchase_requests_tenant_id_key")
  @@unique([tenantId, purchaseRequestRef], map: "material_purchase_requests_tenant_ref_key")
  @@index([tenantId, status, createdAt], map: "material_purchase_requests_status_idx")
  @@map("material_purchase_requests")
}

model AssetModel {
  id                   String                  @id @default(cuid())
  modelRef             String                  @default(dbgenerated("gen_random_uuid()")) @map("model_ref") @db.Uuid
  tenantId             String                  @map("tenant_id")
  code                 String                  @db.VarChar(64)
  name                 String                  @db.VarChar(160)
  description          String?                 @db.VarChar(1000)
  family               String                  @db.VarChar(80)
  resourceType         AssetResourceType       @map("resource_type")
  serialPolicy         AssetSerialPolicy       @default(OPTIONAL) @map("serial_policy")
  identificationPolicy Json                    @default("{}") @map("identification_policy")
  capacity             Json                    @default("{}")
  status               AssetRecordStatus       @default(ACTIVE)
  version              Int                     @default(1)
  createdAt            DateTime                @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt            DateTime                @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant               Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_models_tenant_fkey")
  instances            AssetInstance[]
  costVersions         AssetCostVersion[]
  maintenanceRules     AssetMaintenanceRule[]
  externalOffers       ExternalResourceOffer[]

  @@unique([tenantId, id], map: "asset_models_tenant_id_key")
  @@unique([tenantId, modelRef], map: "asset_models_tenant_ref_key")
  @@unique([tenantId, code], map: "asset_models_tenant_code_key")
  @@index([tenantId, status, resourceType, family], map: "asset_models_catalog_idx")
  @@map("asset_models")
}

model AssetCodeCounter {
  id        String   @id @default(cuid())
  tenantId  String   @map("tenant_id")
  prefix    String   @db.VarChar(24)
  nextValue BigInt   @default(1) @map("next_value")
  updatedAt DateTime @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_code_counters_tenant_fkey")

  @@unique([tenantId, prefix], map: "asset_code_counters_tenant_prefix_key")
  @@map("asset_code_counters")
}

model AssetInstance {
  id                String                  @id @default(cuid())
  assetRef          String                  @default(dbgenerated("gen_random_uuid()")) @map("asset_ref") @db.Uuid
  tenantId          String                  @map("tenant_id")
  assetModelId      String                  @map("asset_model_id")
  currentLocationId String?                 @map("current_location_id")
  internalCode      String                  @map("internal_code") @db.VarChar(64)
  serialNumber      String?                 @map("serial_number") @db.VarChar(160)
  barcode           String?                 @db.VarChar(160)
  operationalStatus AssetOperationalStatus  @default(AVAILABLE) @map("operational_status")
  physicalCondition AssetPhysicalCondition  @default(GOOD) @map("physical_condition")
  acquiredAt        DateTime?               @map("acquired_at") @db.Date
  acquisitionCost   Decimal?                @map("acquisition_cost") @db.Decimal(18, 4)
  replacementCost   Decimal?                @map("replacement_cost") @db.Decimal(18, 4)
  currency          String?                 @db.Char(3)
  usageHours        Decimal?                @map("usage_hours") @db.Decimal(14, 2)
  usageCycles       BigInt?                 @map("usage_cycles")
  odometerKm        Decimal?                @map("odometer_km") @db.Decimal(14, 2)
  version           Int                     @default(1)
  retiredAt         DateTime?               @map("retired_at") @db.Timestamptz(6)
  createdAt         DateTime                @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt         DateTime                @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant            Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_instances_tenant_fkey")
  assetModel        AssetModel              @relation(fields: [tenantId, assetModelId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_instances_model_fkey")
  currentLocation   MaterialLocation?       @relation("AssetCurrentLocation", fields: [tenantId, currentLocationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_instances_location_fkey")
  costVersions      AssetCostVersion[]
  reservations      AssetReservation[]
  assignments       AssetAssignment[]
  inspections       AssetInspection[]
  incidents         AssetIncident[]
  maintenanceOrders AssetMaintenanceOrder[]
  historyEvents     AssetHistoryEvent[]

  @@unique([tenantId, id], map: "asset_instances_tenant_id_key")
  @@unique([tenantId, assetRef], map: "asset_instances_tenant_ref_key")
  @@unique([tenantId, internalCode], map: "asset_instances_tenant_code_key")
  @@unique([tenantId, barcode], map: "asset_instances_tenant_barcode_key")
  @@unique([tenantId, assetModelId, serialNumber], map: "asset_instances_tenant_model_serial_key")
  @@index([tenantId, operationalStatus, physicalCondition, currentLocationId], map: "asset_instances_availability_idx")
  @@index([tenantId, assetModelId, operationalStatus], map: "asset_instances_model_status_idx")
  @@map("asset_instances")
}

model AssetCostVersion {
  id                    String         @id @default(cuid())
  costRef               String         @default(dbgenerated("gen_random_uuid()")) @map("cost_ref") @db.Uuid
  tenantId              String         @map("tenant_id")
  assetModelId          String?        @map("asset_model_id")
  assetInstanceId       String?        @map("asset_instance_id")
  costType              AssetCostType  @map("cost_type")
  amount                Decimal        @db.Decimal(18, 4)
  currency              String         @db.Char(3)
  temporalUnit          String?        @map("temporal_unit") @db.VarChar(24)
  validFrom             DateTime       @map("valid_from") @db.Timestamptz(6)
  validTo               DateTime?      @map("valid_to") @db.Timestamptz(6)
  version               Int
  source                String         @db.VarChar(80)
  createdByMembershipId String         @map("created_by_membership_id")
  createdByUserId       String         @map("created_by_user_id")
  createdAt             DateTime       @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                Tenant         @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_cost_versions_tenant_fkey")
  assetModel            AssetModel?    @relation(fields: [tenantId, assetModelId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_cost_versions_model_fkey")
  assetInstance         AssetInstance? @relation(fields: [tenantId, assetInstanceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_cost_versions_instance_fkey")

  @@unique([tenantId, costRef], map: "asset_cost_versions_tenant_ref_key")
  @@unique([tenantId, assetModelId, assetInstanceId, costType, version], map: "asset_cost_versions_scope_version_key")
  @@index([tenantId, assetModelId, costType, validFrom(sort: Desc)], map: "asset_cost_versions_model_idx")
  @@index([tenantId, assetInstanceId, costType, validFrom(sort: Desc)], map: "asset_cost_versions_instance_idx")
  @@map("asset_cost_versions")
}

model AssetMutationCommand {
  id                String           @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId          String           @map("tenant_id")
  requestId         String           @map("request_id") @db.VarChar(191)
  operation         String           @db.VarChar(80)
  payloadHash       String           @map("payload_hash") @db.Char(64)
  targetRef         String           @map("target_ref") @db.VarChar(191)
  resultJson        Json             @map("result_json")
  actorMembershipId String           @map("actor_membership_id")
  actorUserId       String           @map("actor_user_id")
  createdAt         DateTime         @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant           @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_mutation_commands_tenant_fkey")
  actorMembership   TenantMembership @relation("AssetMutationCommandActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "asset_mutation_commands_actor_fkey")

  @@unique([tenantId, requestId], map: "asset_mutation_commands_tenant_request_key")
  @@index([tenantId, targetRef, createdAt], map: "asset_mutation_commands_target_idx")
  @@map("asset_mutation_commands")
}

model AssetReservation {
  id                   String                 @id @default(cuid())
  reservationRef       String                 @default(dbgenerated("gen_random_uuid()")) @map("reservation_ref") @db.Uuid
  tenantId             String                 @map("tenant_id")
  assetInstanceId      String                 @map("asset_instance_id")
  pipelineCaseId       String?                @map("pipeline_case_id")
  operationalReference String?                @map("operational_reference") @db.VarChar(191)
  startsAt             DateTime               @map("starts_at") @db.Timestamptz(6)
  endsAt               DateTime               @map("ends_at") @db.Timestamptz(6)
  status               AssetReservationStatus @default(ACTIVE)
  version              Int                    @default(1)
  actorMembershipId    String                 @map("actor_membership_id")
  actorUserId          String                 @map("actor_user_id")
  createdAt            DateTime               @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt            DateTime               @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant               Tenant                 @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_reservations_tenant_fkey")
  assetInstance        AssetInstance          @relation(fields: [tenantId, assetInstanceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_reservations_instance_fkey")
  pipelineCase         PipelineCase?          @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_reservations_case_fkey")
  actorMembership      TenantMembership       @relation("AssetReservationActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "asset_reservations_actor_fkey")
  assignments          AssetAssignment[]

  @@unique([tenantId, id], map: "asset_reservations_tenant_id_key")
  @@unique([tenantId, reservationRef], map: "asset_reservations_tenant_ref_key")
  @@index([tenantId, assetInstanceId, status, startsAt, endsAt], map: "asset_reservations_interval_idx")
  @@index([tenantId, pipelineCaseId, status], map: "asset_reservations_case_idx")
  @@map("asset_reservations")
}

model AssetAssignment {
  id                    String                  @id @default(cuid())
  assignmentRef         String                  @default(dbgenerated("gen_random_uuid()")) @map("assignment_ref") @db.Uuid
  tenantId              String                  @map("tenant_id")
  assetInstanceId       String                  @map("asset_instance_id")
  reservationId         String?                 @map("reservation_id")
  pipelineCaseId        String?                 @map("pipeline_case_id")
  assigneeMembershipId  String?                 @map("assignee_membership_id")
  assigneeUserId        String?                 @map("assignee_user_id")
  custodianMembershipId String?                 @map("custodian_membership_id")
  custodianUserId       String?                 @map("custodian_user_id")
  operationalReference  String?                 @map("operational_reference") @db.VarChar(191)
  status                AssetAssignmentStatus   @default(PLANNED)
  originLocationId      String?                 @map("origin_location_id")
  destinationLocationId String?                 @map("destination_location_id")
  handoutCondition      AssetPhysicalCondition? @map("handout_condition")
  returnCondition       AssetPhysicalCondition? @map("return_condition")
  handedOutAt           DateTime?               @map("handed_out_at") @db.Timestamptz(6)
  returnedAt            DateTime?               @map("returned_at") @db.Timestamptz(6)
  version               Int                     @default(1)
  actorMembershipId     String                  @map("actor_membership_id")
  actorUserId           String                  @map("actor_user_id")
  createdAt             DateTime                @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt             DateTime                @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant                Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_assignments_tenant_fkey")
  assetInstance         AssetInstance           @relation(fields: [tenantId, assetInstanceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_assignments_instance_fkey")
  reservation           AssetReservation?       @relation(fields: [tenantId, reservationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_assignments_reservation_fkey")
  pipelineCase          PipelineCase?           @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_assignments_case_fkey")
  assigneeMembership    TenantMembership?       @relation("AssetAssignmentAssignee", fields: [tenantId, assigneeMembershipId, assigneeUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "asset_assignments_assignee_fkey")
  custodianMembership   TenantMembership?       @relation("AssetAssignmentCustodian", fields: [tenantId, custodianMembershipId, custodianUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "asset_assignments_custodian_fkey")
  actorMembership       TenantMembership        @relation("AssetAssignmentActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "asset_assignments_actor_fkey")
  originLocation        MaterialLocation?       @relation("AssetAssignmentOrigin", fields: [tenantId, originLocationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_assignments_origin_fkey")
  destinationLocation   MaterialLocation?       @relation("AssetAssignmentDestination", fields: [tenantId, destinationLocationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_assignments_destination_fkey")
  inspections           AssetInspection[]
  incidents             AssetIncident[]

  @@unique([tenantId, id], map: "asset_assignments_tenant_id_key")
  @@unique([tenantId, assignmentRef], map: "asset_assignments_tenant_ref_key")
  @@index([tenantId, assetInstanceId, status], map: "asset_assignments_instance_status_idx")
  @@index([tenantId, custodianMembershipId, custodianUserId, status], map: "asset_assignments_custodian_idx")
  @@map("asset_assignments")
}

model AssetInspection {
  id                String                 @id @default(cuid())
  inspectionRef     String                 @default(dbgenerated("gen_random_uuid()")) @map("inspection_ref") @db.Uuid
  tenantId          String                 @map("tenant_id")
  assetInstanceId   String                 @map("asset_instance_id")
  assignmentId      String?                @map("assignment_id")
  locationId        String?                @map("location_id")
  inspectionType    AssetInspectionType    @map("inspection_type")
  physicalCondition AssetPhysicalCondition @map("physical_condition")
  safeToUse         Boolean                @map("safe_to_use")
  notes             String?                @db.VarChar(2000)
  evidenceRefs      String[]               @default([]) @map("evidence_refs") @db.Uuid
  actorMembershipId String                 @map("actor_membership_id")
  actorUserId       String                 @map("actor_user_id")
  inspectedAt       DateTime               @default(now()) @map("inspected_at") @db.Timestamptz(6)
  tenant            Tenant                 @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_inspections_tenant_fkey")
  assetInstance     AssetInstance          @relation(fields: [tenantId, assetInstanceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_inspections_instance_fkey")
  assignment        AssetAssignment?       @relation(fields: [tenantId, assignmentId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_inspections_assignment_fkey")
  location          MaterialLocation?      @relation("AssetInspectionLocation", fields: [tenantId, locationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_inspections_location_fkey")
  actorMembership   TenantMembership       @relation("AssetInspectionActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "asset_inspections_actor_fkey")
  incidents         AssetIncident[]

  @@unique([tenantId, id], map: "asset_inspections_tenant_id_key")
  @@unique([tenantId, inspectionRef], map: "asset_inspections_tenant_ref_key")
  @@index([tenantId, assetInstanceId, inspectedAt(sort: Desc)], map: "asset_inspections_history_idx")
  @@map("asset_inspections")
}

model AssetIncident {
  id                 String                  @id @default(cuid())
  incidentRef        String                  @default(dbgenerated("gen_random_uuid()")) @map("incident_ref") @db.Uuid
  tenantId           String                  @map("tenant_id")
  assetInstanceId    String                  @map("asset_instance_id")
  assignmentId       String?                 @map("assignment_id")
  inspectionId       String?                 @map("inspection_id")
  incidentType       AssetIncidentType       @map("incident_type")
  severity           AssetIncidentSeverity
  status             AssetIncidentStatus     @default(OPEN)
  resultingCondition AssetPhysicalCondition? @map("resulting_condition")
  description        String                  @db.VarChar(2000)
  evidenceRefs       String[]                @default([]) @map("evidence_refs") @db.Uuid
  actorMembershipId  String                  @map("actor_membership_id")
  actorUserId        String                  @map("actor_user_id")
  occurredAt         DateTime                @map("occurred_at") @db.Timestamptz(6)
  resolvedAt         DateTime?               @map("resolved_at") @db.Timestamptz(6)
  version            Int                     @default(1)
  createdAt          DateTime                @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime                @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant             Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_incidents_tenant_fkey")
  assetInstance      AssetInstance           @relation(fields: [tenantId, assetInstanceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_incidents_instance_fkey")
  assignment         AssetAssignment?        @relation(fields: [tenantId, assignmentId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_incidents_assignment_fkey")
  inspection         AssetInspection?        @relation(fields: [tenantId, inspectionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_incidents_inspection_fkey")
  actorMembership    TenantMembership        @relation("AssetIncidentActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "asset_incidents_actor_fkey")
  maintenanceOrders  AssetMaintenanceOrder[]

  @@unique([tenantId, id], map: "asset_incidents_tenant_id_key")
  @@unique([tenantId, incidentRef], map: "asset_incidents_tenant_ref_key")
  @@index([tenantId, assetInstanceId, status, occurredAt(sort: Desc)], map: "asset_incidents_asset_idx")
  @@map("asset_incidents")
}

model AssetMaintenanceRule {
  id                 String                  @id @default(cuid())
  ruleRef            String                  @default(dbgenerated("gen_random_uuid()")) @map("rule_ref") @db.Uuid
  tenantId           String                  @map("tenant_id")
  assetModelId       String                  @map("asset_model_id")
  ruleType           AssetMaintenanceTrigger @map("rule_type")
  intervalDays       Int?                    @map("interval_days")
  intervalUsageHours Decimal?                @map("interval_usage_hours") @db.Decimal(14, 2)
  intervalOdometerKm Decimal?                @map("interval_odometer_km") @db.Decimal(14, 2)
  intervalCycles     BigInt?                 @map("interval_cycles")
  status             AssetRecordStatus       @default(ACTIVE)
  version            Int                     @default(1)
  createdAt          DateTime                @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime                @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant             Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_maintenance_rules_tenant_fkey")
  assetModel         AssetModel              @relation(fields: [tenantId, assetModelId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_maintenance_rules_model_fkey")

  @@unique([tenantId, ruleRef], map: "asset_maintenance_rules_tenant_ref_key")
  @@index([tenantId, assetModelId, status], map: "asset_maintenance_rules_model_idx")
  @@map("asset_maintenance_rules")
}

model AssetMaintenanceOrder {
  id                String                 @id @default(cuid())
  maintenanceRef    String                 @default(dbgenerated("gen_random_uuid()")) @map("maintenance_ref") @db.Uuid
  tenantId          String                 @map("tenant_id")
  assetInstanceId   String                 @map("asset_instance_id")
  incidentId        String?                @map("incident_id")
  maintenanceType   AssetMaintenanceType   @map("maintenance_type")
  status            AssetMaintenanceStatus @default(SCHEDULED)
  title             String                 @db.VarChar(240)
  notes             String?                @db.VarChar(2000)
  scheduledStart    DateTime               @map("scheduled_start") @db.Timestamptz(6)
  scheduledEnd      DateTime               @map("scheduled_end") @db.Timestamptz(6)
  startedAt         DateTime?              @map("started_at") @db.Timestamptz(6)
  completedAt       DateTime?              @map("completed_at") @db.Timestamptz(6)
  costAmount        Decimal?               @map("cost_amount") @db.Decimal(18, 4)
  currency          String?                @db.Char(3)
  version           Int                    @default(1)
  actorMembershipId String                 @map("actor_membership_id")
  actorUserId       String                 @map("actor_user_id")
  createdAt         DateTime               @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt         DateTime               @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant            Tenant                 @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_maintenance_orders_tenant_fkey")
  assetInstance     AssetInstance          @relation(fields: [tenantId, assetInstanceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_maintenance_orders_instance_fkey")
  incident          AssetIncident?         @relation(fields: [tenantId, incidentId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_maintenance_orders_incident_fkey")
  actorMembership   TenantMembership       @relation("AssetMaintenanceOrderActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "asset_maintenance_orders_actor_fkey")

  @@unique([tenantId, id], map: "asset_maintenance_orders_tenant_id_key")
  @@unique([tenantId, maintenanceRef], map: "asset_maintenance_orders_tenant_ref_key")
  @@index([tenantId, assetInstanceId, status, scheduledStart, scheduledEnd], map: "asset_maintenance_orders_interval_idx")
  @@map("asset_maintenance_orders")
}

model AssetHistoryEvent {
  id                String           @id @default(cuid())
  eventRef          String           @default(dbgenerated("gen_random_uuid()")) @map("event_ref") @db.Uuid
  tenantId          String           @map("tenant_id")
  assetInstanceId   String           @map("asset_instance_id")
  eventType         String           @map("event_type") @db.VarChar(80)
  previousState     Json?            @map("previous_state")
  resultingState    Json             @map("resulting_state")
  referenceType     String?          @map("reference_type") @db.VarChar(80)
  referenceRef      String?          @map("reference_ref") @db.Uuid
  actorMembershipId String           @map("actor_membership_id")
  actorUserId       String           @map("actor_user_id")
  occurredAt        DateTime         @default(now()) @map("occurred_at") @db.Timestamptz(6)
  tenant            Tenant           @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "asset_history_events_tenant_fkey")
  assetInstance     AssetInstance    @relation(fields: [tenantId, assetInstanceId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "asset_history_events_instance_fkey")
  actorMembership   TenantMembership @relation("AssetHistoryEventActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "asset_history_events_actor_fkey")

  @@unique([tenantId, eventRef], map: "asset_history_events_tenant_ref_key")
  @@index([tenantId, assetInstanceId, occurredAt(sort: Desc)], map: "asset_history_events_asset_idx")
  @@map("asset_history_events")
}

model ExternalResourceOffer {
  id                   String                        @id @default(cuid())
  offerRef             String                        @default(dbgenerated("gen_random_uuid()")) @map("offer_ref") @db.Uuid
  tenantId             String                        @map("tenant_id")
  assetModelId         String?                       @map("asset_model_id")
  providerReference    String                        @map("provider_reference") @db.VarChar(191)
  providerNameSnapshot String                        @map("provider_name_snapshot") @db.VarChar(160)
  resourceDescription  String                        @map("resource_description") @db.VarChar(500)
  capacity             Json                          @default("{}")
  rateAmount           Decimal?                      @map("rate_amount") @db.Decimal(18, 4)
  currency             String?                       @db.Char(3)
  temporalUnit         String?                       @map("temporal_unit") @db.VarChar(24)
  validFrom            DateTime?                     @map("valid_from") @db.Timestamptz(6)
  validTo              DateTime?                     @map("valid_to") @db.Timestamptz(6)
  availabilityStatus   ExternalAvailabilityStatus    @default(UNCONFIRMED) @map("availability_status")
  termsSnapshot        Json                          @default("{}") @map("terms_snapshot")
  contractualReference String?                       @map("contractual_reference") @db.VarChar(191)
  status               AssetRecordStatus             @default(ACTIVE)
  version              Int                           @default(1)
  createdAt            DateTime                      @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt            DateTime                      @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant               Tenant                        @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "external_resource_offers_tenant_fkey")
  assetModel           AssetModel?                   @relation(fields: [tenantId, assetModelId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "external_resource_offers_model_fkey")
  reservations         ExternalResourceReservation[]

  @@unique([tenantId, id], map: "external_resource_offers_tenant_id_key")
  @@unique([tenantId, offerRef], map: "external_resource_offers_tenant_ref_key")
  @@unique([tenantId, providerReference, contractualReference], map: "external_resource_offers_contract_key")
  @@index([tenantId, status, availabilityStatus, validFrom, validTo], map: "external_resource_offers_availability_idx")
  @@map("external_resource_offers")
}

model ExternalResourceReservation {
  id                   String                            @id @default(cuid())
  reservationRef       String                            @default(dbgenerated("gen_random_uuid()")) @map("reservation_ref") @db.Uuid
  tenantId             String                            @map("tenant_id")
  offerId              String                            @map("offer_id")
  pipelineCaseId       String?                           @map("pipeline_case_id")
  startsAt             DateTime                          @map("starts_at") @db.Timestamptz(6)
  endsAt               DateTime                          @map("ends_at") @db.Timestamptz(6)
  quantity             Int
  agreedAmount         Decimal?                          @map("agreed_amount") @db.Decimal(18, 4)
  currency             String?                           @db.Char(3)
  status               ExternalResourceReservationStatus @default(REQUESTED)
  operationalReference String?                           @map("operational_reference") @db.VarChar(191)
  version              Int                               @default(1)
  actorMembershipId    String                            @map("actor_membership_id")
  actorUserId          String                            @map("actor_user_id")
  createdAt            DateTime                          @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt            DateTime                          @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant               Tenant                            @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "external_resource_reservations_tenant_fkey")
  offer                ExternalResourceOffer             @relation(fields: [tenantId, offerId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "external_resource_reservations_offer_fkey")
  pipelineCase         PipelineCase?                     @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "external_resource_reservations_case_fkey")
  actorMembership      TenantMembership                  @relation("ExternalResourceReservationActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "external_resource_reservations_actor_fkey")

  @@unique([tenantId, reservationRef], map: "external_resource_reservations_tenant_ref_key")
  @@index([tenantId, offerId, status, startsAt, endsAt], map: "external_resource_reservations_interval_idx")
  @@map("external_resource_reservations")
}

model LogisticsRule {
  id                String              @id @default(cuid())
  ruleRef           String              @default(dbgenerated("gen_random_uuid()")) @map("rule_ref") @db.Uuid
  seriesRef         String              @map("series_ref") @db.Uuid
  tenantId          String              @map("tenant_id")
  family            LogisticsRuleFamily
  code              String              @db.VarChar(80)
  name              String              @db.VarChar(180)
  priority          Int                 @default(100)
  specificity       Int                 @default(0)
  conditions        Json
  conditionHash     String              @map("condition_hash") @db.Char(64)
  result            Json
  state             LogisticsRuleState  @default(DRAFT)
  version           Int
  validFrom         DateTime?           @map("valid_from") @db.Timestamptz(6)
  validTo           DateTime?           @map("valid_to") @db.Timestamptz(6)
  replacesRuleId    String?             @map("replaces_rule_id")
  requestId         String              @map("request_id") @db.VarChar(191)
  payloadHash       String              @map("payload_hash") @db.Char(64)
  actorMembershipId String              @map("actor_membership_id")
  actorUserId       String              @map("actor_user_id")
  createdAt         DateTime            @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant              @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_rules_tenant_fkey")
  actorMembership   TenantMembership    @relation("LogisticsRuleActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "logistics_rules_actor_fkey")
  replacesRule      LogisticsRule?      @relation("LogisticsRuleReplacement", fields: [tenantId, replacesRuleId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_rules_replaces_fkey")
  replacement       LogisticsRule?      @relation("LogisticsRuleReplacement")

  @@unique([tenantId, id], map: "logistics_rules_tenant_id_key")
  @@unique([tenantId, ruleRef], map: "logistics_rules_tenant_ref_key")
  @@unique([tenantId, seriesRef, version], map: "logistics_rules_series_version_key")
  @@unique([tenantId, replacesRuleId], map: "logistics_rules_replaces_key")
  @@unique([tenantId, requestId], map: "logistics_rules_request_key")
  @@index([tenantId, family, state, priority, specificity, validFrom, validTo], map: "logistics_rules_resolve_idx")
  @@map("logistics_rules")
}

model LogisticsCalculation {
  id                     String                     @id @default(cuid())
  calculationRef         String                     @default(dbgenerated("gen_random_uuid()")) @map("calculation_ref") @db.Uuid
  tenantId               String                     @map("tenant_id")
  pipelineCaseId         String                     @map("pipeline_case_id")
  routeVersion           Int                        @map("route_version")
  serviceSelectionRef    String                     @map("service_selection_ref") @db.Uuid
  serviceRevision        Int                        @map("service_revision")
  surveyPublicationRef   String?                    @map("survey_publication_ref") @db.Uuid
  surveyLogicalSha256    String?                    @map("survey_logical_sha256") @db.Char(64)
  materialRequirementRef String?                    @map("material_requirement_ref") @db.Uuid
  materialLogicalSha256  String?                    @map("material_logical_sha256") @db.Char(64)
  availabilityObservedAt DateTime                   @map("availability_observed_at") @db.Timestamptz(6)
  intervalStart          DateTime                   @map("interval_start") @db.Timestamptz(6)
  intervalEnd            DateTime                   @map("interval_end") @db.Timestamptz(6)
  inputSnapshot          Json                       @map("input_snapshot")
  rulesSnapshot          Json                       @map("rules_snapshot")
  resultSnapshot         Json                       @map("result_snapshot")
  inputHash              String                     @map("input_hash") @db.Char(64)
  resultHash             String                     @map("result_hash") @db.Char(64)
  status                 LogisticsCalculationStatus @default(VALID)
  requestId              String                     @map("request_id") @db.VarChar(191)
  payloadHash            String                     @map("payload_hash") @db.Char(64)
  actorMembershipId      String                     @map("actor_membership_id")
  actorUserId            String                     @map("actor_user_id")
  createdAt              DateTime                   @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                 Tenant                     @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_calculations_tenant_fkey")
  pipelineCase           PipelineCase               @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_calculations_case_fkey")
  actorMembership        TenantMembership           @relation("LogisticsCalculationActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "logistics_calculations_actor_fkey")
  publishedRevision      LogisticsPlanRevision?

  @@unique([tenantId, id], map: "logistics_calculations_tenant_id_key")
  @@unique([tenantId, calculationRef], map: "logistics_calculations_tenant_ref_key")
  @@unique([tenantId, requestId], map: "logistics_calculations_request_key")
  @@index([tenantId, pipelineCaseId, createdAt(sort: Desc)], map: "logistics_calculations_case_idx")
  @@map("logistics_calculations")
}

model LogisticsPlan {
  id             String                  @id @default(cuid())
  planRef        String                  @default(dbgenerated("gen_random_uuid()")) @map("plan_ref") @db.Uuid
  tenantId       String                  @map("tenant_id")
  pipelineCaseId String                  @map("pipeline_case_id")
  status         LogisticsPlanStatus     @default(ACTIVE)
  createdAt      DateTime                @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant         Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_plans_tenant_fkey")
  pipelineCase   PipelineCase            @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_plans_case_fkey")
  revisions      LogisticsPlanRevision[]

  @@unique([tenantId, id], map: "logistics_plans_tenant_id_key")
  @@unique([tenantId, planRef], map: "logistics_plans_tenant_ref_key")
  @@unique([tenantId, pipelineCaseId], map: "logistics_plans_case_key")
  @@map("logistics_plans")
}

model LogisticsPlanRevision {
  id                      String                      @id @default(cuid())
  revisionRef             String                      @default(dbgenerated("gen_random_uuid()")) @map("revision_ref") @db.Uuid
  tenantId                String                      @map("tenant_id")
  planId                  String                      @map("plan_id")
  calculationId           String                      @unique @map("calculation_id")
  revision                Int
  status                  LogisticsPlanRevisionStatus @default(PUBLISHED)
  inputSnapshot           Json                        @map("input_snapshot")
  rulesSnapshot           Json                        @map("rules_snapshot")
  resultSnapshot          Json                        @map("result_snapshot")
  logicalSha256           String                      @map("logical_sha256") @db.Char(64)
  publishedByMembershipId String                      @map("published_by_membership_id")
  publishedByUserId       String                      @map("published_by_user_id")
  publishedAt             DateTime                    @default(now()) @map("published_at") @db.Timestamptz(6)
  tenant                  Tenant                      @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_revisions_tenant_fkey")
  plan                    LogisticsPlan               @relation(fields: [tenantId, planId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_revisions_plan_fkey")
  calculation             LogisticsCalculation        @relation(fields: [tenantId, calculationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_revisions_calculation_fkey")
  publishedByMembership   TenantMembership            @relation("LogisticsRevisionActor", fields: [tenantId, publishedByMembershipId, publishedByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "logistics_revisions_actor_fkey")
  items                   LogisticsPlanItem[]
  issues                  LogisticsPlanIssue[]
  overrides               LogisticsPlanOverride[]
  costingCalculations     CostingCalculation[]
  costingRevisions        CostingRevision[]

  @@unique([tenantId, id], map: "logistics_revisions_tenant_id_key")
  @@unique([tenantId, revisionRef], map: "logistics_revisions_tenant_ref_key")
  @@unique([tenantId, planId, revision], map: "logistics_revisions_plan_revision_key")
  @@unique([tenantId, calculationId], map: "logistics_revisions_calculation_tenant_key")
  @@index([tenantId, planId, status, revision(sort: Desc)], map: "logistics_revisions_current_idx")
  @@map("logistics_plan_revisions")
}

model LogisticsPlanItem {
  id                String                       @id @default(cuid())
  itemRef           String                       @default(dbgenerated("gen_random_uuid()")) @map("item_ref") @db.Uuid
  tenantId          String                       @map("tenant_id")
  revisionId        String                       @map("revision_id")
  family            LogisticsPlanItemFamily
  kind              String                       @db.VarChar(80)
  label             String                       @db.VarChar(240)
  quantity          Decimal?                     @db.Decimal(18, 4)
  unit              String?                      @db.VarChar(32)
  estimatedHours    Decimal?                     @map("estimated_hours") @db.Decimal(12, 3)
  trips             Int?
  requiredQuantity  Decimal?                     @map("required_quantity") @db.Decimal(18, 4)
  availableQuantity Decimal?                     @map("available_quantity") @db.Decimal(18, 4)
  reservedQuantity  Decimal?                     @map("reserved_quantity") @db.Decimal(18, 4)
  shortageQuantity  Decimal?                     @map("shortage_quantity") @db.Decimal(18, 4)
  availability      LogisticsAvailabilityStatus?
  priceStatus       LogisticsPriceStatus?        @map("price_status")
  source            LogisticsSource
  sourceRef         String?                      @map("source_ref") @db.Uuid
  sourceVersion     Int?                         @map("source_version")
  snapshot          Json
  position          Int
  tenant            Tenant                       @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_items_tenant_fkey")
  revision          LogisticsPlanRevision        @relation(fields: [tenantId, revisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_items_revision_fkey")
  overrides         LogisticsPlanOverride[]

  @@unique([tenantId, id], map: "logistics_items_tenant_id_key")
  @@unique([tenantId, itemRef], map: "logistics_items_tenant_ref_key")
  @@unique([tenantId, revisionId, position], map: "logistics_items_position_key")
  @@index([tenantId, revisionId, family], map: "logistics_items_family_idx")
  @@map("logistics_plan_items")
}

model LogisticsPlanIssue {
  id                     String                   @id @default(cuid())
  issueRef               String                   @default(dbgenerated("gen_random_uuid()")) @map("issue_ref") @db.Uuid
  tenantId               String                   @map("tenant_id")
  revisionId             String                   @map("revision_id")
  code                   String                   @db.VarChar(80)
  severity               LogisticsIssueSeverity
  family                 LogisticsPlanItemFamily?
  message                String                   @db.VarChar(500)
  source                 LogisticsSource
  sourceSnapshot         Json                     @map("source_snapshot")
  status                 LogisticsIssueStatus     @default(OPEN)
  resolvedReason         String?                  @map("resolved_reason") @db.VarChar(1000)
  resolvedByMembershipId String?                  @map("resolved_by_membership_id")
  resolvedByUserId       String?                  @map("resolved_by_user_id")
  resolvedAt             DateTime?                @map("resolved_at") @db.Timestamptz(6)
  version                Int                      @default(1)
  tenant                 Tenant                   @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_issues_tenant_fkey")
  revision               LogisticsPlanRevision    @relation(fields: [tenantId, revisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_issues_revision_fkey")
  resolvedByMembership   TenantMembership?        @relation("LogisticsIssueResolver", fields: [tenantId, resolvedByMembershipId, resolvedByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "logistics_issues_resolver_fkey")

  @@unique([tenantId, issueRef], map: "logistics_issues_tenant_ref_key")
  @@index([tenantId, revisionId, severity, status], map: "logistics_issues_revision_idx")
  @@map("logistics_plan_issues")
}

model LogisticsPlanOverride {
  id                String                @id @default(cuid())
  overrideRef       String                @default(dbgenerated("gen_random_uuid()")) @map("override_ref") @db.Uuid
  tenantId          String                @map("tenant_id")
  revisionId        String                @map("revision_id")
  itemId            String                @map("item_id")
  suggestedValue    Json                  @map("suggested_value")
  finalValue        Json                  @map("final_value")
  reason            String                @db.VarChar(1000)
  actorMembershipId String                @map("actor_membership_id")
  actorUserId       String                @map("actor_user_id")
  createdAt         DateTime              @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant                @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_overrides_tenant_fkey")
  revision          LogisticsPlanRevision @relation(fields: [tenantId, revisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_overrides_revision_fkey")
  item              LogisticsPlanItem     @relation(fields: [tenantId, itemId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_overrides_item_fkey")
  actorMembership   TenantMembership      @relation("LogisticsOverrideActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "logistics_overrides_actor_fkey")

  @@unique([tenantId, overrideRef], map: "logistics_overrides_tenant_ref_key")
  @@index([tenantId, revisionId, itemId, createdAt(sort: Desc)], map: "logistics_overrides_item_idx")
  @@map("logistics_plan_overrides")
}

model LogisticsMutationCommand {
  id                String           @id @default(cuid())
  tenantId          String           @map("tenant_id")
  requestId         String           @map("request_id") @db.VarChar(191)
  operation         String           @db.VarChar(80)
  payloadHash       String           @map("payload_hash") @db.Char(64)
  targetRef         String           @map("target_ref") @db.VarChar(191)
  resultJson        Json             @map("result_json")
  actorMembershipId String           @map("actor_membership_id")
  actorUserId       String           @map("actor_user_id")
  createdAt         DateTime         @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant           @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "logistics_commands_tenant_fkey")
  actorMembership   TenantMembership @relation("LogisticsCommandActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "logistics_commands_actor_fkey")

  @@unique([tenantId, requestId], map: "logistics_commands_tenant_request_key")
  @@index([tenantId, targetRef, createdAt(sort: Desc)], map: "logistics_commands_target_idx")
  @@map("logistics_mutation_commands")
}

model CostingRule {
  id                   String               @id @default(cuid())
  ruleRef              String               @default(dbgenerated("gen_random_uuid()")) @map("rule_ref") @db.Uuid
  seriesRef            String               @map("series_ref") @db.Uuid
  tenantId             String               @map("tenant_id")
  family               CostingFamily
  code                 String               @db.VarChar(80)
  name                 String               @db.VarChar(180)
  classification       CostingEconomicClass
  source               CostingSource
  priority             Int                  @default(100)
  specificity          Int                  @default(0)
  conditions           Json
  conditionHash        String               @map("condition_hash") @db.Char(64)
  unitCost             Decimal?             @map("unit_cost") @db.Decimal(18, 6)
  currency             String               @db.Char(3)
  minimumMarginBps     Int?                 @map("minimum_margin_bps")
  recommendedMarginBps Int?                 @map("recommended_margin_bps")
  result               Json
  state                CostingRuleState     @default(DRAFT)
  version              Int
  validFrom            DateTime?            @map("valid_from") @db.Timestamptz(6)
  validTo              DateTime?            @map("valid_to") @db.Timestamptz(6)
  replacesRuleId       String?              @map("replaces_rule_id")
  requestId            String               @map("request_id") @db.VarChar(191)
  payloadHash          String               @map("payload_hash") @db.Char(64)
  actorMembershipId    String               @map("actor_membership_id")
  actorUserId          String               @map("actor_user_id")
  createdAt            DateTime             @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant               Tenant               @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "costing_rules_tenant_fkey")
  actorMembership      TenantMembership     @relation("CostingRuleActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "costing_rules_actor_fkey")
  replacesRule         CostingRule?         @relation("CostingRuleReplacement", fields: [tenantId, replacesRuleId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_rules_replaces_fkey")
  replacement          CostingRule?         @relation("CostingRuleReplacement")

  @@unique([tenantId, id], map: "costing_rules_tenant_id_key")
  @@unique([tenantId, ruleRef], map: "costing_rules_tenant_ref_key")
  @@unique([tenantId, seriesRef, version], map: "costing_rules_series_version_key")
  @@unique([tenantId, replacesRuleId], map: "costing_rules_replaces_key")
  @@unique([tenantId, requestId], map: "costing_rules_request_key")
  @@index([tenantId, family, state, priority, specificity, validFrom, validTo], map: "costing_rules_resolve_idx")
  @@map("costing_rules")
}

model CostingExchangeRate {
  id                String               @id @default(cuid())
  rateRef           String               @default(dbgenerated("gen_random_uuid()")) @map("rate_ref") @db.Uuid
  seriesRef         String               @map("series_ref") @db.Uuid
  tenantId          String               @map("tenant_id")
  baseCurrency      String               @map("base_currency") @db.Char(3)
  quoteCurrency     String               @map("quote_currency") @db.Char(3)
  rate              Decimal              @db.Decimal(24, 10)
  source            String               @db.VarChar(120)
  state             CostingRuleState     @default(DRAFT)
  version           Int
  effectiveAt       DateTime             @map("effective_at") @db.Timestamptz(6)
  validTo           DateTime?            @map("valid_to") @db.Timestamptz(6)
  replacesRateId    String?              @map("replaces_rate_id")
  logicalSha256     String               @map("logical_sha256") @db.Char(64)
  requestId         String               @map("request_id") @db.VarChar(191)
  payloadHash       String               @map("payload_hash") @db.Char(64)
  actorMembershipId String               @map("actor_membership_id")
  actorUserId       String               @map("actor_user_id")
  createdAt         DateTime             @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant               @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "costing_exchange_rates_tenant_fkey")
  actorMembership   TenantMembership     @relation("CostingExchangeRateActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "costing_exchange_rates_actor_fkey")
  replacesRate      CostingExchangeRate? @relation("CostingExchangeRateReplacement", fields: [tenantId, replacesRateId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_exchange_rates_replaces_fkey")
  replacement       CostingExchangeRate? @relation("CostingExchangeRateReplacement")

  @@unique([tenantId, id], map: "costing_exchange_rates_tenant_id_key")
  @@unique([tenantId, rateRef], map: "costing_exchange_rates_tenant_ref_key")
  @@unique([tenantId, seriesRef, version], map: "costing_exchange_rates_series_version_key")
  @@unique([tenantId, replacesRateId], map: "costing_exchange_rates_replaces_key")
  @@unique([tenantId, requestId], map: "costing_exchange_rates_request_key")
  @@index([tenantId, baseCurrency, quoteCurrency, state, effectiveAt(sort: Desc)], map: "costing_exchange_rates_resolve_idx")
  @@map("costing_exchange_rates")
}

model CostingCalculation {
  id                  String                   @id @default(cuid())
  calculationRef      String                   @default(dbgenerated("gen_random_uuid()")) @map("calculation_ref") @db.Uuid
  tenantId            String                   @map("tenant_id")
  pipelineCaseId      String                   @map("pipeline_case_id")
  logisticsRevisionId String                   @map("logistics_revision_id")
  baseCurrency        String                   @map("base_currency") @db.Char(3)
  inputSnapshot       Json                     @map("input_snapshot")
  rulesSnapshot       Json                     @map("rules_snapshot")
  ratesSnapshot       Json                     @map("rates_snapshot")
  resultSnapshot      Json                     @map("result_snapshot")
  inputHash           String                   @map("input_hash") @db.Char(64)
  resultHash          String                   @map("result_hash") @db.Char(64)
  status              CostingCalculationStatus @default(VALID)
  requestId           String                   @map("request_id") @db.VarChar(191)
  payloadHash         String                   @map("payload_hash") @db.Char(64)
  actorMembershipId   String                   @map("actor_membership_id")
  actorUserId         String                   @map("actor_user_id")
  createdAt           DateTime                 @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant              Tenant                   @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "costing_calculations_tenant_fkey")
  pipelineCase        PipelineCase             @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_calculations_case_fkey")
  logisticsRevision   LogisticsPlanRevision    @relation(fields: [tenantId, logisticsRevisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_calculations_logistics_revision_fkey")
  actorMembership     TenantMembership         @relation("CostingCalculationActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "costing_calculations_actor_fkey")
  publishedRevision   CostingRevision?

  @@unique([tenantId, id], map: "costing_calculations_tenant_id_key")
  @@unique([tenantId, calculationRef], map: "costing_calculations_tenant_ref_key")
  @@unique([tenantId, requestId], map: "costing_calculations_request_key")
  @@index([tenantId, pipelineCaseId, createdAt(sort: Desc)], map: "costing_calculations_case_idx")
  @@index([tenantId, logisticsRevisionId], map: "costing_calculations_logistics_idx")
  @@map("costing_calculations")
}

model CostingRevision {
  id                      String                  @id @default(cuid())
  revisionRef             String                  @default(dbgenerated("gen_random_uuid()")) @map("revision_ref") @db.Uuid
  tenantId                String                  @map("tenant_id")
  pipelineCaseId          String                  @map("pipeline_case_id")
  logisticsRevisionId     String                  @map("logistics_revision_id")
  calculationId           String                  @unique @map("calculation_id")
  revision                Int
  status                  CostingRevisionStatus   @default(PUBLISHED)
  baseCurrency            String                  @map("base_currency") @db.Char(3)
  inputSnapshot           Json                    @map("input_snapshot")
  rulesSnapshot           Json                    @map("rules_snapshot")
  ratesSnapshot           Json                    @map("rates_snapshot")
  totalsSnapshot          Json                    @map("totals_snapshot")
  logicalSha256           String                  @map("logical_sha256") @db.Char(64)
  publishedByMembershipId String                  @map("published_by_membership_id")
  publishedByUserId       String                  @map("published_by_user_id")
  publishedAt             DateTime                @default(now()) @map("published_at") @db.Timestamptz(6)
  tenant                  Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "costing_revisions_tenant_fkey")
  pipelineCase            PipelineCase            @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_revisions_case_fkey")
  logisticsRevision       LogisticsPlanRevision   @relation(fields: [tenantId, logisticsRevisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_revisions_logistics_revision_fkey")
  calculation             CostingCalculation      @relation(fields: [tenantId, calculationId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_revisions_calculation_fkey")
  publishedByMembership   TenantMembership        @relation("CostingRevisionActor", fields: [tenantId, publishedByMembershipId, publishedByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "costing_revisions_actor_fkey")
  lines                   CostingLine[]
  issues                  CostingIssue[]
  overrides               CostingOverride[]
  quoteCycles             PipelineCaseQuote[]
  quoteProposalRevisions  QuoteProposalRevision[]

  @@unique([tenantId, id], map: "costing_revisions_tenant_id_key")
  @@unique([tenantId, revisionRef], map: "costing_revisions_tenant_ref_key")
  @@unique([tenantId, pipelineCaseId, revision], map: "costing_revisions_case_revision_key")
  @@unique([tenantId, calculationId], map: "costing_revisions_calculation_tenant_key")
  @@index([tenantId, pipelineCaseId, status, revision(sort: Desc)], map: "costing_revisions_current_idx")
  @@map("costing_revisions")
}

model CostingLine {
  id                   String               @id @default(cuid())
  lineRef              String               @default(dbgenerated("gen_random_uuid()")) @map("line_ref") @db.Uuid
  tenantId             String               @map("tenant_id")
  revisionId           String               @map("revision_id")
  logisticsItemRef     String?              @map("logistics_item_ref") @db.Uuid
  family               CostingFamily
  concept              String               @db.VarChar(240)
  classification       CostingEconomicClass
  source               CostingSource
  sourceRef            String?              @map("source_ref") @db.Uuid
  sourceVersion        Int?                 @map("source_version")
  quantity             Decimal              @db.Decimal(18, 4)
  unit                 String               @db.VarChar(32)
  originalCurrency     String               @map("original_currency") @db.Char(3)
  originalUnitCost     Decimal              @map("original_unit_cost") @db.Decimal(18, 6)
  exchangeRateRef      String?              @map("exchange_rate_ref") @db.Uuid
  exchangeRateVersion  Int?                 @map("exchange_rate_version")
  exchangeRate         Decimal              @map("exchange_rate") @db.Decimal(24, 10)
  baseCurrency         String               @map("base_currency") @db.Char(3)
  baseUnitCost         Decimal              @map("base_unit_cost") @db.Decimal(18, 6)
  totalCost            Decimal              @map("total_cost") @db.Decimal(18, 6)
  minimumMarginBps     Int?                 @map("minimum_margin_bps")
  recommendedMarginBps Int?                 @map("recommended_margin_bps")
  suggestedPrice       Decimal?             @map("suggested_price") @db.Decimal(18, 6)
  priceStatus          CostingPriceStatus   @default(NO_MARGIN)
  snapshot             Json
  position             Int
  tenant               Tenant               @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "costing_lines_tenant_fkey")
  revision             CostingRevision      @relation(fields: [tenantId, revisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_lines_revision_fkey")
  overrides            CostingOverride[]

  @@unique([tenantId, id], map: "costing_lines_tenant_id_key")
  @@unique([tenantId, lineRef], map: "costing_lines_tenant_ref_key")
  @@unique([tenantId, revisionId, position], map: "costing_lines_position_key")
  @@index([tenantId, revisionId, family, classification], map: "costing_lines_family_idx")
  @@map("costing_lines")
}

model CostingIssue {
  id                     String               @id @default(cuid())
  issueRef               String               @default(dbgenerated("gen_random_uuid()")) @map("issue_ref") @db.Uuid
  tenantId               String               @map("tenant_id")
  revisionId             String               @map("revision_id")
  code                   String               @db.VarChar(80)
  severity               CostingIssueSeverity
  family                 CostingFamily?
  message                String               @db.VarChar(500)
  source                 CostingSource
  sourceSnapshot         Json                 @map("source_snapshot")
  status                 CostingIssueStatus   @default(OPEN)
  resolvedReason         String?              @map("resolved_reason") @db.VarChar(1000)
  resolvedByMembershipId String?              @map("resolved_by_membership_id")
  resolvedByUserId       String?              @map("resolved_by_user_id")
  resolvedAt             DateTime?            @map("resolved_at") @db.Timestamptz(6)
  version                Int                  @default(1)
  tenant                 Tenant               @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "costing_issues_tenant_fkey")
  revision               CostingRevision      @relation(fields: [tenantId, revisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_issues_revision_fkey")
  resolvedByMembership   TenantMembership?    @relation("CostingIssueResolver", fields: [tenantId, resolvedByMembershipId, resolvedByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "costing_issues_resolver_fkey")

  @@unique([tenantId, issueRef], map: "costing_issues_tenant_ref_key")
  @@index([tenantId, revisionId, severity, status], map: "costing_issues_revision_idx")
  @@map("costing_issues")
}

model CostingOverride {
  id                String                       @id @default(cuid())
  overrideRef       String                       @default(dbgenerated("gen_random_uuid()")) @map("override_ref") @db.Uuid
  tenantId          String                       @map("tenant_id")
  revisionId        String                       @map("revision_id")
  lineId            String?                      @map("line_id")
  kind              CostingOverrideKind
  suggestedValue    Json                         @map("suggested_value")
  finalValue        Json                         @map("final_value")
  reason            String                       @db.VarChar(1000)
  status            CostingOverrideStatus        @default(APPLIED)
  actorMembershipId String                       @map("actor_membership_id")
  actorUserId       String                       @map("actor_user_id")
  createdAt         DateTime                     @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant                       @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "costing_overrides_tenant_fkey")
  revision          CostingRevision              @relation(fields: [tenantId, revisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_overrides_revision_fkey")
  line              CostingLine?                 @relation(fields: [tenantId, lineId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_overrides_line_fkey")
  actorMembership   TenantMembership             @relation("CostingOverrideActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "costing_overrides_actor_fkey")
  authorizations    CostingMarginAuthorization[]

  @@unique([tenantId, id], map: "costing_overrides_tenant_id_key")
  @@unique([tenantId, overrideRef], map: "costing_overrides_tenant_ref_key")
  @@index([tenantId, revisionId, lineId, createdAt(sort: Desc)], map: "costing_overrides_line_idx")
  @@map("costing_overrides")
}

model CostingMarginAuthorization {
  id                String                       @id @default(cuid())
  authorizationRef  String                       @default(dbgenerated("gen_random_uuid()")) @map("authorization_ref") @db.Uuid
  tenantId          String                       @map("tenant_id")
  overrideId        String                       @map("override_id")
  decision          CostingAuthorizationDecision
  reason            String                       @db.VarChar(1000)
  actorMembershipId String                       @map("actor_membership_id")
  actorUserId       String                       @map("actor_user_id")
  createdAt         DateTime                     @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant                       @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "costing_margin_authorizations_tenant_fkey")
  override          CostingOverride              @relation(fields: [tenantId, overrideId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "costing_margin_authorizations_override_fkey")
  actorMembership   TenantMembership             @relation("CostingMarginAuthorizationActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "costing_margin_authorizations_actor_fkey")

  @@unique([tenantId, authorizationRef], map: "costing_margin_authorizations_tenant_ref_key")
  @@unique([tenantId, overrideId], map: "costing_margin_authorizations_override_key")
  @@index([tenantId, createdAt(sort: Desc)], map: "costing_margin_authorizations_created_idx")
  @@map("costing_margin_authorizations")
}

model CostingMutationCommand {
  id                String           @id @default(cuid())
  tenantId          String           @map("tenant_id")
  requestId         String           @map("request_id") @db.VarChar(191)
  operation         String           @db.VarChar(80)
  payloadHash       String           @map("payload_hash") @db.Char(64)
  targetRef         String           @map("target_ref") @db.VarChar(191)
  resultJson        Json             @map("result_json")
  actorMembershipId String           @map("actor_membership_id")
  actorUserId       String           @map("actor_user_id")
  createdAt         DateTime         @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant           @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "costing_commands_tenant_fkey")
  actorMembership   TenantMembership @relation("CostingCommandActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "costing_commands_actor_fkey")

  @@unique([tenantId, requestId], map: "costing_commands_tenant_request_key")
  @@index([tenantId, targetRef, createdAt(sort: Desc)], map: "costing_commands_target_idx")
  @@map("costing_mutation_commands")
}

model QuoteProposal {
  id              String                  @id @default(cuid())
  proposalRef     String                  @default(dbgenerated("gen_random_uuid()")) @map("proposal_ref") @db.Uuid
  tenantId        String                  @map("tenant_id")
  quoteId         String                  @map("quote_id")
  pipelineCaseId  String                  @map("pipeline_case_id")
  position        Int
  reference       String                  @db.VarChar(80)
  state           QuoteProposalState      @default(DRAFT)
  currentRevision Int                     @default(0) @map("current_revision")
  createdAt       DateTime                @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime                @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant          Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "quote_proposals_tenant_fkey")
  quote           PipelineCaseQuote       @relation(fields: [tenantId, quoteId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "quote_proposals_quote_fkey")
  pipelineCase    PipelineCase            @relation(fields: [tenantId, pipelineCaseId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "quote_proposals_case_fkey")
  revisions       QuoteProposalRevision[]
  decisions       QuoteClientDecision[]

  @@unique([tenantId, id], map: "quote_proposals_tenant_id_key")
  @@unique([tenantId, proposalRef], map: "quote_proposals_tenant_ref_key")
  @@unique([tenantId, reference], map: "quote_proposals_tenant_reference_key")
  @@unique([tenantId, quoteId, position], map: "quote_proposals_position_key")
  @@index([tenantId, pipelineCaseId, state, position], map: "quote_proposals_case_state_idx")
  @@map("quote_proposals")
}

model QuoteProposalRevision {
  id                          String                  @id @default(cuid())
  revisionRef                 String                  @default(dbgenerated("gen_random_uuid()")) @map("revision_ref") @db.Uuid
  tenantId                    String                  @map("tenant_id")
  proposalId                  String                  @map("proposal_id")
  costingRevisionId           String                  @map("costing_revision_id")
  revision                    Int
  state                       QuoteProposalState
  proposalName                String                  @map("proposal_name") @db.VarChar(120)
  costingLogicalSha256        String                  @map("costing_logical_sha256") @db.Char(64)
  currency                    String                  @db.Char(3)
  issueDate                   DateTime                @map("issue_date") @db.Date
  validUntil                  DateTime                @map("valid_until") @db.Date
  commercialContextSnapshot   Json                    @map("commercial_context_snapshot")
  payerSnapshot               Json                    @map("payer_snapshot")
  termsSnapshot               Json                    @map("terms_snapshot")
  exchangeSnapshot            Json?                   @map("exchange_snapshot")
  discountSnapshot            Json?                   @map("discount_snapshot")
  totalsSnapshot              Json                    @map("totals_snapshot")
  marginAuthorizationSnapshot Json?                   @map("margin_authorization_snapshot")
  internalSnapshot            Json                    @map("internal_snapshot")
  clientSnapshot              Json                    @map("client_snapshot")
  logicalSha256               String                  @map("logical_sha256") @db.Char(64)
  supersedesRevisionId        String?                 @map("supersedes_revision_id")
  createdByMembershipId       String                  @map("created_by_membership_id")
  createdByUserId             String                  @map("created_by_user_id")
  createdAt                   DateTime                @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant                      Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "quote_proposal_revisions_tenant_fkey")
  proposal                    QuoteProposal           @relation(fields: [tenantId, proposalId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "quote_proposal_revisions_proposal_fkey")
  costingRevision             CostingRevision         @relation(fields: [tenantId, costingRevisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "quote_proposal_revisions_costing_fkey")
  supersedes                  QuoteProposalRevision?  @relation("QuoteRevisionHistory", fields: [tenantId, supersedesRevisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "quote_proposal_revisions_supersedes_fkey")
  supersededBy                QuoteProposalRevision[] @relation("QuoteRevisionHistory")
  createdByMembership         TenantMembership        @relation("QuoteProposalRevisionActor", fields: [tenantId, createdByMembershipId, createdByUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "quote_proposal_revisions_actor_fkey")
  lines                       QuoteLine[]
  issues                      QuoteIssue[]
  dispatches                  QuoteDispatch[]
  decisions                   QuoteClientDecision[]

  @@unique([tenantId, id], map: "quote_proposal_revisions_tenant_id_key")
  @@unique([tenantId, revisionRef], map: "quote_proposal_revisions_tenant_ref_key")
  @@unique([tenantId, proposalId, revision], map: "quote_proposal_revisions_version_key")
  @@index([tenantId, proposalId, state, revision(sort: Desc)], map: "quote_proposal_revisions_current_idx")
  @@map("quote_proposal_revisions")
}

model QuoteLine {
  id             String                @id @default(cuid())
  lineRef        String                @default(dbgenerated("gen_random_uuid()")) @map("line_ref") @db.Uuid
  tenantId       String                @map("tenant_id")
  revisionId     String                @map("revision_id")
  sourceKind     QuoteLineSourceKind   @map("source_kind")
  costingLineRef String?               @map("costing_line_ref") @db.Uuid
  sourceRef      String                @map("source_ref") @db.VarChar(191)
  sourceVersion  Int                   @map("source_version")
  concept        String                @db.VarChar(240)
  quantity       Decimal               @db.Decimal(18, 4)
  unit           String                @db.VarChar(32)
  economicClass  CostingEconomicClass  @map("economic_class")
  priceStatus    QuoteLinePriceStatus  @map("price_status")
  capturedCost   Decimal?              @map("captured_cost") @db.Decimal(18, 6)
  suggestedPrice Decimal?              @map("suggested_price") @db.Decimal(18, 6)
  quotedPrice    Decimal?              @map("quoted_price") @db.Decimal(18, 6)
  currency       String                @db.Char(3)
  reason         String?               @db.VarChar(1000)
  position       Int
  snapshot       Json
  tenant         Tenant                @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "quote_lines_tenant_fkey")
  revision       QuoteProposalRevision @relation(fields: [tenantId, revisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "quote_lines_revision_fkey")

  @@unique([tenantId, id], map: "quote_lines_tenant_id_key")
  @@unique([tenantId, lineRef], map: "quote_lines_tenant_ref_key")
  @@unique([tenantId, revisionId, position], map: "quote_lines_position_key")
  @@index([tenantId, revisionId, sourceKind, economicClass], map: "quote_lines_revision_source_idx")
  @@map("quote_lines")
}

model QuoteIssue {
  id             String                @id @default(cuid())
  issueRef       String                @default(dbgenerated("gen_random_uuid()")) @map("issue_ref") @db.Uuid
  tenantId       String                @map("tenant_id")
  revisionId     String                @map("revision_id")
  code           String                @db.VarChar(80)
  severity       CostingIssueSeverity
  message        String                @db.VarChar(500)
  status         CostingIssueStatus    @default(OPEN)
  sourceSnapshot Json                  @map("source_snapshot")
  tenant         Tenant                @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "quote_issues_tenant_fkey")
  revision       QuoteProposalRevision @relation(fields: [tenantId, revisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "quote_issues_revision_fkey")

  @@unique([tenantId, issueRef], map: "quote_issues_tenant_ref_key")
  @@index([tenantId, revisionId, severity, status], map: "quote_issues_revision_idx")
  @@map("quote_issues")
}

model QuoteDispatch {
  id                String                @id @default(cuid())
  dispatchRef       String                @default(dbgenerated("gen_random_uuid()")) @map("dispatch_ref") @db.Uuid
  tenantId          String                @map("tenant_id")
  revisionId        String                @map("revision_id")
  channel           QuoteDispatchChannel
  recipientSnapshot Json                  @map("recipient_snapshot")
  evidenceRef       String?               @map("evidence_ref") @db.VarChar(191)
  actorMembershipId String                @map("actor_membership_id")
  actorUserId       String                @map("actor_user_id")
  sentAt            DateTime              @default(now()) @map("sent_at") @db.Timestamptz(6)
  tenant            Tenant                @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "quote_dispatches_tenant_fkey")
  revision          QuoteProposalRevision @relation(fields: [tenantId, revisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "quote_dispatches_revision_fkey")
  actorMembership   TenantMembership      @relation("QuoteDispatchActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "quote_dispatches_actor_fkey")

  @@unique([tenantId, dispatchRef], map: "quote_dispatches_tenant_ref_key")
  @@index([tenantId, revisionId, sentAt(sort: Desc)], map: "quote_dispatches_revision_idx")
  @@map("quote_dispatches")
}

model QuoteClientDecision {
  id                String                  @id @default(cuid())
  decisionRef       String                  @default(dbgenerated("gen_random_uuid()")) @map("decision_ref") @db.Uuid
  tenantId          String                  @map("tenant_id")
  proposalId        String                  @map("proposal_id")
  revisionId        String                  @map("revision_id")
  decision          QuoteClientDecisionKind
  method            String                  @db.VarChar(80)
  decidedBySnapshot Json                    @map("decided_by_snapshot")
  evidenceRef       String                  @map("evidence_ref") @db.VarChar(191)
  reason            String?                 @db.VarChar(1000)
  actorMembershipId String                  @map("actor_membership_id")
  actorUserId       String                  @map("actor_user_id")
  decidedAt         DateTime                @default(now()) @map("decided_at") @db.Timestamptz(6)
  tenant            Tenant                  @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "quote_client_decisions_tenant_fkey")
  proposal          QuoteProposal           @relation(fields: [tenantId, proposalId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "quote_client_decisions_proposal_fkey")
  revision          QuoteProposalRevision   @relation(fields: [tenantId, revisionId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "quote_client_decisions_revision_fkey")
  actorMembership   TenantMembership        @relation("QuoteClientDecisionActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "quote_client_decisions_actor_fkey")

  @@unique([tenantId, decisionRef], map: "quote_client_decisions_tenant_ref_key")
  @@unique([tenantId, proposalId, revisionId], map: "quote_client_decisions_once_key")
  @@index([tenantId, proposalId, decidedAt(sort: Desc)], map: "quote_client_decisions_proposal_idx")
  @@map("quote_client_decisions")
}

model QuoteMutationCommand {
  id                String             @id @default(cuid())
  tenantId          String             @map("tenant_id")
  quoteId           String?            @map("quote_id")
  requestId         String             @map("request_id") @db.Uuid
  operation         String             @db.VarChar(80)
  payloadHash       String             @map("payload_hash") @db.Char(64)
  targetRef         String             @map("target_ref") @db.VarChar(191)
  resultJson        Json               @map("result_json")
  actorMembershipId String             @map("actor_membership_id")
  actorUserId       String             @map("actor_user_id")
  createdAt         DateTime           @default(now()) @map("created_at") @db.Timestamptz(6)
  tenant            Tenant             @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "quote_mutation_commands_tenant_fkey")
  quote             PipelineCaseQuote? @relation(fields: [tenantId, quoteId], references: [tenantId, id], onDelete: Restrict, onUpdate: Cascade, map: "quote_mutation_commands_quote_fkey")
  actorMembership   TenantMembership   @relation("QuoteMutationCommandActor", fields: [tenantId, actorMembershipId, actorUserId], references: [tenantId, id, userId], onDelete: Restrict, onUpdate: Cascade, map: "quote_mutation_commands_actor_fkey")

  @@unique([tenantId, requestId], map: "quote_commands_tenant_request_key")
  @@index([tenantId, quoteId, operation, createdAt(sort: Desc)], map: "quote_commands_quote_idx")
  @@map("quote_mutation_commands")
}

model QuoteReferenceCounter {
  tenantId  String   @map("tenant_id")
  year      Int
  value     Int      @default(0)
  updatedAt DateTime @updatedAt @map("updated_at") @db.Timestamptz(6)
  tenant    Tenant   @relation(fields: [tenantId], references: [id], onDelete: Restrict, onUpdate: Cascade, map: "quote_reference_counters_tenant_fkey")

  @@id([tenantId, year])
  @@map("quote_reference_counters")
}

model CatalogMaterial {
  id          String   @id @default(cuid())
  code        String   @unique
  name        String
  description String?
  unit        String?
  active      Boolean  @default(true)
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @default(now()) @updatedAt @map("updated_at")

  @@map("catalog_materials")
}

model CatalogAssumption {
  id          String   @id @default(cuid())
  code        String   @unique
  name        String
  description String?
  active      Boolean  @default(true)
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @default(now()) @updatedAt @map("updated_at")

  @@map("catalog_assumptions")
}

model CatalogServiceType {
  id         String   @id @default(cuid())
  code       String   @unique
  name       String
  category   String
  modePolicy String   @map("mode_policy")
  fixedMode  String?  @map("fixed_mode")
  active     Boolean  @default(true)
  metadata   Json     @default("{}")
  createdAt  DateTime @default(now()) @map("created_at")
  updatedAt  DateTime @default(now()) @updatedAt @map("updated_at")

  @@map("catalog_service_types")
}

model CatalogServiceFlag {
  id          String   @id @default(cuid())
  code        String   @unique
  name        String
  description String?
  active      Boolean  @default(true)
  createdAt   DateTime @default(now()) @map("created_at")
  updatedAt   DateTime @default(now()) @updatedAt @map("updated_at")

  @@map("catalog_service_flags")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model crate_calculation_snapshots {
  id                        String                  @id
  tenant_id                 String
  calculation_ref           String                  @db.VarChar(191)
  source_entity             String                  @db.VarChar(80)
  source_entity_id          String                  @db.VarChar(191)
  settings_id               String
  settings_business_version Int
  settings_hash             String                  @db.Char(64)
  technical_snapshot_json   Json
  economic_snapshot_json    Json
  units_snapshot_json       Json
  currency_code             String                  @db.Char(3)
  calculation_input_hash    String                  @db.Char(64)
  calculation_output_hash   String                  @db.Char(64)
  source                    String                  @db.VarChar(120)
  created_by_user_id        String?
  created_by_membership_id  String?
  request_id                String                  @db.VarChar(191)
  created_at                DateTime                @default(now()) @db.Timestamptz(3)
  tenant_memberships        TenantMembership?       @relation(fields: [tenant_id, created_by_membership_id, created_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "crate_calculation_snapshots_actor_fkey")
  crate_settings_versions   crate_settings_versions @relation(fields: [tenant_id, settings_id], references: [tenant_id, id], onUpdate: NoAction, map: "crate_calculation_snapshots_settings_fkey")
  tenants                   Tenant                  @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "crate_calculation_snapshots_tenant_fkey")

  @@unique([tenant_id, id])
  @@unique([tenant_id, calculation_ref], map: "crate_calculation_snapshots_tenant_ref_key")
  @@unique([tenant_id, request_id], map: "crate_calculation_snapshots_tenant_request_key")
  @@index([tenant_id, source_entity, source_entity_id, created_at(sort: Desc)], map: "crate_calculation_snapshots_entity_idx")
  @@index([tenant_id, settings_id, created_at(sort: Desc)], map: "crate_calculation_snapshots_settings_idx")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model crate_settings_versions {
  id                                                                                                                     String                        @id
  tenant_id                                                                                                              String
  series_id                                                                                                              String
  code                                                                                                                   String                        @db.VarChar(80)
  normalized_code                                                                                                        String                        @db.VarChar(80)
  name                                                                                                                   String                        @db.VarChar(180)
  scope                                                                                                                  String                        @db.VarChar(120)
  schema_version                                                                                                         Int
  business_version                                                                                                       Int
  state                                                                                                                  LogisticsConfigState          @default(DRAFT)
  operation_mode                                                                                                         LogisticsOperationMode        @default(LEGACY_ONLY)
  technical_json                                                                                                         Json
  economic_json                                                                                                          Json
  catalog_refs_json                                                                                                      Json                          @default("[]")
  units_json                                                                                                             Json
  currency_code                                                                                                          String                        @db.Char(3)
  configuration_json                                                                                                     Json
  configuration_hash                                                                                                     String                        @db.Char(64)
  version_hash                                                                                                           String                        @db.Char(64)
  valid_from                                                                                                             DateTime?                     @db.Timestamptz(3)
  valid_to                                                                                                               DateTime?                     @db.Timestamptz(3)
  replaces_settings_id                                                                                                   String?
  source                                                                                                                 String                        @db.VarChar(120)
  evidence_json                                                                                                          Json                          @default("{}")
  created_by_user_id                                                                                                     String
  created_by_membership_id                                                                                               String
  approved_by_user_id                                                                                                    String?
  approved_by_membership_id                                                                                              String?
  approved_at                                                                                                            DateTime?                     @db.Timestamptz(3)
  activated_at                                                                                                           DateTime?                     @db.Timestamptz(3)
  retired_at                                                                                                             DateTime?                     @db.Timestamptz(3)
  request_id                                                                                                             String                        @db.VarChar(191)
  payload_hash                                                                                                           String                        @db.Char(64)
  row_version                                                                                                            Int                           @default(1)
  created_at                                                                                                             DateTime                      @default(now()) @db.Timestamptz(3)
  updated_at                                                                                                             DateTime                      @default(now()) @db.Timestamptz(3)
  crate_calculation_snapshots                                                                                            crate_calculation_snapshots[]
  tenant_memberships_crate_settings_versions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships TenantMembership?             @relation("crate_settings_versions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships", fields: [tenant_id, approved_by_membership_id, approved_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "crate_settings_versions_approver_fkey")
  tenant_memberships_crate_settings_versions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships   TenantMembership              @relation("crate_settings_versions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships", fields: [tenant_id, created_by_membership_id, created_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "crate_settings_versions_creator_fkey")
  crate_settings_versions                                                                                                crate_settings_versions?      @relation("crate_settings_versionsTocrate_settings_versions", fields: [tenant_id, replaces_settings_id], references: [tenant_id, id], onUpdate: NoAction, map: "crate_settings_versions_replaces_fkey")
  other_crate_settings_versions                                                                                          crate_settings_versions[]     @relation("crate_settings_versionsTocrate_settings_versions")
  tenants                                                                                                                Tenant                        @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "crate_settings_versions_tenant_fkey")

  @@unique([tenant_id, normalized_code, business_version], map: "crate_settings_versions_tenant_code_version_key")
  @@unique([tenant_id, id])
  @@unique([tenant_id, request_id], map: "crate_settings_versions_tenant_request_key")
  @@unique([tenant_id, series_id, business_version], map: "crate_settings_versions_tenant_series_version_key")
  @@unique([tenant_id, version_hash], map: "crate_settings_versions_tenant_version_hash_key")
  @@index([tenant_id, scope, state, valid_from, valid_to], map: "crate_settings_versions_resolve_idx")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model logistics_configuration_versions {
  id                                                                                                                              String                 @id
  tenant_id                                                                                                                       String
  version                                                                                                                         Int
  mode                                                                                                                            LogisticsOperationMode @default(LEGACY_ONLY)
  state                                                                                                                           LogisticsConfigState   @default(DRAFT)
  source                                                                                                                          String                 @db.VarChar(120)
  source_snapshot_json                                                                                                            Json
  source_hash                                                                                                                     String                 @db.Char(64)
  config_hash                                                                                                                     String                 @db.Char(64)
  valid_from                                                                                                                      DateTime?
  valid_to                                                                                                                        DateTime?
  evidence_refs_json                                                                                                              Json                   @default("[]")
  created_by_user_id                                                                                                              String
  created_by_membership_id                                                                                                        String
  approved_by_user_id                                                                                                             String?
  approved_by_membership_id                                                                                                       String?
  approved_at                                                                                                                     DateTime?
  activated_at                                                                                                                    DateTime?
  retired_at                                                                                                                      DateTime?
  request_id                                                                                                                      String                 @db.VarChar(191)
  payload_hash                                                                                                                    String                 @db.Char(64)
  row_version                                                                                                                     Int                    @default(1)
  created_at                                                                                                                      DateTime               @default(now())
  updated_at                                                                                                                      DateTime               @default(now())
  tenant_memberships_logistics_configuration_versions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships TenantMembership?      @relation("logistics_configuration_versions_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships", fields: [tenant_id, approved_by_membership_id, approved_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "logistics_configuration_versions_approver_fkey")
  tenant_memberships_logistics_configuration_versions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships   TenantMembership       @relation("logistics_configuration_versions_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships", fields: [tenant_id, created_by_membership_id, created_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "logistics_configuration_versions_creator_fkey")
  tenants                                                                                                                         Tenant                 @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "logistics_configuration_versions_tenant_fkey")

  @@unique([tenant_id, config_hash], map: "logistics_configuration_versions_tenant_hash_key")
  @@unique([tenant_id, id], map: "logistics_configuration_versions_tenant_id_key")
  @@unique([tenant_id, request_id], map: "logistics_configuration_versions_tenant_request_key")
  @@unique([tenant_id, version], map: "logistics_configuration_versions_tenant_version_key")
  @@index([tenant_id, state, valid_from, valid_to], map: "logistics_configuration_versions_tenant_state_idx")
}

model osi_geo_region_aliases {
  id               String             @id
  tenant_id        String
  region_id        String
  country_code     String             @db.Char(2)
  alias            String             @db.VarChar(180)
  normalized_alias String             @db.VarChar(200)
  kind             GeoRegionAliasKind
  created_at       DateTime           @default(now())
  osi_geo_regions  GeoRegion          @relation(fields: [tenant_id, region_id], references: [tenant_id, id], onUpdate: NoAction, map: "osi_geo_region_aliases_region_fkey")

  @@unique([tenant_id, region_id, normalized_alias], map: "osi_geo_region_aliases_tenant_region_alias_key")
  @@index([tenant_id, country_code, normalized_alias], map: "osi_geo_region_aliases_lookup_idx")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model quote_change_order_commands {
  id                   String            @id
  tenant_id            String
  change_order_id      String
  change_order_version Int
  command              String            @db.VarChar(80)
  request_id           String            @db.VarChar(191)
  payload_hash         String            @db.Char(64)
  result_json          Json
  actor_user_id        String?
  actor_membership_id  String?
  created_at           DateTime          @default(now())
  tenant_memberships   TenantMembership? @relation(fields: [tenant_id, actor_membership_id, actor_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "quote_change_order_commands_actor_fkey")
  quote_change_orders  QuoteChangeOrder  @relation(fields: [tenant_id, change_order_id], references: [tenant_id, id], onUpdate: NoAction, map: "quote_change_order_commands_order_fkey")

  @@unique([tenant_id, request_id], map: "quote_change_order_commands_tenant_request_key")
  @@index([tenant_id, change_order_id, created_at(sort: Desc)], map: "quote_change_order_commands_order_idx")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model quote_change_order_items {
  id                  String                     @id
  tenant_id           String
  change_order_id     String
  line_key            String                     @db.VarChar(191)
  change_kind         QuoteChangeOrderItemChange
  classification      String                     @db.VarChar(120)
  description         String
  unit                String?                    @db.VarChar(40)
  source_line_id      String?
  previous_quantity   Decimal?                   @db.Decimal(18, 4)
  new_quantity        Decimal?                   @db.Decimal(18, 4)
  previous_unit_price Decimal?                   @db.Decimal(18, 4)
  new_unit_price      Decimal?                   @db.Decimal(18, 4)
  previous_line_total Decimal                    @default(0) @db.Decimal(18, 2)
  new_line_total      Decimal                    @default(0) @db.Decimal(18, 2)
  before_json         Json?
  after_json          Json?
  created_at          DateTime                   @default(now())
  quote_change_orders QuoteChangeOrder           @relation(fields: [tenant_id, change_order_id], references: [tenant_id, id], onUpdate: NoAction, map: "quote_change_order_items_order_fkey")

  @@unique([tenant_id, change_order_id, line_key], map: "quote_change_order_items_line_key")
  @@unique([tenant_id, id], map: "quote_change_order_items_tenant_id_key")
  @@index([tenant_id, classification], map: "quote_change_order_items_classification_idx")
  @@index([tenant_id, change_order_id, created_at], map: "quote_change_order_items_order_idx")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model quote_change_order_policies {
  id                       String                       @id
  tenant_id                String
  code                     String                       @db.VarChar(100)
  version                  Int
  name                     String                       @db.VarChar(180)
  status                   QuoteChangeOrderPolicyStatus @default(DRAFT)
  cap_percent              Decimal                      @db.Decimal(7, 4)
  approval_rules_json      Json                         @default("{}")
  valid_from               DateTime?
  valid_to                 DateTime?
  policy_hash              String                       @db.Char(64)
  created_by_user_id       String
  created_by_membership_id String
  request_id               String                       @db.VarChar(191)
  created_at               DateTime                     @default(now())
  tenant_memberships       TenantMembership             @relation(fields: [tenant_id, created_by_membership_id, created_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "quote_change_order_policies_actor_fkey")
  tenants                  Tenant                       @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "quote_change_order_policies_tenant_fkey")
  quote_change_orders      QuoteChangeOrder[]

  @@unique([tenant_id, code, version], map: "quote_change_order_policies_tenant_code_version_key")
  @@unique([tenant_id, policy_hash], map: "quote_change_order_policies_tenant_hash_key")
  @@unique([tenant_id, id], map: "quote_change_order_policies_tenant_id_key")
  @@unique([tenant_id, request_id], map: "quote_change_order_policies_tenant_request_key")
  @@index([tenant_id, status, valid_from, valid_to], map: "quote_change_order_policies_tenant_status_idx")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model quote_change_order_sequences {
  tenant_id  String   @id
  next_value BigInt   @default(1)
  updated_at DateTime @default(now())
  tenants    Tenant   @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "quote_change_order_sequences_tenant_fkey")
}

model quote_change_order_subjects {
  tenant_id                String
  pipeline_case_id         String
  base_quote_id            String             @unique(map: "quote_change_order_subjects_quote_key")
  bound_by_user_id         String
  bound_by_membership_id   String
  binding_request_id       String             @db.VarChar(191)
  created_at               DateTime           @default(now())
  tenant_memberships       TenantMembership   @relation(fields: [tenant_id, bound_by_membership_id, bound_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "quote_change_order_subjects_actor_fkey")
  osi_pipeline_cases       PipelineCase       @relation(fields: [pipeline_case_id], references: [id], onUpdate: NoAction, map: "quote_change_order_subjects_case_fkey")
  osi_pipeline_case_quotes PipelineCaseQuote  @relation(fields: [pipeline_case_id, base_quote_id], references: [caseId, id], onUpdate: NoAction, map: "quote_change_order_subjects_quote_case_fkey")
  tenants                  Tenant             @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "quote_change_order_subjects_tenant_fkey")
  quote_change_orders      QuoteChangeOrder[]

  @@id([tenant_id, pipeline_case_id, base_quote_id])
  @@unique([pipeline_case_id, base_quote_id], map: "quote_change_order_subjects_case_quote_key")
  @@unique([tenant_id, binding_request_id], map: "quote_change_order_subjects_tenant_request_key")
  @@index([tenant_id, pipeline_case_id], map: "quote_change_order_subjects_tenant_case_idx")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model risk_engine_rules {
  id                                                                                                               String                     @id
  tenant_id                                                                                                        String
  code                                                                                                             String                     @db.VarChar(100)
  version                                                                                                          Int
  name                                                                                                             String                     @db.VarChar(180)
  description                                                                                                      String?
  priority                                                                                                         Int                        @default(100)
  condition_type                                                                                                   RiskRuleConditionType
  condition_config_json                                                                                            Json
  condition_scope_hash                                                                                             String                     @db.Char(64)
  result                                                                                                           RiskDecisionResult
  state                                                                                                            RiskRuleState              @default(DRAFT)
  valid_from                                                                                                       DateTime?
  valid_to                                                                                                         DateTime?
  version_hash                                                                                                     String                     @db.Char(64)
  replaces_rule_id                                                                                                 String?
  created_by_user_id                                                                                               String
  created_by_membership_id                                                                                         String
  approved_by_user_id                                                                                              String?
  approved_by_membership_id                                                                                        String?
  approved_at                                                                                                      DateTime?
  activated_at                                                                                                     DateTime?
  retired_at                                                                                                       DateTime?
  request_id                                                                                                       String                     @db.VarChar(191)
  payload_hash                                                                                                     String                     @db.Char(64)
  row_version                                                                                                      Int                        @default(1)
  created_at                                                                                                       DateTime                   @default(now())
  updated_at                                                                                                       DateTime                   @default(now())
  logistic_override_approvals                                                                                      LogisticOverrideApproval[]
  osi_transport_zone_rules                                                                                         TransportZoneRule[]
  osi_zone_rules                                                                                                   ZoneRule[]
  tenant_memberships_risk_engine_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships TenantMembership?          @relation("risk_engine_rules_tenant_id_approved_by_membership_id_approved_by_user_idTotenant_memberships", fields: [tenant_id, approved_by_membership_id, approved_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "risk_engine_rules_approver_membership_fkey")
  tenant_memberships_risk_engine_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships   TenantMembership           @relation("risk_engine_rules_tenant_id_created_by_membership_id_created_by_user_idTotenant_memberships", fields: [tenant_id, created_by_membership_id, created_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "risk_engine_rules_creator_membership_fkey")
  risk_engine_rules                                                                                                risk_engine_rules?         @relation("risk_engine_rulesTorisk_engine_rules", fields: [tenant_id, replaces_rule_id], references: [tenant_id, id], onUpdate: NoAction, map: "risk_engine_rules_replaces_fkey")
  other_risk_engine_rules                                                                                          risk_engine_rules[]        @relation("risk_engine_rulesTorisk_engine_rules")
  tenants                                                                                                          Tenant                     @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "risk_engine_rules_tenant_fkey")
  risk_evaluation_rules                                                                                            risk_evaluation_rules[]

  @@unique([tenant_id, code, version], map: "risk_engine_rules_tenant_code_version_key")
  @@unique([tenant_id, id], map: "risk_engine_rules_tenant_id_key")
  @@unique([tenant_id, request_id], map: "risk_engine_rules_tenant_request_id_key")
  @@unique([tenant_id, version_hash], map: "risk_engine_rules_tenant_version_hash_key")
  @@index([tenant_id, replaces_rule_id], map: "risk_engine_rules_replaces_idx")
  @@index([tenant_id, condition_type, state], map: "risk_engine_rules_tenant_condition_idx")
  @@index([tenant_id, state, valid_from, valid_to, priority], map: "risk_engine_rules_tenant_state_validity_idx")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model risk_engine_settings {
  tenant_id                String                  @id
  mode                     RiskEngineOperationMode @default(LEGACY_ONLY)
  version                  Int                     @default(1)
  updated_by_user_id       String?
  updated_by_membership_id String?
  updated_at               DateTime                @default(now())
  tenant_memberships       TenantMembership?       @relation("risk_engine_settings_actor", fields: [tenant_id, updated_by_membership_id, updated_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "risk_engine_settings_actor_membership_fkey") @ignore
  tenants                  Tenant                  @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "risk_engine_settings_tenant_fkey")
}

model risk_evaluation_rules {
  tenant_id         String
  evaluation_id     String
  rule_id           String
  rule_code         String             @db.VarChar(100)
  rule_version      Int
  rule_hash         String             @db.Char(64)
  matched           Boolean
  result            RiskDecisionResult
  reasons_json      Json
  risk_evaluations  risk_evaluations   @relation(fields: [tenant_id, evaluation_id], references: [tenant_id, id], onUpdate: NoAction, map: "risk_evaluation_rules_evaluation_fkey")
  risk_engine_rules risk_engine_rules  @relation(fields: [tenant_id, rule_id], references: [tenant_id, id], onUpdate: NoAction, map: "risk_evaluation_rules_rule_fkey")

  @@id([tenant_id, evaluation_id, rule_id])
  @@index([tenant_id, rule_id, matched], map: "risk_evaluation_rules_rule_idx")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model risk_evaluations {
  id                          String                     @id
  tenant_id                   String
  entity                      String                     @db.VarChar(120)
  entity_id                   String                     @db.VarChar(191)
  case_id                     String?
  quote_id                    String?
  quote_version               Int?
  material_hash               String                     @db.Char(64)
  input_snapshot_json         Json
  rules_snapshot_json         Json
  ruleset_hash                String                     @db.Char(64)
  matched_rules_json          Json
  factors_json                Json
  reasons_json                Json
  result                      RiskDecisionResult
  mode                        RiskEvaluationMode
  evaluated_at                DateTime                   @default(now())
  request_id                  String                     @db.VarChar(191)
  payload_hash                String                     @db.Char(64)
  actor_user_id               String?
  actor_membership_id         String?
  approval_request_id         String?
  created_at                  DateTime                   @default(now())
  logistic_override_approvals LogisticOverrideApproval[]
  quote_change_orders         QuoteChangeOrder[]
  risk_evaluation_rules       risk_evaluation_rules[]
  tenant_memberships          TenantMembership?          @relation(fields: [tenant_id, actor_membership_id, actor_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "risk_evaluations_actor_membership_fkey")
  approval_requests           ApprovalRequest?           @relation(fields: [tenant_id, approval_request_id], references: [tenant_id, id], onUpdate: NoAction, map: "risk_evaluations_approval_request_fkey")
  tenants                     Tenant                     @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "risk_evaluations_tenant_fkey")

  @@unique([tenant_id, id], map: "risk_evaluations_tenant_id_key")
  @@unique([tenant_id, request_id], map: "risk_evaluations_tenant_request_id_key")
  @@index([tenant_id, actor_membership_id, evaluated_at(sort: Desc)], map: "risk_evaluations_tenant_actor_idx")
  @@index([tenant_id, case_id, quote_id, quote_version], map: "risk_evaluations_tenant_case_quote_idx")
  @@index([tenant_id, entity, entity_id, evaluated_at(sort: Desc)], map: "risk_evaluations_tenant_entity_idx")
  @@index([tenant_id, result, mode, evaluated_at(sort: Desc)], map: "risk_evaluations_tenant_result_idx")
}

/// This table contains check constraints and requires additional setup for migrations. Visit https://pris.ly/d/check-constraints for more info.
model vehicle_import_batches {
  id                                                                                                                          String                   @id
  tenant_id                                                                                                                   String
  batch_code                                                                                                                  String                   @db.VarChar(80)
  source_kind                                                                                                                 String                   @db.VarChar(40)
  source_key                                                                                                                  String?                  @db.VarChar(160)
  manifest_json                                                                                                               Json
  manifest_hash                                                                                                               String                   @db.Char(64)
  payload_hash                                                                                                                String                   @db.Char(64)
  item_count                                                                                                                  Int
  created_count                                                                                                               Int                      @default(0)
  duplicate_count                                                                                                             Int                      @default(0)
  conflict_count                                                                                                              Int                      @default(0)
  invalid_count                                                                                                               Int                      @default(0)
  status                                                                                                                      VehicleImportBatchStatus
  request_id                                                                                                                  String                   @db.VarChar(191)
  confirmed_by_user_id                                                                                                        String?
  confirmed_by_membership_id                                                                                                  String?
  rolled_back_by_user_id                                                                                                      String?
  rolled_back_by_membership_id                                                                                                String?
  imported_at                                                                                                                 DateTime?                @db.Timestamptz(3)
  rolled_back_at                                                                                                              DateTime?                @db.Timestamptz(3)
  row_version                                                                                                                 Int                      @default(1)
  created_at                                                                                                                  DateTime                 @default(now()) @db.Timestamptz(3)
  updated_at                                                                                                                  DateTime                 @default(now()) @db.Timestamptz(3)
  osi_vehicles                                                                                                                Vehicle[]
  tenant_memberships_vehicle_import_batches_tenant_id_confirmed_by_membership_id_confirmed_by_user_idTotenant_memberships     TenantMembership?        @relation("vehicle_import_batches_tenant_id_confirmed_by_membership_id_confirmed_by_user_idTotenant_memberships", fields: [tenant_id, confirmed_by_membership_id, confirmed_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "vehicle_import_batches_confirmed_membership_fkey")
  tenant_memberships_vehicle_import_batches_tenant_id_rolled_back_by_membership_id_rolled_back_by_user_idTotenant_memberships TenantMembership?        @relation("vehicle_import_batches_tenant_id_rolled_back_by_membership_id_rolled_back_by_user_idTotenant_memberships", fields: [tenant_id, rolled_back_by_membership_id, rolled_back_by_user_id], references: [tenantId, id, userId], onUpdate: NoAction, map: "vehicle_import_batches_rolledback_membership_fkey")
  tenants                                                                                                                     Tenant                   @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "vehicle_import_batches_tenant_fkey")
  vehicle_import_items                                                                                                        vehicle_import_items[]

  @@unique([tenant_id, batch_code], map: "vehicle_import_batches_tenant_batch_code_key")
  @@unique([tenant_id, id])
  @@unique([tenant_id, manifest_hash], map: "vehicle_import_batches_tenant_manifest_hash_key")
  @@unique([tenant_id, request_id], map: "vehicle_import_batches_tenant_request_id_key")
  @@index([tenant_id, status, created_at(sort: Desc)], map: "vehicle_import_batches_tenant_status_created_idx")
}

model vehicle_import_items {
  id                     String                  @id
  tenant_id              String
  batch_id               String
  ordinal                Int
  source_stable_id       String?                 @db.VarChar(191)
  reconciliation_key     String?                 @db.VarChar(260)
  status                 VehicleImportItemStatus
  reason_code            String?                 @db.VarChar(100)
  vehicle_id             String?
  source_json            Json
  before_json            Json?
  after_json             Json?
  created_at             DateTime                @default(now()) @db.Timestamptz(3)
  vehicle_import_batches vehicle_import_batches  @relation(fields: [tenant_id, batch_id], references: [tenant_id, id], onUpdate: NoAction, map: "vehicle_import_items_batch_fkey")
  tenants                Tenant                  @relation(fields: [tenant_id], references: [id], onUpdate: NoAction, map: "vehicle_import_items_tenant_fkey")
  osi_vehicles           Vehicle?                @relation(fields: [tenant_id, vehicle_id], references: [tenant_id, id], onUpdate: NoAction, map: "vehicle_import_items_vehicle_fkey")

  @@unique([tenant_id, batch_id, ordinal], map: "vehicle_import_items_batch_ordinal_key")
  @@unique([tenant_id, id])
  @@index([tenant_id, batch_id, status], map: "vehicle_import_items_batch_status_idx")
  @@index([tenant_id, vehicle_id], map: "vehicle_import_items_vehicle_idx")
}

enum TemplateType {
  PIC
  PGD
  NPS
  PST
}

enum TemplateScope {
  GLOBAL
  TENANT
}

enum TemplateVersionStatus {
  DRAFT
  PENDING_APPROVAL
  APPROVED
  PUBLISHED
  REJECTED
  ARCHIVED
}

enum ProjectKState {
  PENDING_VALIDATION
  VALIDATED
  RELEASED
}

enum SignalPolicy {
  HARD_BLOCK
  SOFT_ALERT
}

enum SignalKind {
  PAYMENT
  PERMITS_PARKING
  PGD_BLOCKING_DOCS
  CRATES
  THIRD_PARTIES
}

enum PgdItemStatus {
  MISSING
  SUBMITTED
  VALIDATED
  REJECTED
}

enum PgdVisibility {
  CLIENT_VIEW
  INTERNAL_VIEW
}

enum PgdResponsible {
  CLIENT
  SUPERVISOR
  DRIVER
  INTERNAL
}

enum PgdFileType {
  PDF
  PHOTO
  SIGNATURE
  OTHER
}

enum LeadType {
  L1
  L2
  L3
  L4
}

enum AcceptanceStatus {
  PENDING
  ACCEPTED
  REJECTED
}

enum TenantStatus {
  ACTIVE
  SUSPENDED
  INACTIVE
}

enum TenantMembershipStatus {
  ACTIVE
  SUSPENDED
  INACTIVE
}

enum TenantMembershipRole {
  A
  V
  K
  B
  C
  C1
  D
  E
  G
  N
  PA
  PB
  PC
  PD
  PF
  I
  PE
}

enum TenantProvisioningSource {
  MANUAL
  BACKFILL
}

enum EmployeeEmploymentStatus {
  ACTIVE
  ON_LEAVE
  SUSPENDED
  TERMINATED
}

enum EmployeeContractType {
  PERMANENT
  MOBILE_STAFF
  FIXED_TERM
  CONTRACTOR
}

enum EmployeeAvailabilityStatus {
  AVAILABLE
  LIMITED
  UNAVAILABLE
}

enum AuthSessionStatus {
  ACTIVE
  REVOKED
  COMPROMISED
  EXPIRED
}

enum AuthRefreshTokenStatus {
  ACTIVE
  ROTATED
  REVOKED
  COMPROMISED
  EXPIRED
}

enum LeadConversionStatus {
  PENDING
  READY
  CONVERTED
  SENT_TO_K
}

enum KycDocumentType {
  PASSPORT_ID
  POWER_OF_ATTORNEY
  VALUATION_LETTER
  COMPLIANCE_CHECKLIST
  TAX_REGISTRATION
  OTHER
}

enum KycDocumentStatus {
  UPLOADED
  VALIDATED
  REJECTED
}

enum SurveyType {
  PRESENCIAL
  VIRTUAL
  LISTADO
}

enum SurveyStatus {
  NOT_STARTED
  IN_PROGRESS
  SUBMITTED
}

enum SurveyPropertyType {
  CASA
  APARTAMENTO
  OFICINA
  NAVE
}

enum SurveyStairsType {
  CARACOL
  RECTAS
  MIXTO
}

enum SurveyPackLevel {
  P1
  P2
  P3
}

enum SurveyDimensionUnit {
  CM
  IN
}

enum SurveyMediaType {
  SITE
  PRE_EXISTING_DAMAGE
  NESTING_ITEM
  OTHER
}

enum LeadDerivedMode {
  NATIONAL
  IMPORT_EXPORT
  PENDING
}

enum LeadModePolicy {
  FIXED
  BY_DESTINATION
}

enum ServiceMode {
  NATIONAL
  IMPORT_EXPORT
}

enum SurveyRule {
  ALWAYS
  THRESHOLD
  OPTIONAL
}

enum PipelineCaseStatus {
  NEW_INBOX
  AWAITING_ICP
  GOVERNANCE_CONFIRMED
  REQUIREMENTS_CONFIRMED
  SURVEY_PLANNING
  SURVEY_SCHEDULED
  SURVEY_COMPLETED
  CRATING_ESTIMATE_PENDING
  PRICING_IN_PROGRESS
  QUOTE_DRAFT
  INTERNAL_REVIEW
  QUOTE_SENT
  NEGOTIATION
  WON
  LOST
  CHANGE_CONTROL
  APPROVED
  OPS_HANDOFF
}

enum PipelineCaseCommandType {
  CREATE
  UPDATE
  TRANSITION
  REOPEN
  ASSIGN_OWNER
  UNASSIGN_OWNER
}

enum PipelineCaseEvidenceType {
  SURVEY
  QUOTE
  PROJECT
  APPROVAL
  ADDENDUM
}

enum PipelineMode {
  LOCAL
  EXPORT
  IMPORT
}

enum ServiceCatalogUsage {
  PRIMARY
  COMPLEMENTARY
  BOTH
}

enum ServiceCatalogStatus {
  ACTIVE
  INACTIVE
}

enum CaseServiceSelectionSource {
  MANUAL
  DEFAULT_COMBINATION
}

enum CaseServiceItemKind {
  PRIMARY
  COMPLEMENTARY
  OTHER
}

enum CaseServiceItemSource {
  MANUAL
  DEFAULT
  OTHER
}

enum ServiceClassificationStatus {
  PENDING
}

enum SurveyCatalogVersionStatus {
  DRAFT
  ACTIVE
  RETIRED
}

enum SurveyAssignmentStatus {
  ASSIGNED
  ARRIVED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum SurveyDraftStatus {
  IN_PROGRESS
  READY_FOR_REVIEW
  PUBLISHED
  CANCELLED
}

enum SurveyPublicationStatus {
  CURRENT
  SUPERSEDED
}

enum SurveyShipmentMode {
  SEA
  AIR
  ROAD
  LOCAL
  STORAGE
}

enum SurveyItemCondition {
  GOOD
  USED
  DAMAGED
  PRE_EXISTING_DAMAGE
}

enum SurveyItemFlag {
  CRATING_CANDIDATE
  FRAGILE
  ASSEMBLE
  DISASSEMBLE
  CRANE_CANDIDATE
  VALUABLE
  OVERSIZED
}

enum SurveyMeasurementUnit {
  CM
  IN
}

enum SurveyMetricSource {
  CATALOG
  MEASURED
  DENSITY
}

enum SurveyAccessSide {
  ORIGIN
  DESTINATION
}

enum SurveyAccessFlag {
  STAIRS
  PASSENGER_ELEVATOR
  FREIGHT_ELEVATOR
  ITEM_DOES_NOT_FIT_ELEVATOR
  NARROW_PASSAGE
  LONG_CARRY
  RESTRICTED_PARKING
  LOADING_DOCK
  RESTRICTED_HOURS
  PERMIT_REQUIRED
  CRANE_OR_HOIST
  TRANSSHIPMENT
}

enum SurveyConditionKind {
  FACILITY
  INCONVENIENCE
}

enum SurveyPhotoPurpose {
  ITEM
  DAMAGE
  ORIGIN_ACCESS
  DESTINATION_ACCESS
  SPECIAL_CONDITION
  GENERAL
}

enum SurveyBlobStatus {
  ACTIVE
  QUARANTINED
  RETIRED
}

enum ClientAddressStatus {
  ACTIVE
  INACTIVE
}

enum PipelineCaseRouteRole {
  ORIGIN
  DESTINATION
  ADDITIONAL_STOP
}

enum PipelineDestinationStatus {
  CONFIRMED
  APPROXIMATE
  PENDING
}

enum PipelineIntakeChannel {
  WHATSAPP
  INSTAGRAM
  FACEBOOK
  RECOMMENDATION
  YOUTUBE
  OTHER_SOCIAL
  PROMOTION
  CALL
  EMAIL
  WEB
  REFERRED
}

enum PipelineClientProfileType {
  INDIVIDUAL
  CORPORATE
  LEAD_ACCOUNT
  COMMERCIAL
  DIPLOMATIC
}

enum PipelineCustomerType {
  L1_AGENT
  L2_INTL_DIRECT
  L3_CORPORATE
  L4_PERSONAL
}

enum PipelineSurveyMethod {
  PRESENCIAL
  VIRTUAL
  LISTADO_FOTOS
  NO_APLICA
}

enum PipelineQuoteLevel {
  BASIC
  STANDARD
  PREMIUM
}

enum PipelineEventType {
  SURVEY
  FOLLOW_UP
  DEADLINE
  SERVICE
}

enum PipelineEventStatus {
  PENDING
  DONE
  CANCELLED
}

enum LeadEstimateConfidence {
  LOW
  MED
  HIGH
}

enum AccountType {
  INDIVIDUAL
  CORPORATE
  AGENT
  GOVERNMENT
  NON_PROFIT
}

enum LocationType {
  ORIGIN
  DESTINATION
  WAREHOUSE
  OFFICE
  OTHER
}

enum ParkingDifficulty {
  EASY
  MEDIUM
  HARD
}

enum LongCarryBand {
  NONE
  LOW
  MEDIUM
  HIGH
}

enum ServiceCaseMode {
  LOCAL
  EXPORT
  IMPORT
}

enum ServiceCaseCustomerType {
  L1_AGENT
  L2_INTL_DIRECT
  L3_CORPORATE
  L4_PERSONAL
}

enum ServiceCaseStatus {
  NEW_INBOX
  AWAITING_ICP
  GOVERNANCE_CONFIRMED
  REQUIREMENTS_CONFIRMED
  SURVEY_PLANNING
  SURVEY_SCHEDULED
  SURVEY_COMPLETED
  CRATING_ESTIMATE_PENDING
  PRICING_IN_PROGRESS
  INTERNAL_REVIEW
  QUOTE_SENT
  NEGOTIATION
  CHANGE_CONTROL
  APPROVED
  OPS_HANDOFF
  CLOSED_WON
  CLOSED_LOST
}

enum SurveyMethod {
  PRESENCIAL
  VIRTUAL
  LISTADO_FOTOS
  LISTA
  FOTOS
}

enum NoDestinationCaseType {
  CASE_A_PACKING_ORIGIN
  CASE_B_ORIGIN_STORAGE
  CASE_C_PICKUP_NO_DELIVERY
}

enum QuoteLevel {
  ESTIMATE
  FINAL
}

enum QuoteStatus {
  DRAFT
  SENT
  APPROVED
  REJECTED
  EXPIRED
}

enum QuoteBlock {
  ORIGIN
  TRANSPORT
  DESTINATION
  THIRD_PARTY
  STORAGE
}

enum QuoteItemType {
  SERVICE
  SURCHARGE
  MATERIAL
  CRATING
  DISCOUNT
  TAX
}

enum CommercialEventType {
  SURVEY
  FOLLOW_UP
  DEADLINE
  SERVICE
}

enum CommercialEventStatus {
  SCHEDULED
  CONFIRMED
  DONE
  CANCELLED
}

enum CratingRequestStatus {
  NEEDED
  ESTIMATING
  APPROVED
  IN_PRODUCTION
  DELIVERED
}

enum EntityKind {
  COMPANY
  PERSON
}

enum EntityTypeCode {
  CLIENT
  CORPORATE
  ACCOUNT
  PARTNER
  AGENT
  REFERRER
  SUPPLIER
}

enum LeadSourceChannel {
  WHATSAPP
  PHONE
  EMAIL
  WEB
  REFERRAL
  PARTNER
  CORPORATE
  OTHER
}

enum LeadStage {
  NEW_LEAD
  CONTACTADO
  SURVEY_PENDIENTE
  SURVEY_REALIZADO
  COTIZANDO
  PROPUESTA_ENVIADA
  FOLLOW_UP
  GANADO
  PERDIDO
  EXPEDIENTE_ABIERTO
  EN_COORDINACION
  LISTO_PARA_HANDOFF
}

enum LeadPartyRole {
  CLIENT
  BILL_TO
  ACCOUNT
  PARTNER
  REFERRER
  CORPORATE_OWNER
  AGENT_ORIGIN
  AGENT_DESTINATION
}

enum CommissionType {
  PERCENT
  FIXED
}

enum CommissionStatus {
  PENDING
  APPROVED
  PAYABLE
  PAID
  VOID
}

enum CommissionAppliesTo {
  LEAD
  INVOICE
  COLLECTION
  MARGIN
}

enum CommercialServiceType {
  EXPORT
  IMPORT
  LOCAL_ORIGIN
  LOCAL_DESTINATION
  LOCAL_INTERNAL
  STORAGE
  CRATING
}

enum AddressRole {
  ORIGIN
  DESTINATION
  PICKUP_LOCAL
  DELIVERY_LOCAL
  STORAGE_LOCATION
  BILLING
}

enum RequirementStatus {
  PENDING
  RECEIVED
  APPROVED
  NOT_REQUIRED
}

enum MasterTariffScope {
  INTL
  LOCAL
  GOV
  CORP
}

enum TariffRateMode {
  AIR
  LCL
  FCL
  LOCAL
}

enum SurchargePricingMode {
  FIXED
  PER_UNIT
  PER_HOUR
  PER_DAY
  PER_KM
  PCT
}

enum AccountSurchargeHandling {
  CHARGE
  INCLUDE
  DISABLE
}

enum TariffOverrideScope {
  GLOBAL
  ROUTE
  MODE
  SERVICE_TYPE
}

enum ApprovalRequestStatus {
  PENDING
  APPROVED
  REJECTED
  CANCELLED
  EXPIRED
}

enum EmployeeProvisioningIdentityMode {
  NEW_GLOBAL_USER
  EXISTING_GLOBAL_USER
}

enum EmployeeProvisioningLifecycleStatus {
  IDENTITY_PENDING
  PROVISIONED_INACTIVE
  ACTIVE
  REVOKED
  TERMINATED
}

enum EmployeeProvisioningInvitationStatus {
  ISSUED
  ACCEPTED
  REVOKED
  EXPIRED
}

enum AdminIdentityInvitationStatus {
  PENDING
  CONSUMED
  REVOKED
}

enum GeoRegionAliasKind {
  CANONICAL
  HISTORICAL
  TYPO_COMPATIBILITY
  EXTERNAL
}

enum LogisticsConfigState {
  DRAFT
  SHADOW
  ACTIVE
  RETIRED
}

enum LogisticsOperationMode {
  LEGACY_ONLY
  SHADOW
  ENFORCED
}

enum OsiCustodyStatus {
  DRIVER
  SUPERVISOR
}

enum OsiHandshakeStatus {
  PENDING
  COMPLETED
  REJECTED
}

enum OsiKind {
  EXTERNAL
  INTERNAL
}

enum PtfSuggestionStatus {
  PENDING
  APPLIED
  IGNORED
  ESCALATED
}

enum QuoteChangeOrderItemChange {
  ADDED
  MODIFIED
  REMOVED
}

enum QuoteChangeOrderPolicyStatus {
  DRAFT
  ACTIVE
  RETIRED
}

enum QuoteChangeOrderStatus {
  DRAFT
  PENDING_APPROVAL
  APPROVED
  PENDING_CUSTOMER
  ACCEPTED
  EXECUTED
  REJECTED
  CANCELLED
  EXPIRED
  SUPERSEDED
}

enum RiskDecisionResult {
  PASS
  REVIEW_REQUIRED
  BLOCKED
}

enum RiskEngineOperationMode {
  LEGACY_ONLY
  SHADOW
  ENFORCED
}

enum RiskEvaluationMode {
  SHADOW
  ENFORCED
}

enum RiskRuleConditionType {
  DISTANCE_OVER_KM
  REGION_IN_SET
  LOGISTIC_FLAG_PRESENT
  MARGIN_BELOW_PERCENT
}

enum RiskRuleState {
  DRAFT
  SHADOW
  ACTIVE
  RETIRED
}

enum TransportRuleScope {
  ZONE_TYPE
  REGION
  ROUTE
  DISTANCE_BAND
}

enum VehicleImportBatchStatus {
  IMPORTED
  ROLLED_BACK
  REJECTED
}

enum VehicleImportItemStatus {
  CREATED
  DUPLICATE
  CONFLICT
  INVALID
  SKIPPED
  ROLLED_BACK
}

enum VehicleOperationalStatus {
  AVAILABLE
  IN_USE
  UNAVAILABLE
  RETIRED
}

enum MaterialRecordStatus {
  ACTIVE
  INACTIVE
}

enum MaterialLocationKind {
  WAREHOUSE_ROOT
  ZONE
  AISLE
  RACK
  LEVEL
  BIN
  RECEIVING
  DISPATCH
  QUARANTINE
  OTHER
}

enum MaterialMovementType {
  RECEIPT
  TRANSFER_OUT
  TRANSFER_IN
  ISSUE
  CONSUMPTION
  RETURN
  ADJUSTMENT_POSITIVE
  ADJUSTMENT_NEGATIVE
}

enum MaterialReservationStatus {
  RESERVED
  ASSIGNED
  DISPATCHED
  RELEASED
  CANCELLED
}

enum PackingRecipeVersionStatus {
  DRAFT
  ACTIVE
  RETIRED
}

enum PackingFormulaType {
  FIXED
  PER_ITEM
  PER_LENGTH
  PER_AREA
}

enum MaterialRequirementSnapshotStatus {
  CURRENT
  SUPERSEDED
}

enum MaterialPurchaseRequestStatus {
  REQUESTED
  APPROVED
  ORDERED
  RECEIVED
  CANCELLED
}

enum AssetResourceType {
  TOOL
  EQUIPMENT
}

enum AssetSerialPolicy {
  REQUIRED
  OPTIONAL
  NONE
}

enum AssetRecordStatus {
  ACTIVE
  INACTIVE
}

enum AssetOperationalStatus {
  AVAILABLE
  ASSIGNED
  IN_USE
  MAINTENANCE
  OUT_OF_SERVICE
  LOST
  RETIRED
}

enum AssetPhysicalCondition {
  GOOD
  FAIR
  DAMAGED
  UNSAFE
}

enum AssetCostType {
  ACQUISITION
  REPLACEMENT
  INTERNAL_RATE
  MAINTENANCE
}

enum AssetReservationStatus {
  ACTIVE
  FULFILLED
  CANCELLED
  EXPIRED
}

enum AssetAssignmentStatus {
  PLANNED
  ACTIVE
  RETURNED
  CANCELLED
}

enum AssetInspectionType {
  PRE_HANDOUT
  RETURN
  PERIODIC
  MAINTENANCE
}

enum AssetIncidentType {
  DAMAGE
  LOSS
  FAILURE
  MISUSE
  ACCIDENT
  OBSERVATION
}

enum AssetIncidentSeverity {
  LOW
  MEDIUM
  HIGH
  CRITICAL
}

enum AssetIncidentStatus {
  OPEN
  UNDER_REVIEW
  RESOLVED
  CLOSED
}

enum AssetMaintenanceTrigger {
  CALENDAR
  USAGE_HOURS
  ODOMETER
  CYCLES
}

enum AssetMaintenanceType {
  PREVENTIVE
  CORRECTIVE
  INSPECTION
  REPAIR
}

enum AssetMaintenanceStatus {
  SCHEDULED
  IN_PROGRESS
  COMPLETED
  CANCELLED
}

enum ExternalAvailabilityStatus {
  UNCONFIRMED
  AVAILABLE
  UNAVAILABLE
}

enum ExternalResourceReservationStatus {
  REQUESTED
  CONFIRMED
  COMPLETED
  CANCELLED
}

enum LogisticsRuleFamily {
  LABOR
  TIME
  TRANSPORT
  MATERIAL
  ASSET
  EXTERNAL
  PER_DIEM
  LODGING
  TOLL
  PARKING
  PERMIT
  ZONE
  CRATING
}

enum LogisticsRuleState {
  DRAFT
  ACTIVE
  SUPERSEDED
  INACTIVE
}

enum LogisticsCalculationStatus {
  VALID
  CONFLICT
}

enum LogisticsPlanStatus {
  ACTIVE
  CLOSED
}

enum LogisticsPlanRevisionStatus {
  PUBLISHED
  SUPERSEDED
}

enum LogisticsPlanItemFamily {
  LABOR
  TIME
  TRANSPORT
  MATERIAL
  ASSET
  EXTERNAL
  TRAVEL
  PERMIT
  CRATING
}

enum LogisticsAvailabilityStatus {
  AVAILABLE
  PARTIAL
  UNAVAILABLE
  PENDING_CONFIRMATION
  NOT_APPLICABLE
}

enum LogisticsPriceStatus {
  CONFIRMED
  REFERENCED
  PENDING
  NOT_APPLICABLE
}

enum LogisticsSource {
  SURVEY
  SERVICE
  ROUTE
  ADMIN_RULE
  INVENTORY
  ASSET
  VEHICLE
  PROVIDER
  CRATING
}

enum LogisticsIssueSeverity {
  INFO
  WARNING
  BLOCKER
}

enum LogisticsIssueStatus {
  OPEN
  RESOLVED
}

enum CostingFamily {
  LABOR
  TRANSPORT
  MATERIAL
  CRATING
  ASSET
  TRAVEL
  THIRD_PARTY
  FREIGHT
  CUSTOMS
  PERMIT
  ADDITIONAL
  RISK
  CURRENCY_COMPENSATION
}

enum CostingSource {
  SURVEY
  SERVICE
  COMBO
  ADMIN
  MOTOR
  PROVIDER
  MATERIAL_COST
  ASSET_COST
  VEHICLE_COST
  EXCHANGE_RATE
}

enum CostingEconomicClass {
  PR
  EX
  DE
}

enum CostingRuleState {
  DRAFT
  ACTIVE
  SUPERSEDED
  INACTIVE
}

enum CostingCalculationStatus {
  VALID
  CONFLICT
}

enum CostingRevisionStatus {
  PUBLISHED
  SUPERSEDED
}

enum CostingIssueSeverity {
  INFO
  WARNING
  BLOCKER
}

enum CostingIssueStatus {
  OPEN
  RESOLVED
}

enum CostingPriceStatus {
  RECOMMENDED
  BELOW_RECOMMENDED
  BELOW_MINIMUM
  ABOVE_RECOMMENDED
  NO_MARGIN
}

enum CostingOverrideKind {
  COST
  EXCHANGE_RATE
  MARGIN
  SUGGESTED_PRICE
  CLASSIFICATION
}

enum CostingOverrideStatus {
  APPLIED
  AUTHORIZATION_REQUIRED
  AUTHORIZED
  REJECTED
}

enum CostingAuthorizationDecision {
  AUTHORIZED
  REJECTED
}

enum QuoteProposalState {
  DRAFT
  READY
  SENT
  ACCEPTED
  REJECTED
  EXPIRED
  SUPERSEDED
  CANCELLED
}

enum QuoteLineSourceKind {
  COSTING
  MANUAL
}

enum QuoteLinePriceStatus {
  CONFIRMED
  PENDING
}

enum QuoteDispatchChannel {
  MANUAL
  EMAIL
  WHATSAPP
  PORTAL
}

enum QuoteClientDecisionKind {
  ACCEPTED
  REJECTED
}

enum ZoneRuleKind {
  ZONE_TYPE_BASE
  REGION_OVERRIDE
  DISTANCE_REVIEW
  DISTANCE_BLOCK
  ROUTE_SURCHARGE
}
