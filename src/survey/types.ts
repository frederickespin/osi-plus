export type SurveyArea = Readonly<{ areaRef: string; code: string; name: string }>;
export type SurveyArticle = Readonly<{ articleRef: string; code: string; name: string; aliases: readonly string[]; frequentAreaRefs: readonly string[]; defaultVolumeM3: number | null; defaultWeightKg: number | null }>;
export type SurveyCatalog = Readonly<{ catalogRef: string; version: number; articles: readonly SurveyArticle[]; areas: readonly SurveyArea[]; conditions: readonly { conditionRef: string; code: string; name: string; kind: string }[] }>;
export type SurveyAssignment = Readonly<{ assignmentRef: string; caseRef: string; caseCode: string; clientDisplayName: string | null; evaluator: { displayName: string }; scheduledStart: string; scheduledEnd: string | null; status: string; arrivalAt: string | null; punctualityConfirmedAt: string | null; context: { origin?: string; destination?: string; services?: readonly { name: string }[] }; instruction: string | null; version: number; surveyRef: string | null }>;
export type SurveyItem = Readonly<{ itemRef: string; article: { articleRef: string; code: string; name: string }; area: SurveyArea; shipmentMode: string; quantity: number; condition: string; flags: readonly string[]; dimensions: { unit: "CM" | "IN"; length: number; width: number; height: number } | null; normalizedCm: { length: number; width: number; height: number } | null; unitVolumeM3: number | null; unitWeightKg: number | null; note: string | null; version: number; photos: readonly { photoRef: string; purpose: string }[] }>;
export type SurveyAccess = Readonly<{ accessRef: string; side: "ORIGIN" | "DESTINATION"; floorNumber: number | null; stairsFloors: number | null; elevatorAvailable: boolean | null; elevatorFloor: number | null; parkingDistanceM: number | null; flags: readonly string[]; notes: string | null; version: number; photos: readonly { photoRef: string; purpose: string }[] }>;
export type SurveyDraft = Readonly<{ surveyRef: string; assignmentRef: string; caseRef: string; caseCode: string; clientDisplayName: string | null; status: string; revision: number; version: number; routeVersion: number; serviceSelectionRef: string; catalog: SurveyCatalog; items: readonly SurveyItem[]; access: readonly SurveyAccess[]; totals: { quantity: number; volumeM3: number; weightKg: number }; notes: string | null; updatedAt: string }>;

